# SEO Cluster Analyzer 🚀

Мощный инструмент для автоматизированной кластеризации и анализа SEO запросов с использованием данных SERP, семантического анализа и машинного обучения.

## 🎯 Основные возможности

- **Интеллектуальная кластеризация** запросов на основе SERP данных и семантики
- **Классификация интента** (информационный/коммерческий/навигационный)
- **Определение стадии воронки** продаж
- **Автоматическая фильтрация брендов** и моделей
- **Расчет SEO метрик** (KEI, сложность, конкуренция)
- **Экспорт в Excel** с подробной аналитикой
- **GUI интерфейс** для удобной работы
- **Batch обработка** множества групп запросов

## 📋 Требования

- Python 3.10+ (рекомендуется 3.13)
- Windows / Linux / macOS
- API ключ [XMLStock](https://xmlstock.com/?ref=vechkasov) для получения SERP данных

## 🔧 Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/yourusername/seo_claster.git
cd seo_claster
```

### 2. Создайте виртуальное окружение

```bash
# Windows
python -m venv venv313
venv313\Scripts\activate

# Linux/macOS
python3 -m venv venv313
source venv313/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r data/temp/requirements.txt
```

### 4. Настройте конфигурацию

```bash
# Скопируйте файл примера
copy config_local.py.example config_local.py  # Windows
cp config_local.py.example config_local.py    # Linux/macOS
```

Откройте `config_local.py` и укажите ваш API ключ XMLStock:

```python
# Получите ключ на https://xmlstock.com/?ref=vechkasov
XMLSTOCK_API_KEY = "your_user:your_api_key"
```

## 🚀 Быстрый старт

### Подготовка данных

1. Поместите CSV файл с запросами в папку `semantika/`
2. Формат CSV: первая колонка должна содержать поисковые запросы
3. Пример: `semantika/домофон.csv`

### Запуск анализа

**Простой запуск одной группы:**

```bash
python main.py домофон
```

**Обработка всех групп из папки semantika/:**

```bash
python main.py
```

**Просмотр списка доступных групп:**

```bash
python main.py --list-groups
```

**Настройка параметров кластеризации:**

```bash
# Минимум 6 общих URL для объединения в кластер
python main.py домофон 6

# Минимум 6 общих URL + максимум 50 запросов в кластере
python main.py домофон 6 50

# Без ограничения размера кластера
python main.py домофон 6 0
```

**Итеративная кластеризация (maxmin):**

```bash
python main.py домофон maxmin
```

### GUI интерфейс

Для работы через графический интерфейс:

```bash
python -m gui
# или
gui.bat  # Windows
```

## 📂 Структура проекта

```
seo_claster/
├── semantika/          # Входные CSV файлы с запросами
├── output/             # Результаты анализа (Excel, графы, JSON)
│   └── groups/         # Результаты по каждой группе
├── data/               # Базы данных и временные файлы
│   └── databases/      # SQLite базы (master_queries.db, serp_data.db)
├── seo_analyzer/       # Основной код анализатора
│   ├── clustering/     # Алгоритмы кластеризации
│   ├── classification/ # Классификация интента и воронки
│   ├── analysis/       # SERP анализ и LSI
│   ├── metrics/        # Расчет SEO метрик
│   └── export/         # Экспорт результатов
├── pipeline/           # Pipeline обработки
├── gui/                # GUI приложение (PyQt6)
└── scripts/            # Вспомогательные скрипты
```

## 🔑 Получение API ключа XMLStock

1. Зарегистрируйтесь на [XMLStock](https://xmlstock.com/?ref=vechkasov)
2. Получите приветственный бонус для тестирования
3. Скопируйте API ключ из личного кабинета
4. Вставьте ключ в `config_local.py`

**Преимущества XMLStock:**
- От 10₽ за 1000 запросов (Яндекс Live, Google XML)
- От 20₽ за 1000 запросов (Яндекс Search API)
- Быстрый старт без прокси и капчи
- Поддержка мобильной выдачи, картинок, видео

## 📊 Результаты анализа

После обработки в папке `output/groups/название_группы/` вы найдете:

- **`название_группы.xlsx`** - Основной Excel файл с кластерами и метриками
- **`название_группы_graph.html`** - Интерактивный граф кластеров
- **`название_группы_hierarchy.json`** - Иерархия разделов сайта (при включении)
- **`filtered_brands.txt`** - Отфильтрованные бренды
- **`filtered_models.txt`** - Отфильтрованные модели

**Структура Excel файла:**
- Лист "Кластеры" - Основная таблица с кластерами
- Лист "Статистика" - Общая статистика по проекту
- Листы по интентам - Группировка по типам запросов
- Листы по воронке - Группировка по стадиям воронки

## ⚙️ Дополнительные опции

### Пакетные команды (Windows)

```bash
СТАРТ.bat           # Быстрый запуск всех групп
gui.bat             # Запуск GUI
groups.bat          # Показать список групп
dev_commands.bat    # Интерактивное меню команд
check_sizes.bat     # Проверка размеров файлов кода
```

### Интеграция с Yandex Direct

Для получения данных о трафике и CPC настройте в `config_local.py`:

```python
YANDEX_DIRECT_ENABLED = True
YANDEX_DIRECT_TOKEN = "ваш_токен"
YANDEX_DIRECT_GEO_ID = 213  # Москва
```

Получение токена:

```bash
python get_yandex_direct_token.py
```

### Построение иерархии сайта

Для автоматического построения иерархии страниц (требует DeepSeek API):

```python
ENABLE_HIERARCHY_ANALYSIS = True
DEEPSEEK_API_KEY = "ваш_ключ"
```

## 🛠️ Полезные скрипты

```bash
# Проверка прогресса SERP
python check_serp_progress.py

# Проверка незавершенных задач
python check_pending_details.py

# Сброс застрявших задач
python reset_stuck_processing.py

# Проверка размеров файлов кода
python check_file_sizes.py
```

## 📝 Конфигурация

Основные параметры в `config_local.py`:

```python
# XMLStock API
XMLSTOCK_API_KEY = "user:key"

# Кластеризация
CLUSTERING_CONFIG_OVERRIDES = {
    "postprocess": {
        "max_cluster_size": 20,  # Макс. запросов в кластере
        "threshold_step": 1,     # Шаг повышения порога
    }
}

# SERP
SERP_CONFIG_OVERRIDES = {
    "serp_similarity_threshold": 5,  # Мин. общих URL
    "top_results": 10,               # Глубина анализа
}
```

## 🐛 Решение проблем

**Ошибка импорта модулей:**

```bash
# Переустановите зависимости
pip install --upgrade -r data/temp/requirements.txt
```

**Ошибка SQLite:**

```bash
FIX_SQLITE.bat  # Автоматическое исправление
```

**Проблемы с кодировкой:**

- Убедитесь, что CSV файлы в UTF-8
- Используйте Excel "Сохранить как" → UTF-8 CSV

## 📖 Документация

Подробная документация в папке `docs/`:
- Архитектура проекта
- Алгоритмы кластеризации
- API документация
- Руководства по оптимизации

## 🤝 Поддержка

- **Issues:** [GitHub Issues](https://github.com/yourusername/seo_claster/issues)
- **Email:** your@email.com

## 📜 Лицензия

MIT License - свободное использование для коммерческих и некоммерческих проектов.

---

**Создано с использованием:**
- [XMLStock](https://xmlstock.com/?ref=vechkasov) - SERP API для Яндекс и Google
- Python 3.13
- Sentence Transformers
- NetworkX
- PyQt6

**⭐ Поставьте звезду на GitHub, если проект оказался полезен!**

