# 🚀 Как использовать SEO Analyzer

## Быстрый старт (3 команды)

### 1. Посмотрите что есть
```bash
python main.py --list-groups
```

### 2. Запустите обработку
```bash
python main.py
```
*Обработает ВСЕ группы параллельно (самый быстрый режим)*

### 3. Результаты
```
output/groups/
├── скуд/
│   ├── seo_analysis.xlsx      ← Откройте этот файл
│   └── ...
├── видеонаблюдение/
└── ...
```

---

## Основные команды

| Что хотите | Команда |
|-----------|---------|
| **Обработать всё** | `python main.py` |
| Одну группу | `python main.py скуд` |
| Список групп | `python main.py --list-groups` |
| Статистика доменов | `python manage_groups.py stats` |
| Интерактивное меню | `groups.bat` |

---

## Работа с группами

### Создать новую группу

1. Создайте файл `semantika/новая_группа.csv`:
   ```csv
   Запрос;frequency_world;frequency_exact
   ваш запрос 1;1000;800
   ваш запрос 2;500;400
   ```

2. Запустите:
   ```bash
   python main.py новая_группа
   ```

### Обработать все группы

```bash
python main.py              # Параллельно (быстро)
python main.py --sequential # Последовательно (для отладки)
```

### Обработать одну группу

```bash
python main.py скуд
python main.py видеонаблюдение
python main.py домофон
```

---

## Результаты

### Где искать

```
output/groups/имя_группы/
├── seo_analysis.xlsx           ← Основной отчет Excel
├── seo_analysis_full.csv       ← Полная таблица CSV
├── clusters_summary.csv        ← Сводка по кластерам
├── brands.csv                  ← Список брендов
├── dashboard.html              ← Интерактивный дашборд
└── serp_data.db                ← База SERP данных
```

### Что смотреть

1. **`seo_analysis.xlsx`** - главный файл
   - Листы: Full Data, Top-1000, Clusters, Statistics
   
2. **`dashboard.html`** - откройте в браузере
   - Графики и визуализация

3. **`clusters_summary.csv`** - сводка по кластерам
   - Темы, размеры, метрики

---

## Статистика и анализ

### Глобальная статистика доменов

```bash
python manage_groups.py stats
```

Покажет:
- Всего доменов в базе
- Коммерческих / информационных
- Топ-10 доменов

### Информация о конкретном домене

```bash
python manage_groups.py domain ozon.ru
```

Покажет:
- Классификацию домена
- Статистику по группам
- Уровень коммерциализации

---

## Дополнительные опции

### Изменить параметры SERP

```bash
python main.py --serp-mode strict           # Строгая кластеризация
python main.py --serp-mode balanced         # Сбалансированная (по умолчанию)
python main.py --serp-mode soft             # Мягкая
```

### Экспорт брендов

```bash
python main.py скуд --export-brands
```

### Пропустить генерацию форм (быстрее)

```bash
python main.py --skip-forms
```

---

## Конфигурация

### API ключ xmlstock

Создайте `config_local.py`:
```python
XMLSTOCK_API_KEY = "ваш_user:ваш_ключ"
```

### Настройки кластеризации

В `config_local.py`:
```python
CLUSTERING_CONFIG_OVERRIDES = {
    "serp_advanced": {
        "min_common_urls": 7,      # Мин. общих URL
        "top_positions": 20,       # Глубина SERP
        "mode": "strict",          # Режим
    }
}
```

---

## Troubleshooting

### Не находит группу
```bash
# Проверьте список
python main.py --list-groups

# Проверьте файл
dir semantika\
```

### Ошибка API
```bash
# Проверьте ключ
type config_local.py

# Или установите
set XMLSTOCK_API_KEY=user:key
```

### Медленно работает
```bash
# Убедитесь что используете кэш
python main.py --list-groups  # Должны быть 💾 у групп

# Используйте параллельный режим
python main.py  # По умолчанию параллельный
```

---

## Справка

### Полная справка CLI
```bash
python main.py --help
```

### Документация

- `НОВЫЙ_РЕЖИМ_ГОТОВ.md` - Новый режим по умолчанию
- `СИСТЕМА_ГРУПП_ГОТОВА.md` - Система групп
- `docs/QUICK_START_GROUPS.md` - Быстрый старт
- `docs/guides/MULTI_GROUP_SYSTEM.md` - Полное руководство

### Интерактивное меню

```bash
groups.bat  # Windows
```

---

## Примеры

### Пример 1: Первый запуск
```bash
# 1. Посмотрите группы
python main.py --list-groups

# 2. Запустите
python main.py

# 3. Откройте результаты
explorer output\groups\
```

### Пример 2: Добавление новой группы
```bash
# 1. Создайте файл
notepad semantika\crm.csv

# 2. Обработайте
python main.py crm

# 3. Проверьте
python manage_groups.py stats
```

### Пример 3: Переанализ
```bash
# Просто запустите заново
python main.py

# Используется кэш - быстро!
```

---

## Полезные ссылки

- [Telegram канал](https://t.me/your_channel) - новости и обновления
- [GitHub Issues](https://github.com/your_repo/issues) - сообщить об ошибке
- [Документация](docs/) - подробные гайды

---

**Версия:** 1.3  
**Дата:** Декабрь 2025

**Вопросы? Читайте документацию в папке `docs/`**

