# ⚙️ Настройка SOFT-кластеризации по SERP

## 🎯 Проблема: Все запросы в одном кластере

Если вы видите что **слишком много запросов** попадает в один кластер (например, все 5000+ запросов в кластере "скуд"), нужно **увеличить строгость** кластеризации.

---

## 📊 Параметры кластеризации

### 1. `--serp-similarity-threshold` (Порог схожести)

**Что это**: Минимальное количество общих URL в топ-30, чтобы запросы попали в один кластер.

**Значения**:
- `7` (старое по умолчанию) - **СЛИШКОМ МЯГКО** для узких тематик
- `15` (новое по умолчанию) - **РЕКОМЕНДУЕТСЯ** для большинства тематик
- `20-25` - строгая кластеризация, меньше кластеров
- `5-10` - мягкая кластеризация, больше кластеров

**Пример**:
```bash
python main.py data.csv --serp-similarity-threshold 15
```

### 2. `--max-cluster-size` (Максимальный размер кластера)

**Что это**: Максимальное количество запросов в одном кластере. Если кластер больше - он будет разбит на подкластеры.

**Значения**:
- `50` (по умолчанию) - оптимально для большинства случаев
- `30-40` - строгое ограничение, больше мелких кластеров
- `100+` - мягкое ограничение, позволяет большие кластеры

**Пример**:
```bash
python main.py data.csv --max-cluster-size 50
```

### 3. `--serp-top-positions` (Глубина анализа)

**Что это**: Сколько позиций в SERP учитывать при сравнении (из 30 доступных).

**Значения**:
- `30` (по умолчанию) - учитываем всю выдачу
- `10-15` - учитываем только топ позиции (строже)
- `20-25` - средний вариант

**Пример**:
```bash
python main.py data.csv --serp-top-positions 20
```

---

## 🚀 Рекомендуемые настройки

### Для узких тематик (все запросы про одно - например "СКУД", "SEO", "CRM"):

```bash
python main.py data.csv --skip-embeddings \
  --serp-similarity-threshold 20 \
  --max-cluster-size 30 \
  --serp-top-positions 20
```

**Результат**: Более строгая кластеризация, больше мелких кластеров.

### Для широких тематик (разные темы):

```bash
python main.py data.csv --skip-embeddings \
  --serp-similarity-threshold 10 \
  --max-cluster-size 100 \
  --serp-top-positions 30
```

**Результат**: Мягкая кластеризация, меньше крупных кластеров.

### Универсальные настройки (рекомендуется):

```bash
python main.py data.csv --skip-embeddings \
  --serp-similarity-threshold 15 \
  --max-cluster-size 50
```

**Результат**: Баланс между строгостью и гибкостью.

---

## 📈 Как выбрать правильные параметры

### Шаг 1: Запустите с настройками по умолчанию

```bash
python main.py data.csv --skip-embeddings
```

### Шаг 2: Проверьте статистику кластеров

Смотрите в логе:
```
✓ Кластеризовано: 5292 запросов в 37 кластеров
• Кластеры с 2+ запросами: 11
• Одиночные кластеры: 26
• Максимальный размер кластера: 4859 запросов  <-- ПРОБЛЕМА!
• Средний размер кластера: 143.0 запросов
⚠️  Найдено 1 кластеров с >100 запросами!
   Рекомендуется увеличить --serp-similarity-threshold (текущий: 15)
```

### Шаг 3: Если есть предупреждение - увеличьте порог

Если видите **"Найдено N кластеров с >100 запросами"**, увеличьте `--serp-similarity-threshold`:

```bash
# Было 15 - стало 20
python main.py data.csv --skip-embeddings --serp-similarity-threshold 20
```

### Шаг 4: Повторяйте пока не получите хорошее распределение

**Хорошее распределение**:
- ✅ Максимальный кластер < 100 запросов
- ✅ Средний размер < 50 запросов
- ✅ Нет предупреждений о больших кластерах

---

## 🔍 Что означают кластеры

### Кластер = Группа запросов с похожей выдачей

Если два запроса имеют **15+ общих URL** в топ-30 Яндекса, они попадают в один кластер.

**Пример кластера** (правильно):
- "купить iphone 15"
- "iphone 15 цена"
- "где купить iphone 15"
- "iphone 15 москва"

Все эти запросы показывают **интернет-магазины** → один кластер.

**Пример НЕправильного кластера**:
- "скуд" (общий запрос)
- "контроллер скуд" (оборудование)
- "монтаж скуд" (услуга)
- "скуд что это" (информационный)

Эти запросы имеют **разный интент** → должны быть в разных кластерах!

---

## ✅ Текущий запуск

Сейчас запущен анализ с **новыми параметрами**:
- ✅ `--serp-similarity-threshold 15` (вместо 7)
- ✅ `--max-cluster-size 50` (ограничение на размер)

После завершения проверьте результаты и при необходимости увеличьте порог до 20-25.

---

## 📝 Примеры команд

### Очень строгая кластеризация (мало кластеров, но точные):
```bash
python main.py data.csv --skip-embeddings --serp-similarity-threshold 25 --max-cluster-size 30
```

### Мягкая кластеризация (много кластеров):
```bash
python main.py data.csv --skip-embeddings --serp-similarity-threshold 10 --max-cluster-size 100
```

### Балансная кластеризация (РЕКОМЕНДУЕТСЯ):
```bash
python main.py data.csv --skip-embeddings --serp-similarity-threshold 15 --max-cluster-size 50
```

---

## 💡 Советы

1. **Узкая тематика** (все запросы про одно) → увеличивайте порог до 20-25
2. **Широкая тематика** (разные темы) → уменьшайте порог до 10-12
3. **Смотрите на предупреждения** в логе - они подскажут оптимальные значения
4. **Итерируйте** - запускайте несколько раз с разными параметрами

Текущий анализ использует **оптимальные параметры** для вашей тематики! ✨


