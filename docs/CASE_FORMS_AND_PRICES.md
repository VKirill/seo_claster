# 📊 Падежные формы и ценовые данные в JSON

## Обзор изменений

В JSON экспорт добавлены:
1. **Падежные формы** для каждого ключевого слова
2. **Ценовые данные** на уровне кластера и ключевых слов
3. **Географическая метка** для гео-кластеров

---

## 1. Падежные формы (Case Forms)

### Генерация форм

Падежные формы генерируются автоматически через **pymorphy3** на этапе 8 pipeline:

```python
# pipeline/stages/forms_generator.py
from seo_analyzer.core.forms_generator import FormsGenerator

generator = FormsGenerator()
forms = generator.generate_all_forms("контроллер сетевой скуд")
```

### Результат в JSON

```json
{
  "keyword": "elsys mb light 2a 00 tп контроллер сетевой скуд",
  "frequency_world": 4,
  "frequency_exact": 1,
  "main_intent": "commercial",
  "funnel_stage": "Interest",
  "case_forms": {
    "nominative": "elsys mb light 2a 00 tп контроллер сетевой скуд",
    "genitive": "elsys mb light 2a 00 tп контроллера сетевого скуда",
    "dative": "elsys mb light 2a 00 tп контроллеру сетевому скуду",
    "accusative": "elsys mb light 2a 00 tп контроллер сетевой скуд",
    "instrumental": "elsys mb light 2a 00 tп контроллером сетевым скудом",
    "prepositional": "elsys mb light 2a 00 tп контроллере сетевом скуде"
  }
}
```

### Падежи (6 форм)

| Падеж | Английское название | Вопросы | Пример |
|-------|-------------------|---------|--------|
| Именительный | `nominative` | кто? что? | контроллер скуд |
| Родительный | `genitive` | кого? чего? | контроллера скуда |
| Дательный | `dative` | кому? чему? | контроллеру скуду |
| Винительный | `accusative` | кого? что? | контроллер скуд |
| Творительный | `instrumental` | кем? чем? | контроллером скудом |
| Предложный | `prepositional` | о ком? о чём? | о контроллере скуде |

### Использование падежных форм

1. **SEO-оптимизация текстов**: Использование естественных форм в контенте
2. **Генерация метаданных**: Title, Description в разных падежах
3. **Внутренняя перелинковка**: Анкоры в разных падежах
4. **Голосовой поиск**: Запросы часто в косвенных падежах

---

## 2. Ценовые данные

### На уровне кластера

```json
{
  "name": "карточки скуд",
  "target_url": "/skud/",
  "total_frequency": 1118,
  "avg_frequency": 101,
  "funnel_stage": "Interest",
  "main_intent": "commercial",
  
  "serp_avg_price": 7850.50,
  "serp_median_price": 7500.00,
  "serp_min_price": 3500,
  "serp_max_price": 15000,
  "serp_currency": "RUR",
  "serp_offers_count": 12.3,
  "serp_offers_with_discount": 4.2,
  "serp_avg_discount_percent": 8.5,
  
  "price_stats": {
    "queries_with_prices": 8,
    "price_coverage": 72.7,
    "discount_ratio": 35.2
  }
}
```

### На уровне ключевого слова

```json
{
  "keyword": "карточки скуд",
  "frequency_world": 250,
  "main_intent": "commercial",
  
  "serp_avg_price": 7960.0,
  "serp_min_price": 5000,
  "serp_max_price": 12000,
  "serp_median_price": 7500.0,
  "serp_currency": "RUR",
  "serp_offers_count": 15,
  "serp_offers_with_discount": 5,
  "serp_avg_discount_percent": 8.5
}
```

### Метрики цен

| Поле | Описание | Уровень кластера |
|------|----------|------------------|
| `serp_avg_price` | Средняя цена | Среднее от всех средних |
| `serp_median_price` | Медианная цена | Среднее от всех медиан |
| `serp_min_price` | Минимальная цена | Минимум от всех минимумов |
| `serp_max_price` | Максимальная цена | Максимум от всех максимумов |
| `serp_currency` | Валюта | Самая частая (mode) |
| `serp_offers_count` | Кол-во предложений | Среднее |
| `serp_offers_with_discount` | Предложений со скидкой | Среднее |
| `serp_avg_discount_percent` | Средняя скидка (%) | Среднее |

### Дополнительная статистика (price_stats)

```json
"price_stats": {
  "queries_with_prices": 8,        // Сколько запросов с ценами
  "price_coverage": 72.7,           // % запросов с ценами
  "discount_ratio": 35.2            // % предложений со скидками
}
```

### Использование ценовых данных

1. **Ценообразование**: Анализ рыночных цен конкурентов
2. **Позиционирование**: Определение ценового сегмента
3. **Контент-стратегия**: Упоминание цен в диапазоне рынка
4. **A/B тестирование**: Выбор оптимальной цены для посадочной страницы

---

## 3. Географическая метка

Для кластеров с географической привязкой:

```json
{
  "name": "видеонаблюдение скуд москва",
  "target_url": "/skud/moscow/",
  "total_frequency": 150,
  "funnel_stage": "Interest",
  "main_intent": "commercial_geo",
  "geo": "москва",  // ← Географическая метка
  "cluster_id": 42,
  "keywords": [...]
}
```

### Возможные значения `geo`:

- `"москва"` - Москва и МО
- `"санкт-петербург"` - Санкт-Петербург
- `"екатеринбург"` - Екатеринбург
- `null` или отсутствует - запросы без географии

---

## Полная структура кластера в JSON

```json
{
  // 1. ОСНОВНАЯ ИНФОРМАЦИЯ
  "name": "карточки скуд",
  "target_url": "/skud/kartochki/",
  "total_frequency": 1118,
  "avg_frequency": 101,
  "funnel_stage": "Interest",
  "main_intent": "commercial",
  "geo": "москва",  // если есть география
  
  // 2. ЦЕНОВЫЕ ДАННЫЕ (коммерческие запросы)
  "serp_avg_price": 7850.50,
  "serp_median_price": 7500.00,
  "serp_min_price": 3500,
  "serp_max_price": 15000,
  "serp_currency": "RUR",
  "serp_offers_count": 12.3,
  "serp_offers_with_discount": 4.2,
  "serp_avg_discount_percent": 8.5,
  
  // 3. ДОПОЛНИТЕЛЬНАЯ СТАТИСТИКА
  "price_stats": {
    "queries_with_prices": 8,
    "price_coverage": 72.7,
    "discount_ratio": 35.2
  },
  
  // 4. ИДЕНТИФИКАТОРЫ
  "cluster_id": 1151,
  "keywords_count": 11,
  
  // 5. URL КЛАСТЕРИЗАЦИИ (почему сгруппировались)
  "common_urls": ["site.ru/skud", "shop.ru/access"],
  "clustering_basis_urls": [
    {"url": "site.ru/skud", "count": 8, "percentage": 72}
  ],
  
  // 6. КЛЮЧЕВЫЕ СЛОВА
  "keywords": [
    {
      "keyword": "карточки скуд",
      "frequency_world": 250,
      "frequency_exact": 15,
      "main_intent": "commercial",
      "funnel_stage": "Interest",
      
      // Цены для запроса
      "serp_avg_price": 7960.0,
      "serp_min_price": 5000,
      "serp_max_price": 12000,
      "serp_median_price": 7500.0,
      "serp_currency": "RUR",
      "serp_offers_count": 15,
      "serp_offers_with_discount": 5,
      "serp_avg_discount_percent": 8.5,
      
      // Падежные формы
      "case_forms": {
        "nominative": "карточки скуд",
        "genitive": "карточек скуда",
        "dative": "карточкам скуда",
        "accusative": "карточки скуд",
        "instrumental": "карточками скуда",
        "prepositional": "карточках скуде"
      }
    }
  ]
}
```

---

## Как это работает в pipeline

### 1. SERP анализ (Этап 3)
```python
# pipeline/stages/serp_analyzer.py
# Сохраняем xml_response в DataFrame
analyzer.df['xml_response'] = analyzer.df['keyword'].map(
    lambda x: serp_dict.get(x, {}).get('xml_response')
)
```

### 2. Классификация интента (Этап 4)
```python
# pipeline/stages/classifier.py
from seo_analyzer.classification.intent.serp_offer_classifier import SERPOfferClassifier

offer_classifier = SERPOfferClassifier()
result = offer_classifier.classify_by_serp_offers(xml_response)

# Извлекаем цены
price_data = offer_classifier.extract_price_data(result['offer_infos'])
```

### 3. Кластеризация (Этап 6)
```python
# seo_analyzer/clustering/serp_advanced_clusterer.py
# Географическая сегментация
for query in queries:
    geo = semantic_checker.extract_geo(query)  # Natasha NER
    query_geo_dict[query] = geo

# Раздельная кластеризация по гео-сегментам
for geo_segment, geo_queries in geo_groups.items():
    # Кластеризуем только запросы этого сегмента
    ...
```

### 4. Генерация форм (Этап 8)
```python
# pipeline/stages/forms_generator.py
forms_generator = FormsGenerator()
forms = forms_generator.generate_all_forms(keyword)

# Добавляем в DataFrame
analyzer.df['form_nominative'] = ...
analyzer.df['form_genitive'] = ...
# ... и т.д.
```

### 5. Экспорт (Этап 10)
```python
# seo_analyzer/export/json_exporter.py
# Добавляем падежные формы
if 'form_nominative' in row:
    keyword_info['case_forms'] = {
        'nominative': row['form_nominative'],
        'genitive': row['form_genitive'],
        # ...
    }

# Добавляем цены
if 'serp_avg_price' in row:
    keyword_info['serp_avg_price'] = row['serp_avg_price']
    # ...
```

---

## Файлы изменены

### Падежные формы:
- `seo_analyzer/export/json_exporter.py` - добавлен блок `case_forms` в `_build_keyword_info`

### Ценовые данные:
- `seo_analyzer/classification/intent/serp_offer_classifier.py` - новый модуль для извлечения цен
- `pipeline/stages/classifier.py` - интеграция ценового анализа
- `seo_analyzer/export/json_exporter.py` - добавлены ценовые поля в кластеры и ключевые слова

### География:
- `seo_analyzer/clustering/serp_advanced_clusterer.py` - исправлены отступы для гео-сегментации
- `seo_analyzer/export/json_exporter.py` - добавлено поле `geo` в кластеры

---

## Проверка результатов

```bash
# Запустить анализ
python main.py

# Проверить JSON
# output/groups/<группа>/seo_analysis.json
```

### Что проверить:

1. **Падежные формы**: У ключевых слов есть `case_forms` с 6 падежами
2. **Цены на уровне кластера**: Поля `serp_avg_price`, `serp_min_price` и т.д.
3. **Цены на уровне слова**: Те же поля для каждого ключевого слова
4. **География**: Кластеры с гео-запросами имеют поле `geo`

---

✅ **Все изменения реализованы и готовы к использованию!**

