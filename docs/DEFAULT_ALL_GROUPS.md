# Изменение: По умолчанию обработка всех групп

## ✅ Что изменилось

### Раньше
```bash
python main.py                    # Обрабатывал один файл из конфига
python main.py --process-all-groups  # Нужно было явно указывать
```

### Теперь (NEW!)
```bash
python main.py                    # Обрабатывает ВСЕ группы (по умолчанию)
python main.py скуд               # Обрабатывает только группу "скуд"
```

## 🚀 Новый подход

### По умолчанию: ВСЕ группы

Просто запустите без аргументов:
```bash
python main.py
```

**Что произойдет:**
```
📁 Режим: Обработка всех групп из semantika/
⚡ Режим: ПАРАЛЛЕЛЬНАЯ обработка (быстрее благодаря SERP кэшу)

📊 ГРУППА 1/4: скуд - СТАРТ
📊 ГРУППА 2/4: видеонаблюдение - СТАРТ
📊 ГРУППА 3/4: домофон - СТАРТ
📊 ГРУППА 4/4: example_group - СТАРТ

✅ Все группы обработаны
```

### Одна группа: укажите название

```bash
python main.py скуд
```

**Что произойдет:**
```
📁 Режим: Обработка группы 'скуд'

✓ Группа: скуд
✓ Файл: semantika\скуд.csv
✓ Output: output\groups\скуд

... обработка ...
```

## 📋 Все варианты использования

### 1. Все группы (параллельно)
```bash
python main.py
```

### 2. Все группы (последовательно)
```bash
python main.py --sequential
```

### 3. Одна группа
```bash
python main.py скуд
python main.py видеонаблюдение
python main.py example_group
```

### 4. Показать список групп
```bash
python main.py --list-groups
```

### 5. Обычный файл (обратная совместимость)
```bash
python main.py semantika/мой_файл.csv --single-file
```

## 🎯 Преимущества

✅ **Проще использовать**
- Не нужно помнить `--process-all-groups`
- Просто `python main.py` и всё!

✅ **Интуитивнее**
- Без аргументов = всё обработать
- С аргументом = конкретная группа

✅ **Быстрее работать**
- Один запуск для всех групп
- Параллельная обработка по умолчанию

## 🔧 Технические детали

### Логика работы

**`pipeline/cli.py`:**

```python
# Если не указан файл - обрабатываем все группы
if input_file is None:
    print("📁 Режим: Обработка всех групп")
    runner = MultiGroupRunner(args)
    await runner.run_all_groups(parallel=True)
    return

# Если указан файл - проверяем, это группа или путь
if input_file:
    # Ищем группу по имени
    group = manager.get_group(input_file)
    if group:
        # Обрабатываем группу
        args.group = input_file
        analyzer = SEOAnalyzer(args)
        await analyzer.run()
```

### Изменённые файлы

**`pipeline/args_builder.py`:**
- `input_file` теперь `nargs="?"`, `default=None`
- Убран аргумент `--group`
- Убран аргумент `--process-all-groups`
- Добавлен `--single-file` для обратной совместимости

**`pipeline/cli.py`:**
- Новая логика определения режима
- Автоматическое определение группы по имени
- Обработка всех групп по умолчанию

**`pipeline/stages/data_loader.py`:**
- Обработка случая когда `input_file` = `None`

## 📊 Сравнение

### Старый способ

```bash
# Все группы
python main.py --process-all-groups

# Одна группа
python main.py --group скуд

# Показать группы
python main.py --list-groups
```

### Новый способ (проще!)

```bash
# Все группы
python main.py

# Одна группа
python main.py скуд

# Показать группы
python main.py --list-groups
```

## 🎓 Примеры

### Пример 1: Ежедневная обработка

**Раньше:**
```bash
python main.py --process-all-groups
```

**Теперь:**
```bash
python main.py
```

### Пример 2: Работа с одной группой

**Раньше:**
```bash
python main.py --group скуд
```

**Теперь:**
```bash
python main.py скуд
```

### Пример 3: Последовательная обработка

**Раньше:**
```bash
python main.py --process-all-groups --sequential
```

**Теперь:**
```bash
python main.py --sequential
```

## 💡 Рекомендации

### Типичные сценарии

#### Ежедневная работа
```bash
# Просто запускайте каждый день
python main.py
```

#### Работа над конкретной темой
```bash
# Обрабатывайте только нужную группу
python main.py скуд
```

#### Первый запуск нового проекта
```bash
# 1. Посмотрите что есть
python main.py --list-groups

# 2. Обработайте всё
python main.py
```

## 🔄 Обратная совместимость

✅ **Все старые команды работают:**

```bash
# Работает (через --single-file)
python main.py semantika/файл.csv --single-file

# Работает
python main.py --list-groups

# Работает
python main.py --sequential
```

## 🚨 Важно

### Если нужен старый режим (один файл)

```bash
python main.py semantika/файл.csv --single-file
```

### Если случайно запустили без аргументов

Не волнуйтесь! Система:
1. Покажет режим работы
2. Обработает все группы параллельно
3. Использует кэш где возможно

Остановить: `Ctrl+C`

## 📚 См. также

- [ASYNC_GROUPS_AND_TOP20.md](ASYNC_GROUPS_AND_TOP20.md) - Асинхронная обработка
- [MULTI_GROUP_SYSTEM.md](guides/MULTI_GROUP_SYSTEM.md) - Система групп
- [QUICK_START_GROUPS.md](QUICK_START_GROUPS.md) - Быстрый старт

---

**Версия:** 1.2  
**Дата:** Декабрь 2025  
**Статус:** ✅ Готово к использованию

