# 🔑 Как настроить API ключ xmlstock

## 3 способа (выберите любой)

### ✅ Способ 1: config_local.py (рекомендуется)

**Самый удобный для постоянной работы**

1. Скопируйте пример:
   ```bash
   copy config_local.py.example config_local.py
   ```

2. Откройте `config_local.py` и вставьте свой ключ:
   ```python
   XMLSTOCK_API_KEY = "b3c2f28ec0a90b44e486af55c2f6b270"
   ```

3. Используйте без указания ключа:
   ```bash
   python main.py semantika/скуд.csv --enable-serp
   ```

**Преимущества:**
- ✅ Не нужно вводить ключ каждый раз
- ✅ Файл в .gitignore (не попадет в GitHub)
- ✅ Работает автоматически

**⚠️ ВАЖНО:** Файл `config_local.py` уже в `.gitignore` и **не попадет в git!**

---

### 📌 Способ 2: Переменная окружения

**Удобно для серверов и CI/CD**

#### Windows:
```bash
# Один раз в сессию
set XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270

# Навсегда (в системных переменных)
setx XMLSTOCK_API_KEY "b3c2f28ec0a90b44e486af55c2f6b270"
```

#### Linux/Mac:
```bash
# Один раз в сессии
export XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270

# Навсегда (добавить в ~/.bashrc или ~/.zshrc)
echo 'export XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270' >> ~/.bashrc
source ~/.bashrc
```

Затем запускайте без аргумента:
```bash
python main.py semantika/скуд.csv --enable-serp
```

---

### 🔧 Способ 3: Аргумент командной строки

**Для разовых запусков или тестирования**

```bash
python main.py semantika/скуд.csv \
  --enable-serp \
  --xmlstock-api-key b3c2f28ec0a90b44e486af55c2f6b270
```

**Минусы:**
- ❌ Нужно вводить каждый раз
- ❌ Ключ виден в истории команд
- ❌ Легко ошибиться при вводе

---

## 🔍 Приоритет загрузки ключа

Система проверяет в таком порядке:

1. **--xmlstock-api-key** (аргумент командной строки)
2. **config_local.py** (локальный файл)
3. **XMLSTOCK_API_KEY** (переменная окружения)

Первый найденный ключ используется.

---

## 📋 Проверка что ключ работает

```bash
python main.py semantika/скуд.csv --enable-serp --max-serp-queries 1
```

Вы должны увидеть:
```
✓ API ключ загружен из config_local.py
🔍 ЭТАП 4.5: SERP анализ
🔄 Инициализация SERP анализатора...
📊 Анализ SERP для 1 топ запросов...
```

Если видите:
```
⚠️  API ключ xmlstock не найден!
```

Значит ключ не загрузился. Проверьте:
1. Файл `config_local.py` существует?
2. В нем правильный синтаксис Python?
3. Ключ без лишних пробелов и кавычек?

---

## 🔒 Безопасность

### Что в .gitignore (НЕ попадет в git):

✅ `config_local.py` - ваш ключ  
✅ `.env` - файл с переменными  
✅ `*.key` - файлы с ключами  
✅ `secrets.json` - секреты  

### Что можно комитить (примеры):

✅ `config_local.py.example` - шаблон без ключа  
✅ Вся остальная конфигурация  

### ⚠️ НИКОГДА не комитьте:

❌ Реальный API ключ  
❌ Пароли  
❌ Токены  
❌ Любые секреты  

---

## 💡 Рекомендация

**Используйте config_local.py** - это самый удобный способ:

```bash
# 1. Один раз настроили
copy config_local.py.example config_local.py
# Отредактировали, вставили ключ

# 2. Всегда работает автоматически
python main.py data.csv --enable-serp
python main.py data2.csv --enable-serp
python main.py data3.csv --enable-serp
```

Не нужно каждый раз вводить ключ! 🎉

---

## 🆘 Где взять API ключ?

1. Зарегистрируйтесь на **xmlstock.com**
2. Получите API ключ в личном кабинете
3. Формат ключа: `username:apikey` или просто длинная строка
4. Вставьте в `config_local.py`

---

## 📞 Помощь

Если что-то не работает:

1. Проверьте `.gitignore` - там должен быть `config_local.py`
2. Убедитесь что файл назван **точно** `config_local.py` (не `.txt`, не `.py.example`)
3. Синтаксис Python: `XMLSTOCK_API_KEY = "ваш_ключ"`

**Вопросы?** Создайте Issue на GitHub или напишите в поддержку.

---

**Версия:** 1.1  
**Статус:** ✅ Безопасно, не попадет в git

