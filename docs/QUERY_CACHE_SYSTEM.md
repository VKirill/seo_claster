# 🗄️ Система кэширования запросов

## 📋 Описание

**Проблема:** При каждом запуске анализа:
- ❌ CSV файл загружается заново
- ❌ Нормализация запросов пересчитывается
- ❌ Лемматизация пересчитывается
- ❌ Извлечение ключевых фраз (key phrases) пересчитывается
- ❌ NER (Named Entity Recognition) пересчитывается
- ❌ Дедупликация пересчитывается

**Решение:** Система кэширования обработанных запросов в SQLite БД

```
Первый запуск группы:
CSV → Предобработка (normalized, NER, key phrases) → Сохранение в БД → Анализ

Второй и последующие запуски:
БД → Анализ (мгновенно, без предобработки!)
```

## ⚡ Преимущества

| Без кэша | С кэшем |
|----------|---------|
| 🐌 CSV загружается каждый раз | ⚡ Загрузка из БД (SQL query) |
| 🐌 Нормализация ~30-60 сек | ⚡ Уже в БД |
| 🐌 NER ~20-40 сек | ⚡ Уже в БД |
| 🐌 Key phrases ~10-20 сек | ⚡ Уже в БД |
| 🐌 Дедупликация ~5-15 сек | ⚡ Уже применена |
| **Итого:** 1-2 минуты | **Итого:** 1-2 секунды |

### 🎯 Основные выгоды:

1. **Быстрый повторный запуск** - мгновенный старт при повторной кластеризации
2. **Эксперименты с настройками** - можно пересчитывать кластеры без ожидания
3. **Автоматическое обновление** - кэш пересоздаётся при изменении CSV
4. **История данных** - можно хранить разные версии обработки

## 📊 Что хранится в кэше

### Таблица `query_groups` (метаданные):
- `group_name` - название группы
- `csv_file_path` - путь к CSV файлу
- `csv_hash` - MD5 hash CSV файла (для определения изменений)
- `total_queries` - всего запросов в CSV
- `unique_queries` - уникальных запросов после дедупликации
- `duplicates_removed` - количество удалённых дублей
- `imported_at` - дата импорта
- `updated_at` - дата обновления

### Таблица `cached_queries` (обработанные запросы):
- `keyword` - исходный запрос
- `frequency_world` / `frequency_exact` - частотности
- `normalized` - нормализованная форма
- `lemmatized` - лемматизированная форма
- `words_count` - количество слов
- `main_words` - главные слова (key phrases)
- `key_phrase` - ключевая фраза
- `ner_entities` - именованные сущности (NER)
- `ner_locations` - локации (NER)
- `extra_data` - дополнительные поля (JSON)

## 🚀 Использование

### 1. Обычный запуск (с автоматическим кэшированием)

```bash
# Первый запуск - создаёт кэш
python main.py

# Второй запуск - использует кэш
python main.py
```

**Вывод при первом запуске:**
```
📁 Работа с группой: скуд
✓ Группа: скуд
✓ Файл: semantika\скуд.csv
✓ Загружено 15234 запросов

🔧 ЭТАП 2: Предобработка
🔄 Нормализация запросов...
✓ Нормализация завершена
🔄 Извлечение ключевых фраз...
✓ Извлечены ключевые фразы
🔄 Извлечение именованных сущностей (NER)...
✓ Извлечены именованные сущности

💾 Сохранение обработанных запросов в кэш...
💾 Кэш сохранён: 13156 запросов для группы 'скуд'
  ⚡ Следующий запуск будет мгновенным (без предобработки)
```

**Вывод при втором запуске:**
```
📁 Работа с группой: скуд
✓ Группа: скуд
⚡ Загрузка из кэша...
📦 Кэш загружен: 13156 запросов для группы 'скуд'
  📊 Дубликатов удалено при импорте: 2078
  📅 Кэш создан: 2025-12-08 15:30:45

🔧 ЭТАП 2: Предобработка
⚡ Данные загружены из кэша
✓ Предобработка пропущена (normalized, lemmatized, NER, key phrases уже в кэше)
✓ Уникальных запросов: 13156
```

### 2. Принудительное обновление кэша

```bash
# Игнорировать кэш и пересоздать его
python main.py --force-refresh
```

Используйте когда:
- CSV файл изменился вручную
- Хотите пересчитать предобработку с новыми настройками
- Подозреваете что кэш повреждён

### 3. Автоматическое обновление при изменении CSV

Кэш **автоматически** пересоздаётся если:
- CSV файл изменился (по MD5 hash)
- Добавились/удалились запросы
- Изменились частотности

```bash
# Редактируем CSV
notepad semantika\скуд.csv

# Запускаем анализ - кэш обновится автоматически
python main.py
```

**Вывод:**
```
🔄 CSV файл изменился, обновление кэша...
✓ Загружено 15500 запросов  (было 15234)
```

## 🛠️ Управление кэшем

### Командная строка

```bash
# Список всех закэшированных групп
python manage_query_cache.py --list

# Информация о группе
python manage_query_cache.py --info скуд

# Статистика по кэшу
python manage_query_cache.py --stats

# Очистить кэш группы
python manage_query_cache.py --clear скуд

# Очистить весь кэш
python manage_query_cache.py --clear-all
```

### Интерактивное меню (Windows)

```bash
cache_manager.bat
```

Откроется меню:
```
═══════════════════════════════════════════════════════════════════════════════
                     🗄️  УПРАВЛЕНИЕ КЭШЕМ ЗАПРОСОВ
═══════════════════════════════════════════════════════════════════════════════

 1. 📋 Список закэшированных групп
 2. 📊 Статистика по кэшу
 3. ℹ️  Информация о группе
 4. 🗑️  Очистить кэш группы
 5. 🧹 Очистить весь кэш
 0. ❌ Выход

═══════════════════════════════════════════════════════════════════════════════
```

## 📊 Примеры вывода

### Список групп

```bash
python manage_query_cache.py --list
```

```
📦 Закэшированных групп: 3
================================================================================

📊 Группа: видеонаблюдение
   Файл: semantika\видеонаблюдение.csv
   Запросов: 56,949 (удалено дублей: 6,894)
   Кэш создан: 2025-12-08 14:25:30
   Hash CSV: 7f3a9c8d1e2b4567...

📊 Группа: домофон
   Файл: semantika\домофон.csv
   Запросов: 53,614 (удалено дублей: 8,030)
   Кэш создан: 2025-12-08 14:28:15
   Hash CSV: 9d2c7f1a4e8b3265...

📊 Группа: скуд
   Файл: semantika\скуд.csv
   Запросов: 13,156 (удалено дублей: 2,078)
   Кэш создан: 2025-12-08 15:30:45
   Hash CSV: 2a8f5c3d7e1b9046...
```

### Статистика

```bash
python manage_query_cache.py --stats
```

```
📊 Статистика кэша
================================================================================
Файл БД:            output/query_cache.db
Размер БД:          45.32 MB
Групп в кэше:       3
Всего запросов:     123,719
Удалено дублей:     17,002
Средний размер:     381.25 bytes/запрос
```

### Информация о группе

```bash
python manage_query_cache.py --info скуд
```

```
📊 Группа: скуд
================================================================================
Файл CSV:           semantika\скуд.csv
Hash файла:         2a8f5c3d7e1b9046f5a3c8d2e7b4091a
Всего запросов:     15,234
Уникальных:         13,156
Удалено дублей:     2,078
Кэш создан:         2025-12-08 15:30:45
Обновлён:           2025-12-08 15:30:45

✅ CSV файл не изменился - кэш актуален
```

## ⚙️ Техническая информация

### Расположение БД

```
output/
  query_cache.db  ← Кэш всех групп
  serp_data.db    ← SERP данные (отдельная БД)
```

### Размер кэша

| Запросов | Размер БД | Примерно |
|----------|-----------|----------|
| 10,000 | ~3.5 MB | Небольшая группа |
| 50,000 | ~17 MB | Средняя группа |
| 100,000 | ~35 MB | Большая группа |
| 500,000 | ~175 MB | Очень большая |

### Индексы для производительности

Созданы индексы на:
- `(group_name, keyword)` - быстрый поиск запроса в группе
- `(group_name, normalized)` - поиск по нормализованной форме

### Определение изменений CSV

Используется MD5 hash всего содержимого CSV файла:
- ✅ Детектирует любые изменения (добавление, удаление, изменение строк)
- ✅ Быстрое вычисление (< 1 сек для больших файлов)
- ✅ Надёжное определение

## 🔧 Продвинутое использование

### Программный доступ

```python
from seo_analyzer.core.cache import QueryCacheDatabase

# Инициализация
cache = QueryCacheDatabase()

# Проверка существования
if cache.group_exists('скуд'):
    print("Группа в кэше")

# Проверка изменений CSV
if cache.is_csv_changed('скуд', Path('semantika/скуд.csv')):
    print("CSV изменился")

# Загрузка
df = cache.load_queries('скуд')

# Сохранение
cache.save_queries(
    group_name='скуд',
    csv_path=Path('semantika/скуд.csv'),
    df=processed_df,
    duplicates_removed=2078
)

# Статистика
stats = cache.get_group_stats('скуд')
print(f"Запросов: {stats['unique_queries']}")

# Очистка
cache.clear_group_cache('скуд')
```

### Миграция старых данных

Если у вас есть обработанные CSV файлы, импортируйте их в кэш:

```python
from pathlib import Path
import pandas as pd
from seo_analyzer.core.cache import QueryCacheDatabase

# Загружаем обработанные данные
df = pd.read_csv('output/groups/скуд/seo_analysis_full.csv')

# Сохраняем в кэш
cache = QueryCacheDatabase()
cache.save_queries(
    group_name='скуд',
    csv_path=Path('semantika/скуд.csv'),
    df=df[['keyword', 'frequency_world', 'frequency_exact', 
           'normalized', 'lemmatized', 'words_count',
           'main_words', 'key_phrase', 'ner_entities', 'ner_locations']],
    duplicates_removed=0
)
```

## ❓ FAQ

**Q: Где хранится кэш?**  
A: В `output/query_cache.db` (SQLite БД)

**Q: Что делать если кэш повреждён?**  
A: Очистите его: `python manage_query_cache.py --clear <группа>`

**Q: Обновляется ли кэш автоматически при изменении CSV?**  
A: Да, при изменении CSV кэш пересоздаётся автоматически

**Q: Можно ли использовать кэш для одного файла (не группы)?**  
A: Да, но только для групп из `semantika/` кэш включается автоматически

**Q: Сколько места занимает кэш?**  
A: ~350 bytes на запрос (для 100,000 запросов ~35 MB)

**Q: Влияет ли кэш на результаты?**  
A: Нет, результаты идентичны - кэшируется только предобработка

**Q: Нужно ли очищать кэш вручную?**  
A: Нет, но можно если нужно освободить место или сбросить данные

## 🎯 Рекомендации

### ✅ Используйте кэш для:

- Экспериментов с параметрами кластеризации
- Тестирования разных threshold для SERP
- Пересчёта метрик с новыми формулами
- Разработки новых функций

### ⚠️ Очищайте кэш если:

- CSV файл изменился но hash не обновился (редко)
- Изменилась логика предобработки
- Подозреваете повреждение данных
- Нужно освободить дисковое пространство

### 💡 Лучшие практики:

1. Не трогайте кэш - он управляется автоматически
2. Используйте `--force-refresh` только при необходимости
3. Периодически проверяйте размер кэша: `python manage_query_cache.py --stats`
4. Делайте бэкап БД перед большими изменениями

## 📝 История изменений

| Дата | Версия | Изменение |
|------|--------|-----------|
| 2025-12-08 | 1.0 | Первая версия системы кэширования |

---

**Система кэширования запросов делает повторные запуски мгновенными!** ⚡





