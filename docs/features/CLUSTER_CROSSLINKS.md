# Кросс-ссылки между кластерами (Перелинковка)

## 📌 Описание

Система автоматически анализирует связи между кластерами и добавляет информацию о связанных кластерах для построения внутренней перелинковки сайта.

## 🎯 Зачем это нужно?

При создании контента для SEO важно правильно связывать страницы между собой через внутренние ссылки. Система анализирует:

1. **Пересечение URL в SERP** - если два кластера имеют общие страницы в поисковой выдаче, значит они семантически близки
2. **Общие ключевые слова** - кластеры с похожими словами должны быть связаны перелинковкой
3. **Похожий интент** - коммерческие запросы связываются с коммерческими, информационные с информационными

## 📊 Как это работает

### 1. Анализ связей

Система запускается автоматически перед экспортом и анализирует все кластеры:

```python
from seo_analyzer.analysis import ClusterRelationshipAnalyzer

analyzer = ClusterRelationshipAnalyzer(
    min_url_overlap=3,      # Минимум 3 общих URL для связи
    min_word_overlap=2,     # Минимум 2 общих ключевых слова
    max_related_clusters=5  # Максимум 5 связанных кластеров
)

relationships = analyzer.analyze_relationships(df)
```

### 2. Результат

Для каждого кластера находятся связанные кластеры, отсортированные по силе связи (количество общих URL/слов).

### 3. Экспорт

Информация экспортируется в трёх форматах:

#### CSV
Колонка `related_clusters` содержит названия связанных кластеров через запятую:

```csv
cluster_name,related_clusters
"купить скуд","скуд цена, контроллер скуд, установка скуд"
```

#### Excel
Колонка **"Связанные кластеры (перелинковка)"** с названиями связанных кластеров.

#### JSON
Массив объектов с детальной информацией:

```json
{
  "name": "купить скуд",
  "related_clusters": [
    {
      "cluster_id": 15,
      "cluster_name": "скуд цена",
      "link_strength": 18
    },
    {
      "cluster_id": 23,
      "cluster_name": "контроллер скуд",
      "link_strength": 12
    }
  ],
  "keywords": [...]
}
```

## 🔧 Настройка

Параметры можно изменить в `pipeline/stages/exporter.py`:

```python
relationship_analyzer = ClusterRelationshipAnalyzer(
    min_url_overlap=3,      # Увеличьте для более строгих связей
    min_word_overlap=2,     # Минимум общих ключевых слов
    max_related_clusters=5  # Количество связанных кластеров
)
```

### Рекомендуемые настройки

| Тип сайта | min_url_overlap | max_related_clusters | Описание |
|-----------|----------------|---------------------|----------|
| Узкая ниша | 5+ | 3-5 | Строгая перелинковка только близких тем |
| Широкая ниша | 3-4 | 5-7 | Больше связей между кластерами |
| Контент-завод | 2-3 | 7-10 | Максимум связей для большой сети статей |

## 📝 Как использовать для перелинковки

### 1. При создании контента

Открываете Excel файл и смотрите колонку **"Связанные кластеры"**:

```
Кластер: "купить скуд"
Связанные: "скуд цена, контроллер скуд, установка скуд"
```

**Действия:**
- Создаёте статью по кластеру "купить скуд"
- В тексте добавляете ссылки на статьи по связанным кластерам
- Анкоры берёте из названий кластеров

### 2. При написании ТЗ копирайтеру

```
Тема статьи: Купить СКУД
Основные ключи: [список ключей из кластера]

Перелинковка (обязательно добавить в текст):
- Ссылка на статью "СКУД цена" (анкор: цена СКУД, стоимость СКУД)
- Ссылка на статью "Контроллер СКУД" (анкор: контроллер доступа)
- Ссылка на статью "Установка СКУД" (анкор: установить СКУД)
```

### 3. При аудите перелинковки

Используйте JSON файл для программной проверки:

```python
import json

with open('seo_analysis_hierarchy.json') as f:
    data = json.load(f)

for cluster in data['subclusters']:
    print(f"Кластер: {cluster['name']}")
    print(f"URL: {cluster['target_url']}")
    print("Должен ссылаться на:")
    for related in cluster['related_clusters']:
        print(f"  - {related['cluster_name']} (сила связи: {related['link_strength']})")
```

## ⚙️ Техническая информация

### Модули

- `cluster_relationship_analyzer.py` - основной анализ связей
- `cluster_data_grouper.py` - группировка данных кластеров
- `cluster_relationship_applier.py` - применение к DataFrame

### Интеграция

Автоматически запускается в `pipeline/stages/exporter.py` перед экспортом:

```python
# Анализируем связи
relationships = relationship_analyzer.analyze_relationships(analyzer.df)

# Добавляем колонку в DataFrame
analyzer.df = apply_cluster_relationships(analyzer.df, relationships)

# Передаём в экспортеры
analyzer.json_exporter = JSONExporter(relationships=relationships)
```

## 🎨 Пример использования

### Входные данные

Кластер 1: "купить скуд" (15 запросов)
- SERP URLs: [ozon.ru, market.ru, skud-online.ru, ...]

Кластер 2: "скуд цена" (8 запросов)
- SERP URLs: [ozon.ru, market.ru, skud-online.ru, ...]

**Пересечение:** 18 общих URL из 20 → **Сильная связь!**

### Результат

```json
{
  "name": "купить скуд",
  "related_clusters": [
    {
      "cluster_name": "скуд цена",
      "link_strength": 18
    }
  ]
}
```

**Вывод:** При создании страницы "Купить СКУД" обязательно добавить ссылку на страницу "СКУД цена".

## ✅ Преимущества

1. **Автоматический анализ** - не нужно вручную искать связи между кластерами
2. **Основано на SERP** - связи определяются по реальным данным поисковой выдачи
3. **Готово к использованию** - названия кластеров = готовые анкоры для ссылок
4. **Экспорт в 3 форматах** - CSV для Excel, JSON для автоматизации, Excel для ручной работы
5. **Сортировка по силе** - самые сильные связи идут первыми

## 🚀 Дальнейшее развитие

Планируется добавить:
- [ ] Учёт интента при построении связей (коммерческие → коммерческие)
- [ ] Учёт этапа воронки (awareness → consideration → decision)
- [ ] Визуализация графа связей между кластерами
- [ ] Экспорт схемы перелинковки в формате mind map

