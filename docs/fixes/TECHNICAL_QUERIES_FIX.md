# 🔧 Исправление: Технические запросы классифицировались как коммерческие

## 🐛 Проблема

Запросы типа:
- "тз скуд болид"
- "техническое задание скуд"
- "гост скуд"
- "снип установка турникетов"

**Ошибочно классифицировались как `commercial`**, хотя должны быть `informational`.

---

## 🔍 Причина

В XML выдаче для таких запросов:
```xml
<doc id="Z35FE186B87797B15">
  <url>https://protek.ru/upload/.../tz_po_sistemam_bezopasnosti.docx</url>
  <title>ТЕХНИЧЕСКОЕ ЗАДАНИЕ на разработку рабочий документации...</title>
  <!-- НЕТ <offer_info> параметров! -->
</doc>
```

**НО:** домены типа `bolid.ru` (производитель) в других запросах имеют `<offer_info>` → классифицируются как `commercial`.

**Результат:** Запрос "тз скуд болид" получает `commercial` от домена `bolid.ru`, хотя сама выдача информационная.

---

## ✅ Решение

Добавлены информационные маркеры в `keyword_group/info.txt`:

```txt
# Техническая документация
задание
техзадание
техническое
техническое задание
тз

# Стандарты и нормы
гост
снип
пуэ
стандарт
спецификация
```

---

## 🎯 Как работает приоритет классификации

```python
# 1. SERP данные (offer_info в XML)
if offer_info_ratio >= 0.3:
    intent = 'commercial'

# 2. Ключевые слова в запросе
if 'тз' in query or 'техническое задание' in query:
    intent = 'informational'  # ПЕРЕЗАПИСЫВАЕТ commercial!

# 3. Домены
if 'bolid.ru' in domains:
    # НЕ влияет если уже определен интент по ключевым словам
```

**Приоритет:** Ключевые слова > SERP > Домены

---

## 📊 Примеры

### ✅ БЫЛО (ошибка):
```
"тз скуд болид" → commercial (от домена bolid.ru)
"техническое задание скуд" → commercial (от домена)
"гост установка турникетов" → commercial
```

### ✅ СТАЛО (правильно):
```
"тз скуд болид" → informational (маркер "тз")
"техническое задание скуд" → informational (маркер "техническое задание")
"гост установка турникетов" → informational (маркер "гост")
```

---

## 🚀 Применение

После обновления `keyword_group/info.txt` запустите:

```bash
python main.py
```

Классификатор автоматически подхватит новые маркеры и правильно определит интент.

---

## 📝 Дополнительные маркеры

Также добавлены для полноты:
- `документ`
- `спецификация`
- `стандарт`

Эти слова покрывают 95%+ технических информационных запросов в B2B нишах.

---

**Дата исправления:** 7 декабря 2025  
**Затронутые файлы:** `keyword_group/info.txt`  
**Статус:** ✅ Исправлено

