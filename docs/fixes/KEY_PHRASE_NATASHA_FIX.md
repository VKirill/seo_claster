# 🔧 Исправление key_phrase: Natasha с сохранением контекста

## Проблема

**Запрос**: `"биометрическая скуд в школе"`  
**Было**: `key_phrase = "биометрическая скуд в школе"` (вся фраза)  
**Ожидалось**: `key_phrase = "биометрическая скуд в школе"` ✅ (контекст важен!)

**НО ТАКЖЕ:**
- `"скуд для офиса цена"` → должно быть `"скуд для офиса"` (без "цена")
- `"тз на скуд болид"` → должно быть `"тз на скуд болид"` (все слова)

### Почему так происходило?

1. **Lemma возвращал `None`** для некоторых токенов → падали ошибки
2. **Предложные обороты исключались** → терялся контекст ("в школе", "для офиса")
3. **Лишние слова включались** → "цена", "москва" не должны быть в key_phrase
4. **Natasha ошибается с POS** → "скуд" определяется как ADJ вместо NOUN

---

## Решение

### 1. ✅ Fallback для lemma
```python
# БЫЛО
lemma = token.lemma  # Может быть None!

# СТАЛО
lemma = token.lemma if token.lemma else token.text  # ✅ Fallback
```

---

### 2. ✅ ВКЛЮЧЕНИЕ предложных оборотов (контекст важен!)

**Пример**:
```
"биометрическая скуд в школе"

Синтаксис Natasha:
  ┌► биометрическая amod
┌─└─ скуд
│ ┌► в              case    ← предлог!
└►└─ школе          obl     ← обстоятельство места
```

**Решение**: **ВКЛЮЧАЕМ nmod/obl ВМЕСТЕ с предлогами** (контекст важен!):

```python
# Существительное-модификатор (nmod, obl) - ВКЛЮЧАЯ с предлогами
elif other_token.rel in ['nmod', 'obl'] and other_token.pos == 'NOUN':
    # Ищем предлог перед этим существительным
    preposition = None
    for t in doc.tokens:
        if t.head_id == other_token.id and t.rel == 'case' and t.pos == 'ADP':
            preposition = t
            break
    
    # Добавляем предлог (если есть) + существительное
    if preposition and preposition.id not in processed_tokens:
        chunk_tokens.append(preposition)  # "в", "для", "на"
        processed_tokens.add(preposition.id)
    
    chunk_tokens.append(other_token)  # Существительное
    processed_tokens.add(other_token.id)
```

**Результат**:
- ✅ `"биометрическая скуд в школе"` → `"биометрическая скуд в школе"` (контекст сохранен!)
- ✅ `"скуд для офиса"` → `"скуд для офиса"` (целевое назначение важно!)

---

### 3. ✅ Фильтр лишних слов в конце

**Пример проблемы**:
```
"скуд для офиса цена"

Natasha находит группу: "скуд для офиса цена" 
Но "цена" - это атрибут, не часть основного понятия!
```

**Решение**: **Убираем лишние слова-атрибуты в конце фразы**:

```python
# Слова-модификаторы, которые НЕ являются частью основного понятия
exclude_tail_words = {
    'цена', 'стоимость', 'купить', 'заказать', 'москва', 'спб', 
    'санкт-петербург', 'недорого', 'дешево', 'отзыв', 'фото'
}

# Убираем лишние слова в конце
words = best_chunk.split()
while words and words[-1] in exclude_tail_words:
    words.pop()
```

**Результат**:
- ✅ `"скуд для офиса цена"` → `"скуд для офиса"` ("цена" убрана)
- ✅ `"установка видеонаблюдения москва"` → `"установка видеонаблюдения"` ("москва" убрана)
- ✅ `"биометрическая скуд в школе"` → `"биометрическая скуд в школе"` (контекст сохранен!)

---

### 4. ✅ Рекурсивный сбор модификаторов

**Пример**:
```
"система контроля доступа"

Синтаксис:
система (NOUN, head)
    └─ контроля (NOUN, rel=nmod)
        └─ доступа (NOUN, rel=nmod)
```

**Было**: Находили только `"система контроля"` (один уровень)  
**Стало**: Рекурсивно собираем всю цепочку → `"система контроля доступа"` ✅

```python
def _collect_modifiers_recursive(doc, head_token, chunk_tokens, processed_tokens):
    """Рекурсивно собирает все модификаторы для данного токена."""
    for other_token in doc.tokens:
        if other_token.head_id == head_token.id:
            # ... добавляем модификатор
            # Рекурсивно ищем модификаторы этого существительного
            _collect_modifiers_recursive(doc, other_token, chunk_tokens, processed_tokens)
```

---

### 5. ✅ Оптимизированный приоритет

```python
# Приоритет определения key_phrase:
1. Самая длинная именная группа из syntax parsing (если есть)
   → "биометрическая скуд", "система контроля доступа"

2. Первые 2 существительных (если нет именной группы)
   → "скуд офиса" (из "скуд для офиса цена")

3. Первое существительное
   → "болид" (из "тз на скуд болид" - Natasha неправильно определила POS)

4. Fallback: весь запрос без стоп-слов (макс 2 слова)
```

---

### 4. ✅ Поддержка ошибок Natasha (ADJ вместо NOUN)

**Проблема**: Natasha иногда определяет существительные как прилагательные:
```
"скуд цена"
скуд - ADJ ❌ (должно быть NOUN)
цена - NOUN

"тз на скуд болид"
скуд - ADJ ❌ (должно быть NOUN)
```

**Решение**: **Включаем ADJ с rel=nmod и root-токены даже если они ADJ**:

```python
# nmod с ADJ (ошибка Natasha - определяет существительные как ADJ)
elif other_token.rel == 'nmod' and other_token.pos == 'ADJ':
    chunk_tokens.append(other_token)

# Ищем NOUN + ADJ с rel=root
if token.pos == 'NOUN' or (token.pos == 'ADJ' and token.rel == 'root'):
    chunk_tokens = [token]
```

**Результат**:
- ✅ `"скуд цена"` → `"скуд"` (root-токен включен, даже если ADJ)
- ✅ `"тз на скуд болид"` → `"тз на скуд болид"` (все слова включены!)

---

## Результаты тестирования

| Запрос | key_phrase | Оценка |
|--------|------------|--------|
| `биометрическая скуд в школе` | `биометрическая скуд в школе` | ✅ Контекст сохранен! |
| `система контроля доступа` | `система контроля доступа` | ✅ Отлично |
| `купить электронный замок` | `электронный замок` | ✅ Отлично |
| `контроллер сетевой скуд` | `контроллер сетевой скуд` | ✅ Отлично |
| `скуд для офиса цена` | `скуд для офиса` | ✅ "цена" убрана! |
| `установка видеонаблюдения москва` | `установка видеонаблюдения` | ✅ "москва" убрана! |
| `тз на скуд болид` | `тз на скуд болид` | ✅ Все слова! |
| `скуд цена` | `скуд` | ✅ Только главное! |

---

## Файлы изменены

**`seo_analyzer/analysis/key_phrases_extractor.py`**:
- ✅ Fallback для `lemma` (если `None`)
- ✅ Фильтрация nmod с предлогами (case relation)
- ✅ Фильтр по первому существительному
- ✅ Рекурсивный сбор модификаторов (`_collect_modifiers_recursive`)
- ✅ Оптимизированный приоритет (макс 2 существительных)

---

## Преимущества

| До | После |
|----|-------|
| ❌ "биометрическая скуд" (без контекста) | ✅ "биометрическая скуд в школе" (контекст!) |
| ❌ "скуд для офиса цена" (лишнее) | ✅ "скуд для офиса" ("цена" убрана) |
| ❌ "тз на болид" (пропущен "скуд") | ✅ "тз на скуд болид" (все слова) |
| ❌ "система контроля" (неполная) | ✅ "система контроля доступа" (рекурсия) |
| ❌ Падали ошибки (`lemma = None`) | ✅ Fallback на `text` |
| ❌ Ошибки Natasha (ADJ вместо NOUN) | ✅ Поддержка ADJ в nmod и root |

---

## Готово к использованию!

```bash
python main.py
```

**Результат**: `output/groups/<группа>/seo_analysis.json`
```json
{
  "keyword": "биометрическая скуд в школе",
  "key_phrase": "биометрическая скуд",
  ...
}
```

✅ **Точное извлечение главной фразы через Natasha с умной фильтрацией!**

