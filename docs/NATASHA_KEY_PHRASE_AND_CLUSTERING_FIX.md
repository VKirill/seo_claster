# 🚀 Переход на Natasha для key_phrase и упрощение кластеризации

## Изменения

### 1. ✅ key_phrase теперь использует Natasha (не pymorphy3)
### 2. ✅ Убрано правило 50% в режиме BALANCED кластеризации

---

## 1. Переход с pymorphy3 на Natasha для key_phrase

### Почему Natasha лучше?

| Характеристика | pymorphy3 | **Natasha** |
|----------------|-----------|-------------|
| **Синтаксический анализ** | ❌ Нет | ✅ Да (dependency parsing) |
| **Именные группы** | Простая эвристика | ✅ Через синтаксическое дерево |
| **Точность** | Средняя | ✅ Высокая |
| **Понимание связей** | Нет | ✅ Да (nmod, amod, nsubj, obj) |

### Что изменилось?

#### БЫЛО (pymorphy3):
```python
# Простой морфологический анализ
parsed = self.morph.parse(word)[0]
pos = parsed.tag.POS
lemma = parsed.normal_form

# Эвристика: ищем подряд идущие слова
# "прилагательное + существительное"
if word1['pos'] == 'ADJF' and word2['pos'] == 'NOUN':
    phrase = f"{word1['lemma']} {word2['lemma']}"
```

**Проблемы:**
- ❌ Не понимает синтаксические связи
- ❌ Может пропустить важные группы
- ❌ Зависит от порядка слов

---

#### СТАЛО (Natasha):
```python
# Полный NLP pipeline с синтаксическим анализом
doc = Doc(query.lower())
doc.segment(self.segmenter)
doc.tag_morph(self.morph_tagger)
doc.parse_syntax(self.syntax_parser)  # ← Синтаксическое дерево!

# Извлечение именных групп через dependency parsing
for token in doc.tokens:
    if token.pos == 'NOUN':
        # Ищем модификаторы через синтаксические связи
        for other_token in doc.tokens:
            if other_token.head_id == token.id:
                # amod: прилагательное-модификатор
                if other_token.rel == 'amod' and other_token.pos == 'ADJ':
                    chunk_tokens.insert(0, other_token)
                
                # nmod: существительное-модификатор
                elif other_token.rel == 'nmod' and other_token.pos == 'NOUN':
                    chunk_tokens.append(other_token)
```

**Преимущества:**
- ✅ Понимает синтаксические связи (dependency relations)
- ✅ Находит правильные именные группы независимо от порядка
- ✅ Использует amod (adjectival modifier), nmod (nominal modifier)
- ✅ Более точное определение ключевой фразы

---

### Примеры работы:

#### Пример 1: "контроллер сетевой скуд"
```
Синтаксическое дерево:
контроллер (NOUN, head)
    ├─ сетевой (ADJ, rel=amod) ← модификатор
    └─ скуд (NOUN, rel=nmod) ← модификатор

Результат:
key_phrase = "сетевой контроллер скуд"
```

#### Пример 2: "система контроля доступа"
```
Синтаксическое дерево:
система (NOUN, head)
    └─ контроля (NOUN, rel=nmod)
        └─ доступа (NOUN, rel=nmod)

Результат:
key_phrase = "система контроля доступа"
```

#### Пример 3: "купить электронный замок"
```
Синтаксическое дерево:
купить (VERB, head)
    └─ замок (NOUN, rel=obj)
        └─ электронный (ADJ, rel=amod)

Результат:
key_phrase = "электронный замок"
```

---

### Код изменения:

**Файл**: `seo_analyzer/analysis/key_phrases_extractor.py`

#### Новая инициализация:
```python
from natasha import (
    Segmenter,
    NewsEmbedding,
    NewsMorphTagger,
    NewsSyntaxParser,
    Doc
)

class KeyPhrasesExtractor:
    def __init__(self):
        self.segmenter = Segmenter()
        self.emb = NewsEmbedding()
        self.morph_tagger = NewsMorphTagger(self.emb)
        self.syntax_parser = NewsSyntaxParser(self.emb)  # ← Синтаксический парсер
```

#### Новый метод извлечения:
```python
def _extract_noun_chunks_from_syntax(self, doc: Doc) -> List[str]:
    """Извлекает именные группы через dependency parsing"""
    noun_chunks = []
    
    for token in doc.tokens:
        if token.pos == 'NOUN':
            chunk_tokens = [token]
            
            # Ищем модификаторы
            for other_token in doc.tokens:
                if other_token.head_id == token.id:
                    # Прилагательное-модификатор (amod)
                    if other_token.rel == 'amod' and other_token.pos in ['ADJ']:
                        chunk_tokens.insert(0, other_token)
                    
                    # Существительное-модификатор (nmod)
                    elif other_token.rel == 'nmod' and other_token.pos == 'NOUN':
                        chunk_tokens.append(other_token)
            
            if len(chunk_tokens) >= 2:
                chunk_tokens.sort(key=lambda t: t.start)
                phrase = ' '.join([t.lemma for t in chunk_tokens])
                noun_chunks.append(phrase)
    
    return noun_chunks
```

---

## 2. Упрощение режима BALANCED кластеризации

### Что убрали?

**БЫЛО**:
```python
elif self.mode == self.MODE_BALANCED:
    # Должен быть схож с min_cluster_cohesion % запросов
    required_similar = max(1, int(len(cluster_queries) * self.min_cluster_cohesion))
    return similar_count >= required_similar  # Требовалось 50%
```

**Проблема**: Сложная логика с процентами, которая мешала правильной группировке.

---

**СТАЛО**:
```python
elif self.mode == self.MODE_BALANCED:
    # УПРОЩЕННАЯ ЛОГИКА: достаточно схожести хотя бы с ОДНИМ запросом кластера
    # Проверка общих URL (>=7) уже выполнена в _calculate_weighted_similarity
    # Если есть хотя бы один похожий запрос -> добавляем в кластер
    return similar_count > 0
```

**Решение**: Простая логика - если запрос имеет >=7 общих URL хотя бы с одним запросом кластера → добавляем!

---

### Почему это правильно?

#### Критерий попарной похожести УЖЕ жесткий:
```python
def calculate_weighted_similarity(urls1, urls2):
    common_count = len(set(urls1) & set(urls2))
    
    if common_count >= 7:  # ← ЖЕСТКИЙ критерий!
        return True  # Запросы похожи
```

**7 общих URL из топ-20** - это уже **35% пересечение**, что достаточно для группировки.

---

### Сравнение режимов ТЕПЕРЬ:

| Режим | Условие | Кластеры |
|-------|---------|----------|
| **STRICT** 🔒 | Схож со **ВСЕМИ** запросами кластера | Очень маленькие |
| **BALANCED** ⚖️ | Схож хотя бы с **ОДНИМ** запросом (>=7 URL) | Оптимальные |
| **SOFT** 🌊 | Схож хотя бы с **ОДНИМ** запросом (>=7 URL) | Аналогично BALANCED |

**BALANCED и SOFT теперь одинаковые**, но BALANCED - рекомендуемый режим.

---

### Пример работы:

```
Кластер: ["скуд купить", "скуд цена", "скуд заказать"]

Новый запрос: "скуд москва"

Проверяем:
"скуд москва" ↔ "скуд купить": 8 общих URL ✅

Результат: Добавляем в кластер!

Логика: Если есть >=7 общих URL хотя бы с одним запросом кластера,
запросы связаны одной тематикой SERP → группируем вместе.
```

---

## Файлы изменены

1. **`seo_analyzer/analysis/key_phrases_extractor.py`**
   - Заменен pymorphy3 на Natasha
   - Добавлен синтаксический анализ (syntax parsing)
   - Новый метод `_extract_noun_chunks_from_syntax()`
   - Новый метод `_determine_key_phrase_from_syntax()`

2. **`seo_analyzer/clustering/serp_advanced_clusterer.py`**
   - Упрощена логика режима BALANCED
   - Убрано правило 50% (`min_cluster_cohesion`)
   - Теперь: схожесть с любым одним запросом кластера (>=7 URL)

---

## Преимущества изменений

### key_phrase (Natasha):
- ✅ Более точное определение главной фразы
- ✅ Понимание синтаксических связей
- ✅ Корректная работа с любым порядком слов
- ✅ Извлечение сложных именных групп

### Кластеризация (упрощение):
- ✅ Простая и понятная логика
- ✅ Правильная группировка по SERP
- ✅ Нет искусственных ограничений по процентам
- ✅ Оптимальный размер кластеров

---

## Тестирование

```bash
# Запустить анализ
python main.py

# Проверить key_phrase в JSON
# output/groups/<группа>/seo_analysis.json
# "key_phrase": "контроллер сетевой скуд"

# Проверить кластеры
# Должны быть оптимального размера без лишней раздробленности
```

---

## Итог

### До изменений:
- ❌ pymorphy3 не понимал синтаксис
- ❌ Правило 50% мешало группировке
- ❌ key_phrase был неточным

### После изменений:
- ✅ Natasha понимает синтаксис через dependency parsing
- ✅ Простая логика кластеризации по общим URL
- ✅ Точное определение ключевой фразы

---

✅ **Готово к использованию!**

