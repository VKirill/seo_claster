# Настройка сбора Breadcrumbs (хлебных крошек)

## 🍞 Что такое Breadcrumbs

Breadcrumbs (хлебные крошки) - это навигационные элементы на сайтах:
```
Главная > Каталог > Электроника > Смартфоны
```

Они помогают понять структуру сайта конкурентов и построить оптимальную иерархию для вашего проекта.

## ⚙️ Настройки в конфиге

```python
# config_local.py

HIERARCHY_CONFIG_OVERRIDES = {
    "enabled": True,  # Включить анализ иерархии
    
    # Настройки breadcrumbs
    "collect_breadcrumbs": False,  # Собирать ли breadcrumbs со страниц
    "use_breadcrumbs": False,      # Использовать ли breadcrumbs для построения иерархии
}
```

## 🎯 Режимы работы

### Режим 1: Без Breadcrumbs (по умолчанию, быстро)
```python
"collect_breadcrumbs": False,
"use_breadcrumbs": False
```

**Что происходит**:
- ❌ Страницы НЕ скачиваются
- ❌ Breadcrumbs НЕ извлекаются
- ✅ Иерархия строится только на основе семантики запросов (DeepSeek AI)
- ⚡ Быстрое выполнение

**Когда использовать**: Для быстрого анализа или когда breadcrumbs не нужны

---

### Режим 2: Только сбор (сохранить в БД)
```python
"collect_breadcrumbs": True,
"use_breadcrumbs": False
```

**Что происходит**:
- ✅ Страницы скачиваются (ТОП-3)
- ✅ Breadcrumbs извлекаются и сохраняются в БД
- ❌ Breadcrumbs НЕ используются для иерархии (только семантика)
- 🐌 Медленнее из-за скачивания

**Когда использовать**: Для накопления данных в БД на будущее

---

### Режим 3: Полный анализ (сбор + использование)
```python
"collect_breadcrumbs": True,
"use_breadcrumbs": True
```

**Что происходит**:
- ✅ Страницы скачиваются (ТОП-3)
- ✅ Breadcrumbs извлекаются
- ✅ Breadcrumbs используются для построения иерархии
- ✅ DeepSeek анализирует и breadcrumbs, и семантику запросов
- 🐌 Самый медленный, но самый точный

**Когда использовать**: Для детального анализа структуры конкурентов

---

### Режим 4: Использовать кэш (быстро + качественно)
```python
"collect_breadcrumbs": False,  # Не скачивать заново
"use_breadcrumbs": True        # Использовать из БД
```

**Что происходит**:
- ❌ Новые страницы НЕ скачиваются
- ✅ Breadcrumbs берутся из БД (если есть)
- ✅ Используются для построения иерархии
- ⚡ Быстро (данные из кэша)

**Когда использовать**: После первого полного сбора, для повторных анализов

## 📊 Дополнительные настройки

```python
HIERARCHY_CONFIG_OVERRIDES = {
    "max_urls_per_query": 3,      # Сколько URL анализировать (ТОП-3, ТОП-5, ТОП-10)
    "use_database": True,          # Использовать БД для кэширования
    "filter_stop_domains": True,   # Фильтровать нежелательные домены
}
```

## 🗄️ База данных

Breadcrumbs сохраняются в: `output/groups/{название_группы}/page_content.db`

**Структура**:
- URL страницы
- Домен
- Breadcrumbs (JSON массив)
- Текстовый контент
- Позиция в SERP
- Дата сбора

## ⚡ Оптимизация производительности

### Для быстрого анализа:
```python
"collect_breadcrumbs": False,
"use_breadcrumbs": False,
"max_urls_per_query": 3
```

### Для качественного анализа:
```python
"collect_breadcrumbs": True,
"use_breadcrumbs": True,
"max_urls_per_query": 5  # Больше данных
```

### Для повторного анализа (есть кэш):
```python
"collect_breadcrumbs": False,  # Используем кэш
"use_breadcrumbs": True,
"use_database": True
```

## 🚨 Важные замечания

1. **Скорость**: Сбор breadcrumbs замедляет выполнение (скачивание страниц)
2. **Трафик**: При `collect_breadcrumbs=True` генерируется HTTP трафик к сайтам
3. **Кэш**: БД накапливает данные, повторные запуски используют кэш
4. **Стоп-домены**: Некоторые домены (маркетплейсы, агрегаторы) можно исключить

## 📝 Пример конфигурации

```python
# Для ежедневного анализа (быстро)
HIERARCHY_CONFIG_OVERRIDES = {
    "enabled": True,
    "collect_breadcrumbs": False,
    "use_breadcrumbs": False,
}

# Для глубокого анализа раз в месяц
HIERARCHY_CONFIG_OVERRIDES = {
    "enabled": True,
    "collect_breadcrumbs": True,
    "use_breadcrumbs": True,
    "max_urls_per_query": 5,
}
```

## 🔍 Проверка работы

После запуска с `collect_breadcrumbs=True`:
1. Проверьте наличие БД: `output/groups/{группа}/page_content.db`
2. В логе должно быть: "🔍 Извлечение breadcrumbs и контента..."
3. В Excel на листе "Иерархия проекта" будут данные

## 💡 Рекомендации

- **Первый запуск**: `collect=True, use=True` (соберите базу)
- **Последующие запуски**: `collect=False, use=True` (используйте кэш)
- **Быстрый тест**: `collect=False, use=False` (только семантика)

