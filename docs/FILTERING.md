# Фильтрация запросов

Анализатор автоматически применяет два типа фильтрации для очистки семантического ядра от "мусора".

## 1. Фильтрация по частотности

По умолчанию анализатор **автоматически удаляет запросы с нулевой частотностью** (frequency_world = 0).

### Почему это важно?

Запросы с нулевой частотностью:
- ❌ Не имеют трафика
- ❌ Не приносят посетителей
- ❌ Увеличивают время анализа
- ❌ Искажают статистику кластеров

### Примеры использования

#### 1. По умолчанию (убрать только нули)

```bash
python main.py
```

Или явно:
```bash
python main.py --min-frequency 1
```

**Результат:** Удаляются запросы с `frequency_world = 0`

#### 2. Убрать запросы с низкой частотностью

```bash
# Минимум 10 показов в месяц
python main.py --min-frequency 10
```

**Результат:** Удаляются запросы с `frequency_world < 10`

#### 3. Только высокочастотные запросы

```bash
# Минимум 100 показов
python main.py --min-frequency 100
```

**Результат:** Анализируются только запросы с `frequency_world >= 100`

#### 4. Включить все запросы (даже с нулевой)

```bash
python main.py --min-frequency 0
```

**Результат:** Анализируются все запросы, включая с нулевой частотностью

### Что происходит при фильтрации?

```
🔧 ЭТАП 2: Предобработка
--------------------------------------------------------------------------------
✓ Отфильтровано 15 запросов со стоп-словами
✓ Удалено 234 запросов с нулевой частотностью       ← Вот здесь!
✓ Удалено 0 слишком коротких запросов
```

### Комбинация с другими фильтрами

```bash
# Быстрый анализ только высокочастотных запросов
python main.py \
  --min-frequency 50 \
  --skip-embeddings \
  --skip-graph \
  --skip-forms
```

## 2. Фильтрация по соотношению частотностей

**Новая функция!** Автоматически удаляет "мусорные" запросы с высоким соотношением `частотность / точная_частотность > 51`.

### Что это означает?

**Пример хорошего запроса:**
```
Запрос: "купить телефон iphone"
Частотность: 1000
Точная частотность (!): 500
Соотношение: 1000 / 500 = 2 ✅ (хороший запрос)
```

**Пример плохого запроса:**
```
Запрос: "телефон" (слишком общий)
Частотность: 100000
Точная частотность (!): 500
Соотношение: 100000 / 500 = 200 ❌ (мусорный запрос)
```

### Почему это работает?

Высокое соотношение означает, что:
- ❌ Запрос слишком общий и неконкретный
- ❌ Запрос имеет много разных интерпретаций
- ❌ Точных показов очень мало по сравнению с широкими
- ❌ Такой запрос сложно оптимизировать

### Примеры использования

#### По умолчанию (соотношение ≤ 51)

```bash
python main.py
```

Удаляет запросы с соотношением > 51.

#### Строгая фильтрация (соотношение ≤ 20)

```bash
python main.py --max-frequency-ratio 20
```

Удаляет больше "мусора", оставляет только конкретные запросы.

#### Мягкая фильтрация (соотношение ≤ 100)

```bash
python main.py --max-frequency-ratio 100
```

Удаляет только явный мусор.

#### Отключить фильтрацию

```bash
python main.py --max-frequency-ratio 999999
```

Не рекомендуется! Оставляет весь мусор.

### Что происходит при фильтрации?

```
🔧 ЭТАП 2: Предобработка
--------------------------------------------------------------------------------
✓ Отфильтровано 15 запросов со стоп-словами
✓ Удалено 0 запросов с нулевой частотностью
✓ Удалено 127 запросов с соотношением частотностей > 51    ← Вот здесь!
✓ Удалено 0 слишком коротких запросов
```

### Комбинация фильтров

```bash
# Полная очистка семантики
python main.py --min-frequency 5 --max-frequency-ratio 30
```

Удалит:
- Запросы с частотностью < 5
- Запросы с соотношением > 30

## Другие фильтры

### Стоп-слова

Автоматически фильтруются запросы из файла `keywords_stop/Adult_hard.txt`.

Для добавления своих стоп-слов:
1. Откройте `keywords_stop/Adult_hard.txt`
2. Добавьте слова (по одному на строку)
3. Запустите анализ

### Короткие запросы

Автоматически удаляются запросы короче 2 символов.

Изменить это можно в коде:
```python
# В main.py, метод preprocessing
self.df = filter_short_queries(self.df, min_length=3)  # Минимум 3 символа
```

## Статистика фильтрации

После анализа смотрите в `output/statistics.json`:

```json
{
  "total_queries": 10450,
  "original_queries": 11949,
  "removed_stopwords": 389,
  "removed_zero_frequency": 234,
  "removed_short": 0,
  "removed_duplicates": 1110
}
```

## Рекомендации

### Для первичного анализа

```bash
# Стандартная фильтрация (убирает нули и мусор)
python main.py
```

Применит:
- `--min-frequency 1` (убрать нули)
- `--max-frequency-ratio 51` (убрать мусор)

### Для продакшн-семантики

```bash
# Чистая семантика для реальной работы
python main.py --min-frequency 5 --max-frequency-ratio 30
```

Применит:
- Удалит запросы с частотностью < 5
- Удалит запросы с соотношением > 30

### Для фокуса на топовых запросах

```bash
# Только популярные и конкретные запросы
python main.py --min-frequency 100 --max-frequency-ratio 20
```

Применит:
- Только запросы с частотностью >= 100
- Только с соотношением <= 20 (очень конкретные)

### Для крупных семантик (>100k запросов)

```bash
# Максимально быстрый режим
python main.py \
  --min-frequency 20 \
  --max-frequency-ratio 30 \
  --skip-embeddings \
  --skip-graph \
  --skip-forms
```

### Для отладки/сравнения

```bash
# Отключить все фильтры (не рекомендуется)
python main.py \
  --min-frequency 0 \
  --max-frequency-ratio 999999
```

## Проверка перед анализом

Запустите быстрый тест для просмотра статистики:

```bash
python test_quick.py
```

Вывод покажет:
```
✅ Тест 3: Статистика данных
   Всего запросов: 11949
   Запросов с нулевой частотностью: 234           ← Будут удалены
   Общая частотность: 2,453,498
   Средняя частотность: 205.3
   Точная частотность: 2,453,498
   Запросов с соотношением > 51: 127              ← Будут удалены
   Среднее соотношение: 3.5
   Максимальное соотношение: 245.8
```

**Интерпретация:**
- Если много нулей → используйте `--min-frequency`
- Если много с высоким соотношением → используйте `--max-frequency-ratio`
- Если максимальное соотношение > 100 → точно есть мусор!

