# Автоматическая перезапись файлов после чистки запросов

## 📋 Описание

После этапа предобработки запросов (фильтрация стоп-слов, дублей, низкочастотных запросов и т.д.) анализатор **автоматически перезаписывает исходный файл**, оставляя в нем только целевые запросы.

## 🎯 Зачем это нужно?

### Проблема ДО
- При каждом запуске нужно было заново фильтровать одни и те же мусорные запросы
- Исходные файлы содержали запросы со стоп-словами, дубли, нулевую частотность
- Тратилось время на повторную обработку уже отфильтрованных данных

### Решение ПОСЛЕ
- ✅ После первого запуска файл содержит только целевые запросы
- ✅ Следующие запуски работают быстрее (не нужно фильтровать повторно)
- ✅ Исходный файл всегда актуален и готов к работе
- ✅ Автоматически создается бэкап с оригинальными данными

## 🔧 Как это работает

### 1. Этап предобработки (фильтрация)

```
🔧 ЭТАП 2: Предобработка
--------------------------------------------------------------------------------
✓ Отфильтровано 15 запросов со стоп-словами
✓ Удалено 23 запросов с нулевой частотностью
✓ Удалено 8 запросов с соотношением частотностей > 51.0
✓ Удалено 5 слишком коротких запросов
```

### 2. Нормализация и дедупликация

```
🔄 Нормализация запросов...
✓ Нормализация завершена
🔄 Дедупликация (точные дубли)...
✓ Удалено точных дублей: 12
🔄 Поиск неявных дублей...
✓ Удалено неявных дублей: 18 (8 групп)
✓ Уникальных запросов: 430
```

### 3. Автоматическое сохранение (после всех чисток)

```
💾 Перезапись файла с очищенными запросами (без дублей)...
✓ Создан бэкап: semantika/скуд_backup.csv
✓ Сохранено 430 запросов в: semantika/скуд.csv
✓ Исходный файл обновлен (без дублей, только целевые запросы)
```

## 📂 Структура файлов

### До запуска
```
semantika/
├── скуд.csv          # Исходный файл (500 запросов)
```

### После запуска
```
semantika/
├── скуд.csv          # Очищенный файл (450 запросов) ✨
├── скуд_backup.csv   # Бэкап оригинала (500 запросов) [игнорируется]
```

**Примечание:** Файлы с суффиксом `_backup` автоматически игнорируются при следующих запусках.

## 🛡️ Безопасность данных

### Автоматический бэкап
- Перед перезаписью **автоматически создается бэкап** с суффиксом `_backup`
- Если бэкап уже существует, он **НЕ перезаписывается** (защита от потери данных)
- Бэкап содержит **все оригинальные колонки** включая мусорные данные

### Что сохраняется в файле

#### ✅ Сохраняются (оригинальные данные)
```csv
keyword,frequency_world,frequency_exact,direct_shows,direct_clicks,direct_ctr,...
скуд купить,100,50,1000,50,5.0,...
```

#### ❌ НЕ сохраняются (вычисленные поля)
- `normalized` - нормализованный запрос
- `lemmatized` - лемматизированный запрос
- `main_intent` - интент (будет вычислен заново)
- `semantic_cluster_id` - ID кластера
- `priority_score` - приоритет
- И другие вычисленные поля

## 💡 Примеры использования

### Пример 1: Обычный режим

```bash
python main.py --input-file semantika/скуд.csv
```

**Результат:**
- Обработано 500 запросов → осталось 430 (без дублей)
- Создан `semantika/скуд_backup.csv` (500 запросов)
- Обновлен `semantika/скуд.csv` (430 уникальных запросов)

### Пример 2: Режим групп

```bash
python main.py --group скуд
```

**Результат:**
- Обработана группа из `semantika/скуд.csv`
- Создан `semantika/скуд_backup.csv`
- Файл автоматически обновлен

### Пример 3: Восстановление из бэкапа

Если нужно восстановить оригинальные данные:

```bash
# Windows
copy semantika\скуд_backup.csv semantika\скуд.csv

# Linux/Mac
cp semantika/скуд_backup.csv semantika/скуд.csv
```

## ⚙️ Технические детали

### Модуль: `seo_analyzer/export/csv/filtered_saver.py`

```python
from seo_analyzer.export.csv import save_filtered_queries

# Сохранить отфильтрованные запросы
save_filtered_queries(
    df=filtered_df,              # DataFrame после фильтрации
    original_file_path=Path("semantika/скуд.csv"),
    backup=True                  # Создать бэкап
)
```

### Функция `_select_original_columns()`

Умная фильтрация колонок:
- **Включает:** `keyword`, `frequency_world`, `frequency_exact`, Direct данные, SERP данные
- **Исключает:** `normalized`, `lemmatized`, `semantic_cluster_id`, `priority_score` и другие вычисленные поля

## 🚀 Преимущества

1. **Экономия времени** - не нужно фильтровать одни и те же запросы повторно
2. **Чистота данных** - файлы всегда содержат только целевые запросы
3. **Безопасность** - автоматический бэкап защищает от потери данных
4. **Автоматизация** - всё работает без дополнительных действий
5. **Совместимость** - работает как в обычном режиме, так и в режиме групп

## 📊 Статистика

### Что удаляется при чистке:
- ❌ Запросы со стоп-словами (мусор, нецелевые)
- ❌ Запросы с нулевой частотностью (нет трафика)
- ❌ Запросы с аномальным соотношением частотностей (>51)
- ❌ Слишком короткие запросы (< 2 символов)
- ❌ Точные дубли (одинаковая нормализованная форма)
- ❌ Неявные дубли (синонимы, вариации)

### Что остается:
- ✅ Целевые запросы с трафиком
- ✅ Уникальные запросы без дублей
- ✅ Запросы с нормальной частотностью
- ✅ Качественное семантическое ядро

## 🔄 Workflow

```
1. Загрузка файла (500 запросов)
         ↓
2. Фильтрация (стоп-слова, частотность, длина)
         ↓
3. Нормализация и лемматизация
         ↓
4. Дедупликация (точные + неявные дубли)
         ↓
5. Остается 430 уникальных запросов
         ↓
6. Создается бэкап (500 запросов)
         ↓
7. Файл перезаписывается (430 запросов без дублей) ✨
         ↓
8. Следующий запуск работает с 430 чистыми запросами
```

## ❓ FAQ

### Почему при загрузке из кэша не перезаписывается файл?

**Короткий ответ:** Файл перезаписывается автоматически при обнаружении расхождений.

**Подробно:**

При **первом запуске**:
1. CSV обрабатывается (удаляются дубли, стоп-слова)
2. Создается бэкап (`*_backup.csv`)
3. CSV перезаписывается очищенными данными
4. Данные сохраняются в кэш

При **последующих запусках** (когда данные загружаются из кэша):
1. Система **автоматически проверяет** количество строк в CSV и кэше
2. Если в CSV **больше** строк чем в кэше → обнаружены дубликаты
3. **Автоматически** перезаписывает CSV данными из кэша
4. Показывает сообщение: `✓ CSV файл синхронизирован с кэшем`

**Если дубликаты уже удалены** (количество строк совпадает):
- Перезапись не выполняется (она не нужна)
- CSV уже содержит только целевые запросы

### Как узнать что файл нужно синхронизировать?

Система автоматически покажет:

```
⚠️  Обнаружено расхождение:
   📄 В CSV файле: 63843 запросов
   💾 В кэше: 56949 запросов (без дублей)
   🗑️  Дубликатов в CSV: 6894

💾 Перезапись CSV с очищенными данными...
✓ Создан бэкап: semantika/видеонаблюдение_backup.csv
✓ Сохранено 56949 запросов в: semantika/видеонаблюдение.csv
✓ CSV файл синхронизирован с кэшем
✓ Удалено 6894 дублей из исходного файла
```

### Что если я хочу сохранить оригинальный файл?

Бэкап создается автоматически. Используйте файл с суффиксом `_backup` для восстановления.

### Можно ли отключить автоматическое сохранение?

Сейчас это работает автоматически. Если нужно отключить - создайте issue на GitHub.

### Что делать если бэкап уже существует?

Система НЕ перезапишет существующий бэкап. Это защита от потери данных.

### Файлы `*_backup.csv` обрабатываются при следующем запуске?

**НЕТ!** Файлы с суффиксом `_backup` автоматически игнорируются. Также игнорируются:
- Файлы начинающиеся с `~` (временные файлы Excel)
- Файлы начинающиеся с `.` (скрытые файлы)

### Какие колонки сохраняются?

Только оригинальные данные из входного файла (keyword, frequency_world, direct_* и т.д.). Вычисленные поля (normalized, cluster_id) не сохраняются.

## 🎓 Итог

Функция автоматической перезаписи файлов:
- ⚡ Ускоряет повторные запуски
- 🧹 Поддерживает чистоту данных
- 🛡️ Защищает от потери данных через бэкапы
- 🤖 Работает полностью автоматически

**Просто запускайте анализатор - всё остальное он сделает сам!** 🚀

