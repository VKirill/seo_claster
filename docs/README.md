# SEO Analyzer Full

Универсальный SEO-анализатор с полным функционалом для комплексного анализа поисковых запросов.

## 🎯 Возможности

### Классификация запросов
- **Интент-анализ**: коммерческий, информационный, навигационный, транзакционный
- **Определение брендов**: автоматическое определение брендовых запросов
- **Воронка продаж**: классификация по 5 этапам (Awareness → Purchase)
- **Целевые страницы**: автоматическое определение типа и генерация URL
- **Географическая привязка**: определение городов и регионов

### Многоуровневая кластеризация
- **Семантическая**: TF-IDF + K-Means с автоопределением кластеров
- **Topic Modeling**: LDA/NMF для выделения скрытых тем
- **Иерархическая**: 3-уровневая структура кластеров
- **Структурная**: группировка по паттернам запросов
- **Граф связей**: NetworkX с community detection (Louvain) и PageRank
- **Embeddings**: Sentence-Transformers для семантического анализа

### Дополнительный анализ
- **Дедупликация**: удаление дубликатов на основе нормализации
- **Оценка сложности**: классификация по стратегиям продвижения
- **Модификаторы**: определение ценовых, качественных и других модификаторов
- **Генерация словоформ**: 6 падежей русского языка через pymorphy3

### Экспорт результатов
- **CSV**: полная таблица с 40+ колонками
- **JSON**: иерархическая структура кластеров
- **GraphML/GEXF**: для визуализации в Gephi
- **HTML**: интерактивный дашборд с графиками
- **Бренды**: автоматический экспорт найденных брендов и брендовых запросов

## 📦 Установка

### Требования
- Python 3.11, 3.12, 3.13, или 3.14
- Windows/Linux/macOS

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### ⚠️ Для Python 3.14

Если вы используете Python 3.14, после установки зависимостей выполните:

```bash
python fix_networkx_python314.py
```

Или используйте готовый скрипт запуска:

```batch
run.bat
```

Подробнее см. [PYTHON_3.14_FIX.md](PYTHON_3.14_FIX.md)

## 🚀 Использование

### Базовый запуск

```bash
python main.py
```

### Режимы работы

**Полный анализ (все опции включены):**
```bash
python main.py --export-brands
```

**Стандартный режим (без графа, быстро):**
```bash
python main.py
```

**С графом для Gephi (медленно):**
```bash
python main.py --enable-graph
```

**Без topic modeling:**
```bash
python main.py --skip-topics
```

**Минимальный режим (самый быстрый):**
```bash
python main.py --skip-embeddings --skip-graph --skip-topics --skip-hierarchical --skip-forms
```

**Фильтрация по частотности:**
```bash
# Убрать запросы с частотностью меньше 10
python main.py --min-frequency 10

# Убрать только с нулевой частотностью (по умолчанию)
python main.py --min-frequency 1
```

**Фильтрация по соотношению частотностей:**
```bash
# Убрать мусорные запросы с соотношением > 51 (по умолчанию)
python main.py --max-frequency-ratio 51

# Более строгая фильтрация (соотношение > 20)
python main.py --max-frequency-ratio 20

# Отключить фильтрацию по соотношению
python main.py --max-frequency-ratio 999999
```

### Опции командной строки

| Опция | Описание |
|-------|----------|
| `--enable-graph` | **Включить** построение графа (по умолчанию выключено) |
| `--skip-topics` | Пропустить topic modeling (LDA) |
| `--skip-hierarchical` | Пропустить иерархическую кластеризацию |
| `--skip-forms` | Пропустить генерацию падежных форм |
| `--skip-embeddings` | Пропустить sentence-transformers |
| `--min-frequency N` | Минимальная частотность (по умолчанию: 1) |
| `--max-frequency-ratio N` | Макс. соотношение частотность/точная (по умолчанию: 51) |

## 📁 Структура проекта

```
seo_claster/
├── seo_analyzer/
│   ├── core/                      # Базовые модули
│   │   ├── config.py              # Конфигурация
│   │   ├── stopwords.py           # Фильтрация
│   │   ├── helpers.py             # Загрузка данных
│   │   ├── normalizer.py          # Нормализация
│   │   └── forms_generator.py     # Словоформы
│   ├── classification/            # Классификация
│   │   ├── intent_classifier.py   # Интенты
│   │   ├── brand_detector.py      # Бренды
│   │   ├── funnel_classifier.py   # Воронка
│   │   └── page_mapper.py         # Целевые страницы
│   ├── clustering/                # Кластеризация
│   │   ├── deduplicator.py        # Дедупликация
│   │   ├── semantic_clusterer.py  # TF-IDF + K-Means
│   │   ├── structure_clusterer.py # Структурные паттерны
│   │   ├── topic_modeler.py       # LDA/NMF
│   │   ├── hierarchical.py        # Иерархическая
│   │   ├── difficulty_scorer.py   # Оценка сложности
│   │   └── graph_builder.py       # Граф связей
│   └── export/                    # Экспорт
│       ├── csv_exporter.py        # CSV
│       ├── json_exporter.py       # JSON
│       ├── graph_exporter.py      # GraphML/GEXF
│       └── html_visualizer.py     # HTML дашборд
├── keyword_group/                 # Словари классификации
├── keywords_stop/                 # Стоп-слова
├── semantika/                     # Входные данные
│   └── скуд.csv                   # CSV с запросами
├── output/                        # Результаты анализа
├── main.py                        # Основной pipeline
├── requirements.txt               # Зависимости
└── README.md                      # Документация
```

## 📊 Входные данные

### Основной файл: `semantika/скуд.csv`

Ожидаемые колонки:
- Запросы (keyword, запрос, query)
- Частотность (frequency_world, частотность)
- Точная частотность (frequency_exact, "!")

### Словари: `keyword_group/`

15 словарей для классификации:
- `commercial.txt` (55) - коммерческие маркеры
- `info.txt` (127) - информационные маркеры
- `opt.txt` (23) - B2B маркеры
- `fast.txt` (7) - срочность
- `handmade.txt` (45) - DIY контент
- `funny.txt` (48) - развлекательный контент
- `work.txt` (10) - вакансии
- `bu.txt` (4) - б/у товары
- `Review.txt` (36) - отзывы
- `Russian.txt` (1097) - Россия
- `Moscow.txt` (97) - Москва
- `Kazakhstan.txt` (102) - Казахстан
- `Belarus.txt` (106) - Беларусь
- `Ukraine.txt` (433) - Украина
- `Country.txt` (252) - страны

### Стоп-слова: `keywords_stop/`

- `Adult_hard.txt` - фильтрация adult-контента

## 📈 Результаты анализа

Все результаты сохраняются в папку `output/`:

### Основные файлы
- `seo_analysis_full.csv` - **полная таблица с 40+ колонками** ⭐
- `dashboard.html` - **интерактивный HTML дашборд с группами** 📊
- `clusters_summary.csv` - сводка по кластерам

### Дополнительные файлы
- `seo_analysis_top1000.csv` - топ-1000 запросов
- `brands.csv` - все найденные бренды с частотой упоминаний
- `stopwords_blocked.csv` - запросы, заблокированные стоп-словами
- `implicit_duplicates_removed.csv` - удаленные неявные дубли 🆕
- `implicit_duplicates_groups.csv` - все группы дублей для проверки 🆕
- `brands_top100.csv` - топ-100 брендов
- `branded_queries.csv` - все брендовые запросы

### JSON файлы
- `seo_analysis_hierarchy.json` - иерархическая структура
- `statistics.json` - общая статистика

### Графы
- `queries_graph.graphml` - граф для Gephi (GraphML)
- `queries_graph.gexf` - граф для Gephi (GEXF)

### Визуализация
- `dashboard.html` - интерактивный HTML дашборд

## 📋 Структура CSV экспорта

### Базовые поля
- `keyword` - оригинальный запрос
- `normalized` - нормализованная форма
- `lemmatized` - лемматизированная форма
- `words_count` - количество слов
- `frequency_world` - общая частотность
- `frequency_exact` - точная частотность

### Классификация
- `main_intent` - основной интент
- `commercial_score` - скор коммерческого интента
- `informational_score` - скор информационного интента
- `navigational_score` - скор навигационного интента
- `transactional_score` - скор транзакционного интента

### Флаги
- `is_commercial` - коммерческий запрос
- `is_brand_query` - брендовый запрос
- `is_wholesale` - опт/B2B
- `is_urgent` - срочность
- `is_diy` - DIY контент
- `is_review` - отзывы
- `has_geo` - географическая привязка

### Гео-информация
- `geo_type` - тип (country/city/region)
- `geo_country` - страна
- `geo_city` - город

### Бренды
- `detected_brand` - определенный бренд
- `brand_confidence` - уверенность (0-1)

### Кластеры
- `semantic_cluster_id` - ID семантического кластера
- `cluster_name` - название кластера
- `topic_id` - ID темы (LDA)
- `topic_name` - название темы
- `hierarchical_level1/2/3` - иерархические уровни
- `graph_community_id` - ID сообщества в графе

### Воронка и страницы
- `funnel_stage` - этап воронки
- `target_page_type` - тип целевой страницы
- `suggested_url` - предложенный URL

### Сложность
- `difficulty_score` - скор сложности (0-100)
- `difficulty_level` - уровень (easy/medium/hard)
- `difficulty_cluster` - стратегия (quick_wins/long_tail)

### Граф
- `pagerank_score` - важность в графе
- `node_degree` - количество связей

### Словоформы
- `form_nominative` - именительный падеж
- `form_genitive` - родительный падеж
- `form_dative` - дательный падеж
- `form_accusative` - винительный падеж
- `form_instrumental` - творительный падеж
- `form_prepositional` - предложный падеж

## ⚡ Производительность

Время обработки **12000 запросов**:

| Режим | Время | Описание |
|-------|-------|----------|
| Минимальный | 1-2 мин | Без embeddings, графа, topics |
| Стандартный | 5-7 мин | С topics, без embeddings |
| Полный | 8-12 мин | Все опции включены |

**Этапы:**
- Загрузка и предобработка: ~30 сек
- Классификация: ~1 мин
- Базовая кластеризация: ~1 мин
- Topic Modeling: ~2 мин
- Embeddings + Граф: ~4-5 мин
- Генерация форм: ~1 мин
- Экспорт: ~30 сек

## 🛠️ Технологический стек

- **Python 3.11+**
- **NLP**: pymorphy3, natasha
- **ML**: scikit-learn, sentence-transformers
- **Графы**: networkx, python-louvain
- **Данные**: pandas, numpy
- **Async**: asyncio, aiofiles

## 📝 Примеры использования

### Пример 1: Быстрый анализ для презентации

```bash
python main.py --skip-embeddings --skip-forms
```

Получите основные кластеры и классификацию за 2-3 минуты.

### Пример 2: Глубокий анализ с графом

```bash
python main.py --export-brands
```

Полный анализ с графом связей и списком брендов.

### Пример 3: Только структурный анализ

```bash
python main.py --skip-embeddings --skip-graph --skip-topics
```

Фокус на структурных паттернах и воронке.

## 🎓 Алгоритмы

### Семантическая кластеризация
1. TF-IDF векторизация (1-3 граммы)
2. Автоопределение k методом elbow/silhouette
3. K-Means кластеризация
4. Извлечение топ-терминов для названий

### Topic Modeling
1. CountVectorizer/TF-IDF векторизация
2. LDA или NMF decomposition
3. Присвоение тем с probability score
4. Автоматическая генерация названий

### Граф связей
1. Sentence Transformers embeddings
2. Cosine similarity matrix
3. Фильтрация по порогу схожести
4. Louvain community detection
5. PageRank для определения хабов

## 🤝 Вклад в проект

Проект создан для комплексного SEO-анализа поисковых запросов.

## 📄 Лицензия

MIT License

## 👤 Автор

SEO Analyzer Team

