# ⚙️ Настройки кластеризации

Полный список всех параметров кластеризации в системе.

---

## 📍 Где настраивать

### 1. **Через config.py** (дефолтные значения)
```python
# seo_analyzer/core/config.py

CLUSTERING_CONFIG = {
    # Параметры группировки по словам
    "word_match": {
        "min_match_strength": 2,
        "min_group_size": 2,
        "strengthen_links": True,
        "exclude_stopwords": True,
    },
    
    # Параметры TF-IDF
    "tfidf": {
        "max_features": 1000,
        "min_df": 2,
        "max_df": 0.8,
    },
    
    # K-Means
    "kmeans": {
        "n_clusters_range": (20, 100),
    },
}
```

### 2. **Через аргументы командной строки** (переопределение)
```bash
python main.py --word-match-strength 3 --word-match-min-size 5
```

---

## 🔤 Группировка по совпадениям слов

### Параметры

| Параметр | CLI аргумент | Дефолт | Описание |
|----------|--------------|--------|----------|
| **Сила группировки** | `--word-match-strength` | `2` | Минимум совпадений слов |
| **Мин. размер группы** | `--word-match-min-size` | `2` | Минимум фраз в группе |
| **Усиливать связи** | `--word-match-strengthen` | `True` | Перераспределять фразы |
| **Отключить усиление** | `--word-match-no-strengthen` | - | Отключить перераспределение |
| **Включить группировку** | `--enable-word-match` | `False` | Включить алгоритм |

### Примеры

#### Базовая группировка
```bash
python main.py --enable-word-match
```

#### Строгая группировка (только близкие фразы)
```bash
python main.py --enable-word-match \
  --word-match-strength 3 \
  --word-match-min-size 5
```

#### Мягкая группировка (максимальный охват)
```bash
python main.py --enable-word-match \
  --word-match-strength 1 \
  --word-match-min-size 1
```

#### Быстрая группировка (без усиления связей)
```bash
python main.py --enable-word-match \
  --word-match-no-strengthen
```

---

## 🧠 Семантическая кластеризация (TF-IDF + K-Means)

### Параметры в config.py

```python
CLUSTERING_CONFIG = {
    "tfidf": {
        "max_features": 1000,      # Максимум признаков
        "min_df": 2,               # Мин. встречаемость (документы)
        "max_df": 0.8,             # Макс. встречаемость (доля)
        "ngram_range": (1, 3),     # Униграмы, биграмы, триграмы
    },
    
    "kmeans": {
        "n_clusters_range": (20, 100),  # Диапазон для авто-определения
        "random_state": 42,
        "max_iter": 300,
    },
}
```

### CLI аргументы

| Аргумент | Описание |
|----------|----------|
| *(нет прямых аргументов)* | Семантическая кластеризация **всегда включена** |
| `--skip-embeddings` | Пропустить sentence-transformers (быстрее) |

---

## 🎯 Topic Modeling (LDA/NMF)

### Параметры в config.py

```python
CLUSTERING_CONFIG = {
    "topic_modeling": {
        "n_topics_range": (5, 20),  # Диапазон для авто-определения
        "max_iter": 50,
        "random_state": 42,
    },
}
```

### CLI аргументы

| Аргумент | Описание |
|----------|----------|
| `--skip-topics` | Пропустить topic modeling |
| `--soft-clustering` | Несколько тем на запрос (soft clustering) |

### Примеры

```bash
# С topic modeling
python main.py

# Без topic modeling
python main.py --skip-topics

# Soft clustering (несколько тем на запрос)
python main.py --soft-clustering
```

---

## 📊 Иерархическая кластеризация

### Параметры в config.py

```python
CLUSTERING_CONFIG = {
    "hierarchical": {
        "n_clusters": None,          # None = авто-определение
        "linkage": "ward",
        "distance_threshold": 1.5,
    },
}
```

### CLI аргументы

| Аргумент | Описание |
|----------|----------|
| `--skip-hierarchical` | Пропустить иерархическую кластеризацию |

---

## 🕸️ Граф связей

### Параметры в config.py

```python
CLUSTERING_CONFIG = {
    "graph": {
        "similarity_threshold": 0.5,   # Порог для создания ребра
        "min_edge_weight": 0.3,
        "community_resolution": 1.0,
    },
    
    "embeddings": {
        "model_name": "paraphrase-multilingual-MiniLM-L12-v2",
        "batch_size": 32,
    },
}
```

### CLI аргументы

| Аргумент | Описание |
|----------|----------|
| `--enable-graph` | **Включить** построение графа (по умолчанию **выключено**) |
| `--skip-embeddings` | Пропустить embeddings (граф не будет работать) |

### Примеры

```bash
# Включить граф
python main.py --enable-graph

# Без графа (по умолчанию)
python main.py
```

---

## 🎚️ Полный пример со всеми параметрами

```bash
python main.py \
  --enable-word-match \
  --word-match-strength 2 \
  --word-match-min-size 3 \
  --word-match-strengthen \
  --soft-clustering \
  --enable-graph \
  --min-frequency 10 \
  --max-frequency-ratio 51
```

**Расшифровка:**
- `--enable-word-match` — включить группировку по словам
- `--word-match-strength 2` — минимум 2 совпадения
- `--word-match-min-size 3` — группы от 3 фраз
- `--word-match-strengthen` — усиливать связи
- `--soft-clustering` — несколько тем на запрос
- `--enable-graph` — строить граф связей
- `--min-frequency 10` — убрать запросы < 10 частотности
- `--max-frequency-ratio 51` — убрать мусорные запросы

---

## 📋 Сводная таблица

| Функция | CLI аргумент | Дефолт | Рекомендация |
|---------|--------------|--------|--------------|
| **Группировка по словам** | `--enable-word-match` | `False` | Включить для быстрой группировки |
| Сила группировки | `--word-match-strength` | `2` | `2` для баланса, `3` для строгости |
| Мин. размер группы | `--word-match-min-size` | `2` | `3-5` для качественных групп |
| Усиление связей | `--word-match-strengthen` | `True` | Оставить включенным |
| **Семантическая кластеризация** | *(всегда включена)* | `True` | Не отключать |
| **Topic Modeling** | `--skip-topics` | `True` | Не пропускать |
| Soft clustering | `--soft-clustering` | `False` | Включить для информационных запросов |
| **Иерархическая** | `--skip-hierarchical` | `True` | Не пропускать |
| **Граф связей** | `--enable-graph` | `False` | Включить при необходимости (медленно) |
| **Embeddings** | `--skip-embeddings` | `True` | Не пропускать (если нужен граф) |

---

## 💡 Рекомендации по выбору параметров

### Для продуктовых каталогов (интернет-магазины)
```bash
python main.py \
  --enable-word-match \
  --word-match-strength 2 \
  --word-match-min-size 3
```
**Почему:**
- Средняя строгость
- Баланс качества и охвата
- Отсекаются мелкие группы

---

### Для контент-проектов (блоги, СМИ)
```bash
python main.py \
  --enable-word-match \
  --word-match-strength 3 \
  --word-match-min-size 5 \
  --soft-clustering
```
**Почему:**
- Строгая группировка
- Только крупные темы
- Soft clustering для многотемных статей

---

### Для локального бизнеса (гео-запросы)
```bash
python main.py \
  --enable-word-match \
  --word-match-strength 2 \
  --word-match-min-size 2 \
  --min-frequency 5
```
**Почему:**
- Мягкая группировка (гео-модификаторы)
- Даже малые группы важны
- Фильтрация низкочастотных

---

### Для быстрого анализа
```bash
python main.py \
  --enable-word-match \
  --word-match-no-strengthen \
  --skip-topics \
  --skip-hierarchical \
  --skip-embeddings
```
**Почему:**
- Отключены долгие этапы
- Только базовая группировка
- Быстрая работа

---

## 🔧 Как изменить дефолтные значения

Отредактируйте `seo_analyzer/core/config.py`:

```python
CLUSTERING_CONFIG: Dict[str, Any] = {
    # Группировка по совпадениям слов
    "word_match": {
        "min_match_strength": 3,       # Было 2 → стало 3
        "min_group_size": 5,            # Было 2 → стало 5
        "strengthen_links": False,      # Было True → стало False
        "exclude_stopwords": True,
        "use_lemmatization": True,
    },
}
```

Теперь `python main.py --enable-word-match` будет использовать новые значения.

---

## ❓ FAQ

### Q: Какой параметр важнее: strength или min_size?
**A:** **Strength** определяет качество группировки, **min_size** — её масштаб.
- Высокий strength → меньше групп, выше качество
- Высокий min_size → меньше групп, но крупнее

### Q: Нужно ли всегда включать --enable-word-match?
**A:** **Нет**, это опциональная функция.
- Используйте, если нужна **быстрая группировка по словам**
- Семантическая кластеризация **достаточна** для большинства задач

### Q: Можно ли использовать только word-match без других кластеризаций?
**A:** Технически **да**, но **не рекомендуется**:
```bash
python main.py \
  --enable-word-match \
  --skip-topics \
  --skip-hierarchical
```
Но вы потеряете:
- Topic modeling (темы)
- Иерархическую структуру
- Глубокий семантический анализ

---

**Версия:** 1.3  
**Дата:** 2025-12-07


