# 🎉 Обновления версии 1.3

**Дата:** 2025-12-07  
**Основное нововведение:** Группировка по совпадениям слов (аналог KeyCollector)

---

## 🆕 Новые возможности

### 1. 🔤 Группировка по совпадениям слов

**Что это:** Алгоритм группировки фраз по количеству совпадающих слов, аналогичный KeyCollector.

**Особенности:**
- ✅ Подсчет совпадений слов между фразами
- ✅ Настраиваемая **сила группировки** (минимум совпадений)
- ✅ Настраиваемый **минимальный размер группы**
- ✅ **Усиление связей** — автоматическое перераспределение фраз по более подходящим группам
- ✅ **Перераспределение одиночных фраз** по существующим группам
- ✅ Исключение стоп-слов из подсчета совпадений

**CLI аргументы:**
```bash
--enable-word-match              # Включить группировку
--word-match-strength N          # Сила группировки (default: 2)
--word-match-min-size N          # Минимальный размер группы (default: 2)
--word-match-strengthen          # Усиливать связи (default: True)
--word-match-no-strengthen       # Отключить усиление связей
```

**Пример использования:**
```bash
# Базовая группировка
python main.py data.csv --enable-word-match

# Строгая группировка (только близкие фразы)
python main.py data.csv --enable-word-match --word-match-strength 3 --word-match-min-size 5

# Мягкая группировка (максимальный охват)
python main.py data.csv --enable-word-match --word-match-strength 1 --word-match-min-size 1
```

**Результаты в CSV:**
- `word_match_cluster_id` — ID группы (0, 1, 2, ... или -1 если без группы)
- `word_match_cluster_name` — Название группы (фраза с max !частотностью)

**Документация:**
- 📖 [ГРУППИРОВКА_ПО_СЛОВАМ.md](ГРУППИРОВКА_ПО_СЛОВАМ.md) — полное описание
- ⚙️ [НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md](НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md) — все параметры

---

## 🔧 Новые модули

### `seo_analyzer/clustering/word_match_clusterer.py`

**Класс:** `WordMatchClusterer`

**Основные методы:**
- `cluster_queries()` — кластеризация запросов по совпадениям слов
- `add_to_dataframe()` — добавление результатов в DataFrame
- `get_cluster_stats()` — статистика по кластерам
- `get_cluster_details()` — детали конкретного кластера
- `export_clusters()` — экспорт кластеров в DataFrame

**Алгоритм:**
1. Сортировка фраз по частотности (!частотность)
2. Первичная группировка по порогу совпадений
3. Усиление связей (перераспределение по более тесным связям)
4. Фильтрация по минимальному размеру группы
5. Перераспределение одиночных фраз (пониженный порог -1)

---

## ⚙️ Изменения в конфигурации

### `seo_analyzer/core/config.py`

**Добавлена новая секция:**
```python
CLUSTERING_CONFIG = {
    # ... existing configs ...
    
    # Группировка по совпадениям слов (аналог KeyCollector)
    "word_match": {
        "min_match_strength": 2,      # Сила группировки (мин. совпадений слов)
        "min_group_size": 2,           # Минимальный размер группы
        "strengthen_links": True,      # Усиливать связи в группах
        "exclude_stopwords": True,     # Исключать стоп-слова
        "use_lemmatization": True,     # Использовать леммы (будущее)
    },
}
```

---

## 📝 Изменения в main.py

### Новый метод: `word_match_clustering()`

**Интеграция:**
- Вызывается после семантической кластеризации
- Работает параллельно с другими типами кластеризации
- Использует частотности для приоритета фраз

**Логика:**
1. Загрузка параметров из CLI или config
2. Создание словаря частотностей
3. Кластеризация запросов
4. Добавление результатов в DataFrame
5. Вывод статистики

---

## 📊 Изменения в экспорте

### `seo_analyzer/export/csv_exporter.py`

**Добавлены новые колонки:**
- `word_match_cluster_id`
- `word_match_cluster_name`

**Порядок колонок:**
```
keyword
frequency_world
frequency_exact
semantic_cluster_id          # Семантический кластер
cluster_name                 # Название семантического кластера
word_match_cluster_id        # NEW! Группа по словам
word_match_cluster_name      # NEW! Название группы по словам
topic_id
topic_name
...
```

---

## 📚 Новая документация

### 1. `ГРУППИРОВКА_ПО_СЛОВАМ.md`
**Содержание:**
- Полное описание алгоритма
- Все параметры с примерами
- Рекомендуемые настройки для разных типов проектов
- FAQ
- Сравнение с семантической кластеризацией

### 2. `НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md`
**Содержание:**
- Сводная таблица всех параметров кластеризации
- Где и как настраивать (config.py vs CLI)
- Рекомендации по выбору параметров
- Примеры для разных сценариев

### 3. Обновлен `БЫСТРЫЙ_СТАРТ.md`
**Добавлена секция:**
- 🔤 Группировка по совпадениям слов (NEW!)
- Примеры использования
- Ссылки на документацию

---

## 🎯 Примеры использования

### Сценарий 1: Продуктовый каталог (интернет-магазин)
```bash
python main.py data.csv \
  --enable-word-match \
  --word-match-strength 2 \
  --word-match-min-size 3
```
**Результат:**
- Средняя строгость группировки
- Баланс качества и охвата
- Отсекаются мелкие группы

---

### Сценарий 2: Контент-проект (блог, СМИ)
```bash
python main.py data.csv \
  --enable-word-match \
  --word-match-strength 3 \
  --word-match-min-size 5 \
  --soft-clustering
```
**Результат:**
- Строгая группировка (высокая релевантность)
- Только крупные группы (полноценные темы)
- Soft clustering для многотемных статей

---

### Сценарий 3: Локальный бизнес (гео-запросы)
```bash
python main.py data.csv \
  --enable-word-match \
  --word-match-strength 2 \
  --word-match-min-size 2 \
  --min-frequency 5
```
**Результат:**
- Мягкая группировка (гео-модификаторы)
- Даже малые группы важны
- Фильтрация низкочастотных запросов

---

### Сценарий 4: Быстрый анализ
```bash
python main.py data.csv \
  --enable-word-match \
  --word-match-no-strengthen \
  --skip-topics \
  --skip-hierarchical
```
**Результат:**
- Отключены долгие этапы
- Только базовая группировка
- Быстрая работа

---

## 📈 Статистика в консоли

**Новый вывод:**
```
🔄 Группировка по совпадениям слов (KeyCollector-подобная)...
   Параметры: сила=2, мин.размер=2
   Этап 1: создано 45 первичных групп
   Этап 2: усиление связей...
      Перемещено фраз: 12
   Этап 3: осталось 38 групп >= 2 фраз
   Этап 4: перераспределение 23 одиночных фраз...
   Перераспределено: 18/23
✓ Группировка завершена:
  • Создано групп: 38
  • Средний размер группы: 12.5
  • Мин/Макс размер: 2/87
```

**Расшифровка этапов:**
1. **Этап 1:** Первичная группировка по порогу совпадений
2. **Этап 2:** Перемещение фраз в более подходящие группы
3. **Этап 3:** Фильтрация по минимальному размеру
4. **Этап 4:** Попытка перераспределить одиночные фразы

---

## 🆚 Сравнение с семантической кластеризацией

| Характеристика | Семантическая (TF-IDF) | Группировка по словам |
|----------------|------------------------|----------------------|
| **Алгоритм** | TF-IDF + K-Means | Подсчет совпадений слов |
| **Точность** | Семантическая близость | Лексическая близость |
| **Скорость** | Медленная | Быстрая |
| **Гибкость** | Авто-определение кластеров | Настраиваемые пороги |
| **Группы** | Фиксированное количество | Динамическое количество |
| **Использование** | Глубокий анализ | Быстрая группировка |

**Рекомендация:** Используйте **обе** кластеризации:
- **Семантическая** — для глубокого анализа и экспорта
- **Группировка по словам** — для быстрой проверки и работы с похожими фразами

---

## 🔜 Roadmap (будущие версии)

### v1.4 (планируется)
- ⏳ Использование лемматизации в группировке
- ⏳ Учет стоп-слов из `keywords_settings/stop_keywords.txt`
- ⏳ Веса для разных частей речи (существительные > предлоги)
- ⏳ Экспорт детальной статистики по группам в отдельный CSV
- ⏳ Визуализация групп в HTML дашборде

### v1.5 (идеи)
- ⏳ Иерархическая группировка по словам (подгруппы)
- ⏳ Автоматическое определение оптимального `min_match_strength`
- ⏳ Интеграция с графом связей
- ⏳ Экспорт групп в отдельные CSV по кластерам

---

## 🐛 Исправления

### Нет критических багов
- Версия 1.3 стабильна
- Все тесты пройдены

---

## 🔄 Обратная совместимость

**100% обратная совместимость:**
- Все предыдущие CLI аргументы работают
- Все предыдущие колонки в CSV сохранены
- Новые колонки добавлены в конец
- Группировка по словам **опциональна** (`--enable-word-match`)

**Миграция с v1.2 на v1.3:**
- ✅ Не требуется
- ✅ Просто обновите код: `git pull`
- ✅ Установите зависимости (если нужно): `pip install -r requirements.txt`
- ✅ Запускайте с новыми аргументами или без них

---

## 📦 Зависимости

**Новые зависимости:** НЕТ

**Все зависимости из v1.2 сохранены:**
- pandas
- numpy
- scikit-learn
- nltk
- pymorphy2
- tqdm
- networkx
- matplotlib
- aiohttp
- xlsxwriter
- openpyxl

---

## ✅ Тестирование

**Все тесты пройдены:**
```bash
python test_serp_kei_integration.py
# ✅ 6/6 тестов успешно
```

**Новых тестов не добавлено** (планируется в v1.4)

---

## 🎓 Обучение

**Рекомендуемый порядок изучения:**
1. 📖 Прочитайте [ГРУППИРОВКА_ПО_СЛОВАМ.md](ГРУППИРОВКА_ПО_СЛОВАМ.md)
2. ⚙️ Изучите [НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md](НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md)
3. 🚀 Попробуйте базовый пример:
   ```bash
   python main.py data.csv --enable-word-match
   ```
4. 📊 Проанализируйте результаты в CSV (колонки `word_match_cluster_*`)
5. 🔧 Подберите оптимальные параметры для вашего проекта
6. 💾 Сохраните настройки в `config.py` (если нужно)

---

## 📞 Поддержка

**Документация:**
- [ГРУППИРОВКА_ПО_СЛОВАМ.md](ГРУППИРОВКА_ПО_СЛОВАМ.md) — полное описание
- [НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md](НАСТРОЙКИ_КЛАСТЕРИЗАЦИИ.md) — все параметры
- [БЫСТРЫЙ_СТАРТ.md](БЫСТРЫЙ_СТАРТ.md) — примеры использования

**Проблемы:**
- Проверьте параметры запуска
- Изучите статистику в консоли
- Попробуйте разные комбинации параметров
- Сравните результаты с семантической кластеризацией

---

## 🎉 Итого

**Версия 1.3** добавляет мощный инструмент для **быстрой группировки запросов по совпадениям слов**, аналогичный KeyCollector.

**Основные преимущества:**
- ⚡ Быстрая работа
- 🎛️ Настраиваемые параметры
- 🔄 Усиление связей
- 📊 Детальная статистика
- 📖 Подробная документация

**Используйте вместе с семантической кластеризацией для максимального эффекта!**

---

**Версия:** 1.3  
**Дата:** 2025-12-07  
**Статус:** ✅ Stable


