# Архитектура проекта SEO Cluster

## 📋 Основные правила

### 1. Размер файлов
**МАКСИМУМ 150 строк кода в одном Python файле**

Почему 150 строк?
- Легко читать и понимать весь файл за один раз
- Проще искать нужную функцию
- Удобнее тестировать изолированно
- Меньше конфликтов при работе в команде

### 2. Один файл = одна ответственность
Каждый модуль должен отвечать за **ОДНУ** конкретную задачу.

✅ **Правильно:**
```
brand_detector.py       # Только определение брендов
intent_classifier.py    # Только классификация намерений
kei_calculator.py       # Только расчет KEI
```

❌ **Неправильно:**
```
seo_utils.py            # Слишком общее, непонятно что внутри
helpers.py (500 строк)  # Все подряд в одном файле
analyzer.py             # Что именно анализирует?
```

## 📁 Структура проекта

```
seo_analyzer/
│
├── core/                    # Базовая функциональность
│   ├── config.py            # Конфигурация приложения
│   ├── helpers.py           # Общие вспомогательные функции
│   ├── normalizer.py        # Нормализация и очистка текста
│   ├── stopwords.py         # Работа со стоп-словами
│   ├── serp_cache.py        # Кэширование SERP результатов
│   ├── serp_database.py     # База данных для SERP
│   ├── serp_data_enricher.py # Обогащение SERP данных
│   ├── lsi_extractor.py     # Извлечение LSI ключевых слов
│   └── forms_generator.py   # Генерация словоформ
│
├── clustering/              # Алгоритмы кластеризации
│   ├── semantic_clusterer.py     # Семантическая кластеризация
│   ├── word_match_clusterer.py   # Кластеризация по совпадению слов
│   ├── structure_clusterer.py    # Структурная кластеризация
│   ├── hierarchical.py           # Иерархическая кластеризация
│   ├── graph_builder.py          # Построение графов связей
│   ├── topic_modeler.py          # Тематическое моделирование
│   ├── deduplicator.py           # Базовая дедупликация
│   ├── advanced_deduplicator.py  # Продвинутая дедупликация
│   └── difficulty_scorer.py      # Оценка сложности кластеров
│
├── classification/          # Классификация данных
│   ├── intent_classifier.py # Классификация намерений пользователя
│   ├── funnel_classifier.py # Классификация по воронке продаж
│   ├── brand_detector.py    # Определение брендовых запросов
│   └── page_mapper.py       # Маппинг на типы страниц
│
├── analysis/                # Анализ данных
│   ├── serp_analyzer.py            # Анализ SERP результатов
│   └── cluster_lsi_aggregator.py   # Агрегация LSI по кластерам
│
├── metrics/                 # Расчет метрик
│   └── seo_metrics_calculator.py   # SEO метрики (KEI, сложность и т.д.)
│
└── export/                  # Экспорт результатов
    ├── csv_exporter.py      # Экспорт в CSV формат
    ├── excel_exporter.py    # Экспорт в Excel с форматированием
    ├── json_exporter.py     # Экспорт в JSON
    ├── html_visualizer.py   # HTML дашборды и визуализация
    └── graph_exporter.py    # Экспорт графов
```

## 🎯 Куда добавлять новый функционал

| Задача | Директория | Пример |
|--------|-----------|---------|
| Новый алгоритм кластеризации | `clustering/` | `density_clusterer.py` |
| Новый тип классификации | `classification/` | `language_classifier.py` |
| Новая SEO метрика | `metrics/` | `relevance_calculator.py` |
| Новый формат экспорта | `export/` | `pdf_exporter.py` |
| Анализ новых данных | `analysis/` | `backlink_analyzer.py` |
| Базовая утилита | `core/` | `text_processor.py` |

## 🔧 Инструменты для проверки

### Проверка размеров файлов:
```bash
# Запустить проверку всех файлов
python check_file_sizes.py

# Или через bat-файл
check_sizes.bat

# Проверить конкретную директорию
python check_file_sizes.py clustering

# Изменить лимит строк (например, 200)
python check_file_sizes.py seo_analyzer 200
```

### PowerShell команды:
```powershell
# Быстрая проверка больших файлов
Get-ChildItem -Path "seo_analyzer" -Recurse -Filter "*.py" | 
  Where-Object { (Get-Content $_.FullName | Measure-Object -Line).Lines -gt 150 }

# Подсчитать строки в конкретном файле
(Get-Content "seo_analyzer\core\config.py" | Measure-Object -Line).Lines
```

## 📝 Правила именования

### Файлы и модули
- `snake_case.py` - все строчные, слова через подчеркивание
- Имя отражает содержимое: `brand_detector.py`, `kei_calculator.py`

### Классы
- `PascalCase` - каждое слово с заглавной буквы
- Примеры: `BrandDetector`, `KEICalculator`, `SerpAnalyzer`

### Функции и методы
- `snake_case()` - все строчные, слова через подчеркивание
- Примеры: `calculate_kei()`, `extract_brands()`, `normalize_text()`

### Константы
- `UPPER_CASE` - все заглавные, слова через подчеркивание
- Примеры: `MAX_CLUSTERS`, `DEFAULT_THRESHOLD`, `API_TIMEOUT`

## 🚀 Процесс рефакторинга

### Когда файл превышает 150 строк:

#### 1. Анализ структуры
```python
# Пример большого файла (300 строк)
class SEOAnalyzer:
    def analyze_keywords(self):    # 100 строк
    def analyze_domains(self):     # 100 строк
    def analyze_competitors(self):  # 100 строк
```

#### 2. Определение логических блоков
Группируем связанные функции:
- Анализ ключевых слов
- Анализ доменов
- Анализ конкурентов

#### 3. Создание новых модулей
```
analysis/seo/
├── __init__.py
├── keyword_analyzer.py      (100 строк)
├── domain_analyzer.py       (100 строк)
└── competitor_analyzer.py   (100 строк)
```

#### 4. Сохранение обратной совместимости
```python
# analysis/seo/__init__.py
from .keyword_analyzer import KeywordAnalyzer
from .domain_analyzer import DomainAnalyzer
from .competitor_analyzer import CompetitorAnalyzer

# Фасад для старого API
class SEOAnalyzer:
    def __init__(self):
        self.keyword = KeywordAnalyzer()
        self.domain = DomainAnalyzer()
        self.competitor = CompetitorAnalyzer()
    
    def analyze_keywords(self):
        return self.keyword.analyze()
```

#### 5. Чек-лист после рефакторинга
- [ ] Все файлы <= 150 строк
- [ ] `__init__.py` созданы и экспортируют API
- [ ] Старый код продолжает работать
- [ ] Импорты обновлены
- [ ] Docstrings добавлены
- [ ] Нет дублирования кода
- [ ] Тесты пройдены

## 💡 Признаки что нужно разделить файл

1. ✅ Файл > 150 строк
2. ✅ Более 15 импортов в начале файла
3. ✅ Класс с более чем 5 публичными методами
4. ✅ В файле несколько классов (>2)
5. ✅ Много приватных методов (>5)
6. ✅ Разные области ответственности в одном файле
7. ✅ Трудно подобрать название файлу (слишком общее)

## 🎓 Примеры правильного разделения

### Пример 1: Экспорт в Excel
**Было:** `excel_exporter.py` (435 строк)

**Стало:**
```
export/excel/
├── __init__.py              # Публичный API
├── exporter.py              # Главный класс (80 строк)
├── workbook_builder.py      # Создание книги (120 строк)
├── data_writer.py           # Запись данных (150 строк)
└── sheet_formatter.py       # Форматирование (140 строк)
```

### Пример 2: Метрики
**Было:** `seo_metrics_calculator.py` (376 строк)

**Стало:**
```
metrics/
├── __init__.py
├── kei_calculator.py           # Расчет KEI (120 строк)
├── difficulty_calculator.py    # Расчет сложности (130 строк)
└── competition_calculator.py   # Расчет конкуренции (110 строк)
```

## 📚 Документация кода

### Каждый публичный класс и функция должны иметь docstring:

```python
def calculate_kei(frequency: int, competition: float) -> float:
    """
    Рассчитывает KEI (Keyword Efficiency Index).
    
    Args:
        frequency: Частота запроса в месяц
        competition: Уровень конкуренции (0-1)
    
    Returns:
        Значение KEI
        
    Example:
        >>> calculate_kei(1000, 0.5)
        44.72
    """
    if competition == 0:
        return 0.0
    return (frequency / competition) ** 0.5
```

## 🔄 Текущее состояние проекта

### Файлы требующие рефакторинга (топ-10):

1. `html_visualizer.py` - 680 строк → разбить на 5 модулей
2. `excel_exporter.py` - 435 строк → разбить на 3 модуля
3. `word_match_clusterer.py` - 428 строк → разбить на 3 модуля
4. `topic_modeler.py` - 380 строк → разбить на 3 модуля
5. `seo_metrics_calculator.py` - 376 строк → разбить на 3 модуля
6. `serp_database.py` - 372 строк → разбить на 3 модуля
7. `intent_classifier.py` - 364 строк → разбить на 3 модуля
8. `graph_builder.py` - 342 строк → разбить на 3 модуля
9. `semantic_clusterer.py` - 291 строк → разбить на 2 модуля
10. `advanced_deduplicator.py` - 290 строк → разбить на 2 модуля

**Всего файлов превышающих лимит:** 28 из 29

### Стратегия:
- При **любом изменении** существующего файла - сразу рефакторить
- **Новый функционал** - только в новые правильно организованные файлы
- **Не откладывать** рефакторинг на потом

## ✨ Преимущества такой архитектуры

1. **Читаемость** - легко найти нужный код
2. **Поддерживаемость** - изменения в одном месте не ломают другое
3. **Тестируемость** - каждый модуль можно тестировать отдельно
4. **Расширяемость** - легко добавлять новые функции
5. **Работа в команде** - меньше конфликтов при слиянии
6. **Понятность** - новый разработчик быстро разберется

## 🎯 Цель

Сделать код проекта:
- Простым для понимания
- Легким для модификации
- Удобным для расширения
- Приятным для работы

---

**Помни:** Хороший код пишется не для компьютера, а для людей!


