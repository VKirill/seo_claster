# 🔤 Группировка по совпадениям слов (аналог KeyCollector)

## 📌 Описание

Новая функция **группировки по совпадениям слов** работает аналогично алгоритму группировки в KeyCollector:
- Подсчитывает количество **совпадающих слов** между фразами
- Группирует фразы с **минимальным порогом совпадений**
- **Усиливает связи** между фразами (перераспределяет по более подходящим группам)
- **Перераспределяет одиночные фразы** по существующим группам

---

## 🎯 Параметры

### 1. **Сила группировки** (`--word-match-strength`)
**Что это:** Минимальное количество совпадающих слов между фразами для объединения в одну группу.

**По умолчанию:** `2`

**Примеры:**
```bash
# Строгая группировка (нужно минимум 3 совпадающих слова)
python main.py --enable-word-match --word-match-strength 3

# Мягкая группировка (достаточно 1 общего слова)
python main.py --enable-word-match --word-match-strength 1
```

**Как работает:**
- Фразы `"купить диван недорого"` и `"купить диван москва"` имеют **2 совпадения** (`купить`, `диван`)
- При `--word-match-strength 2` они попадут в **одну группу**
- При `--word-match-strength 3` они останутся в **разных группах**

---

### 2. **Минимальный размер группы** (`--word-match-min-size`)
**Что это:** Минимальное количество фраз в группе для её создания.

**По умолчанию:** `2`

**Примеры:**
```bash
# Только крупные группы (минимум 5 фраз)
python main.py --enable-word-match --word-match-min-size 5

# Даже одиночные фразы формируют группы
python main.py --enable-word-match --word-match-min-size 1
```

**Что происходит с малыми группами:**
- Если группа меньше минимального размера, алгоритм **пытается перераспределить** эти фразы по другим существующим группам
- Если перераспределение невозможно, фразы помечаются как `"Без кластера"` (cluster_id = -1)

---

### 3. **Усиливать связи в группах** (`--word-match-strengthen` / `--word-match-no-strengthen`)
**Что это:** После первичной группировки алгоритм проверяет, нет ли у фраз **более тесных связей** с другими группами.

**По умолчанию:** `ВКЛЮЧЕНО` (`True`)

**Примеры:**
```bash
# Включено (по умолчанию)
python main.py --enable-word-match

# Явно включить
python main.py --enable-word-match --word-match-strengthen

# Отключить
python main.py --enable-word-match --word-match-no-strengthen
```

**Как работает:**
1. **Первичная группировка:** Фраза `"купить диван недорого"` попадает в группу A по 2 совпадениям
2. **Усиление связей:** Алгоритм обнаруживает, что с группой B у фразы **3 совпадения**
3. **Перемещение:** Фраза перемещается в группу B как более подходящую

**Когда отключать:**
- Если нужна **быстрая группировка** без дополнительной оптимизации
- Если важно **сохранить первичное распределение** без перемещений

---

## 🚀 Использование

### Базовый пример
```bash
python main.py --enable-word-match
```
**Результат:**
- Сила группировки: `2`
- Минимальный размер группы: `2`
- Усиление связей: `включено`

---

### Строгая группировка (только близкие фразы)
```bash
python main.py --enable-word-match --word-match-strength 3 --word-match-min-size 5
```
**Результат:**
- Группируются только фразы с **минимум 3 общими словами**
- Группа создается только если в ней **минимум 5 фраз**
- **Качество групп:** высокое (очень похожие фразы)
- **Количество групп:** малое (много одиночных фраз)

---

### Мягкая группировка (максимальный охват)
```bash
python main.py --enable-word-match --word-match-strength 1 --word-match-min-size 1
```
**Результат:**
- Группируются фразы с **минимум 1 общим словом**
- Группа создается даже из **1 фразы**
- **Качество групп:** низкое (разнородные фразы)
- **Количество групп:** большое (почти все фразы в группах)

---

### Рекомендуемые настройки

#### Для продуктовых каталогов (интернет-магазины)
```bash
python main.py --enable-word-match --word-match-strength 2 --word-match-min-size 3
```
**Почему:**
- Средняя строгость группировки
- Отсекаются мелкие группы (< 3 фраз)
- Баланс качества и охвата

---

#### Для контент-проектов (блоги, СМИ)
```bash
python main.py --enable-word-match --word-match-strength 3 --word-match-min-size 5
```
**Почему:**
- Строгая группировка (высокая релевантность)
- Только крупные группы (полноценные темы)
- Меньше шума, выше качество

---

#### Для локального бизнеса (гео-запросы)
```bash
python main.py --enable-word-match --word-match-strength 2 --word-match-min-size 2
```
**Почему:**
- Много гео-модификаторов (города, районы)
- Нужна мягкая группировка для охвата
- Даже малые группы важны

---

## 📊 Результаты

После группировки в DataFrame добавляются **2 новые колонки**:

### 1. `word_match_cluster_id`
**Тип:** `int`

**Значения:**
- `0, 1, 2, ...` — ID группы
- `-1` — фраза не попала ни в одну группу

**Пример:**
```csv
keyword,word_match_cluster_id
купить диван,0
купить диван недорого,0
купить диван москва,0
купить кровать,1
```

---

### 2. `word_match_cluster_name`
**Тип:** `str`

**Значения:**
- Название группы (фраза с **максимальной !частотностью**)
- `"Без кластера"` — если фраза не попала в группу

**Пример:**
```csv
keyword,word_match_cluster_name
купить диван,купить диван
купить диван недорого,купить диван
купить диван москва,купить диван
```

---

## 📈 Статистика

В консоли выводится детальная статистика:

```
🔄 Группировка по совпадениям слов (KeyCollector-подобная)...
   Параметры: сила=2, мин.размер=2
   Этап 1: создано 45 первичных групп
   Этап 2: усиление связей...
      Перемещено фраз: 12
   Этап 3: осталось 38 групп >= 2 фраз
   Этап 4: перераспределение 23 одиночных фраз...
   Перераспределено: 18/23
✓ Группировка завершена:
  • Создано групп: 38
  • Средний размер группы: 12.5
  • Мин/Макс размер: 2/87
```

**Расшифровка:**
- **Этап 1:** Первичная группировка по порогу совпадений
- **Этап 2:** Перемещение фраз в более подходящие группы
- **Этап 3:** Фильтрация по минимальному размеру
- **Этап 4:** Попытка перераспределить одиночные фразы

---

## ⚙️ Настройка в config.py

Если вы хотите изменить **дефолтные** параметры без аргументов командной строки:

```python
# seo_analyzer/core/config.py

CLUSTERING_CONFIG: Dict[str, Any] = {
    # ... другие настройки ...
    
    # Группировка по совпадениям слов (аналог KeyCollector)
    "word_match": {
        "min_match_strength": 2,      # Сила группировки (мин. совпадений слов)
        "min_group_size": 2,           # Минимальный размер группы
        "strengthen_links": True,      # Усиливать связи в группах
        "exclude_stopwords": True,     # Исключать стоп-слова
        "use_lemmatization": True,     # Использовать леммы (пока не реализовано)
    },
}
```

**Дополнительные параметры (не доступны через CLI):**
- `exclude_stopwords` — если `True`, исключает служебные слова (`в`, `на`, `с`, `для`, и т.д.) из подсчета совпадений
- `use_lemmatization` — если `True`, использует леммы вместо исходных слов (пока не реализовано, будет в будущих версиях)

---

## 🆚 Отличия от семантической кластеризации

| Характеристика | Семантическая (TF-IDF) | Группировка по словам |
|----------------|------------------------|----------------------|
| **Алгоритм** | TF-IDF + K-Means | Подсчет совпадений слов |
| **Точность** | Семантическая близость | Лексическая близость |
| **Скорость** | Медленная | Быстрая |
| **Гибкость** | Авто-определение кластеров | Настраиваемые пороги |
| **Группы** | Фиксированное количество | Динамическое количество |
| **Использование** | Глубокий анализ | Быстрая группировка |

**Рекомендация:** Используйте **обе** кластеризации:
- **Семантическая** — для глубокого анализа и экспорта
- **Группировка по словам** — для быстрой проверки и работы с похожими фразами

---

## 🔍 Примеры групп

### Пример 1: Продуктовые запросы
**Входные данные:**
```
купить диван
купить диван недорого
купить диван в москве
диван купить
диван недорого
```

**Результат (strength=2, min_size=2):**
```
Группа 0 (5 фраз):
  - купить диван (cluster_name)
  - купить диван недорого
  - купить диван в москве
  - диван купить
  - диван недорого
```

---

### Пример 2: Информационные запросы
**Входные данные:**
```
как выбрать диван
как выбрать диван для дома
советы по выбору дивана
выбор дивана
```

**Результат (strength=2, min_size=2):**
```
Группа 0 (3 фразы):
  - как выбрать диван (cluster_name)
  - как выбрать диван для дома
  - выбор дивана

Без кластера:
  - советы по выбору дивана (только 1 совпадение с группой)
```

---

## ❓ FAQ

### Q: Почему некоторые фразы не попали ни в одну группу?
**A:** Возможные причины:
1. **Недостаточно совпадений** — фраза не имеет нужного количества общих слов ни с одной группой
2. **Малый размер группы** — группа была отфильтрована по `min_group_size`
3. **Стоп-слова** — все общие слова оказались стоп-словами и были исключены

**Решение:**
- Уменьшите `--word-match-strength`
- Уменьшите `--word-match-min-size`
- Отключите `exclude_stopwords` в конфиге

---

### Q: Как ускорить группировку?
**A:** 
1. Отключите усиление связей: `--word-match-no-strengthen`
2. Увеличьте минимальный размер группы: `--word-match-min-size 5`
3. Предварительно отфильтруйте низкочастотные запросы: `--min-frequency 10`

---

### Q: Можно ли использовать группировку БЕЗ семантической кластеризации?
**A:** Технически **да**, но **не рекомендуется**:
- Семантическая кластеризация дает более глубокий анализ
- Группировка по словам — это дополнительный слой, а не замена

Если очень нужно:
1. Используйте только `--enable-word-match`
2. Пропустите другие этапы: `--skip-topics --skip-hierarchical`

---

### Q: Названия групп совпадают с семантическими кластерами?
**A:** **Нет**, это **разные алгоритмы**:
- **Семантическая:** `cluster_name` (на основе TF-IDF)
- **Группировка по словам:** `word_match_cluster_name` (на основе совпадений)

Обе колонки будут в финальном CSV для сравнения.

---

## 📚 Дополнительно

### Алгоритм работы (упрощенно)
```
1. Сортировка фраз по частотности (!частотность)
2. Для каждой фразы:
   a. Токенизация (разбивка на слова)
   b. Исключение стоп-слов
   c. Подсчет совпадений с существующими группами
   d. Если совпадений >= min_match_strength:
      - Добавить в группу
   e. Иначе:
      - Создать новую группу
3. Усиление связей (если включено):
   - Для каждой фразы в группе:
     * Проверить совпадения с другими группами
     * Если найдена группа с большим количеством совпадений:
       - Переместить фразу в эту группу
4. Фильтрация по min_group_size
5. Перераспределение одиночных фраз (пониженный порог -1)
```

---

## 🎓 Best Practices

1. **Начните с дефолтных параметров** (`strength=2, min_size=2`)
2. **Анализируйте статистику** (средний размер, мин/макс)
3. **Корректируйте параметры:**
   - Слишком много мелких групп → увеличьте `min_size`
   - Слишком мало групп → уменьшите `strength`
   - Много одиночных фраз → уменьшите `strength` или `min_size`
4. **Сравнивайте с семантической кластеризацией**
5. **Экспортируйте в Excel** для визуального анализа

---

## 🛠️ Roadmap

Планируемые улучшения:
- ✅ Базовая группировка по совпадениям слов
- ✅ Усиление связей
- ✅ Перераспределение одиночных фраз
- ⏳ Использование лемматизации
- ⏳ Учет стоп-слов из `keywords_settings/stop_keywords.txt`
- ⏳ Веса для разных частей речи (существительные важнее предлогов)
- ⏳ Экспорт детальной статистики по группам в отдельный CSV

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте параметры запуска
2. Изучите статистику в консоли
3. Попробуйте разные комбинации параметров
4. Сравните результаты с семантической кластеризацией

**Версия:** 1.3  
**Дата:** 2025-12-07


