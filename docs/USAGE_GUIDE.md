# Руководство по использованию SEO Analyzer

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Проверка работоспособности

Запустите быстрый тест:

```bash
python test_quick.py
```

Вы должны увидеть:
```
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

### 3. Запуск полного анализа

**Минимальный режим (быстро, 2-3 минуты):**
```bash
python main.py --skip-embeddings --skip-graph --skip-topics --skip-hierarchical --skip-forms
```

**Стандартный режим (5-7 минут):**
```bash
python main.py --skip-embeddings --skip-graph
```

**Полный режим (8-12 минут):**
```bash
python main.py
```

Для полного режима потребуется установка дополнительных библиотек:
```bash
pip install sentence-transformers python-louvain matplotlib plotly networkx
```

## 📊 Понимание результатов

После выполнения анализа в папке `output/` появятся файлы:

### 1. CSV файлы

**`seo_analysis_full.csv`** - главный файл с полными результатами

Ключевые колонки:
- `keyword` - оригинальный запрос
- `frequency_world` - общая частотность
- `main_intent` - тип интента (commercial/informational/navigational/transactional)
- `funnel_stage` - этап воронки (Awareness/Interest/Consideration/Intent/Purchase)
- `semantic_cluster_id` - ID кластера
- `cluster_name` - название кластера
- `difficulty_cluster` - стратегия продвижения
- `target_page_type` - рекомендуемый тип страницы
- `suggested_url` - предложенный URL

**`seo_analysis_top1000.csv`** - топ-1000 запросов по частотности

**`clusters_summary.csv`** - сводка по кластерам

### 2. JSON файлы

**`seo_analysis_hierarchy.json`** - иерархическая структура
```json
{
  "main_cluster": "СКУД",
  "total_queries": 11949,
  "subclusters": [
    {
      "cluster_id": 0,
      "name": "Монтаж / Установка / Ремонт",
      "keywords_count": 450,
      "total_frequency": 125000,
      "funnel_stage": "Purchase"
    }
  ]
}
```

**`statistics.json`** - общая статистика анализа

### 3. HTML дашборд

**`dashboard.html`** - откройте в браузере для интерактивной визуализации

Содержит:
- Общую статистику
- Распределение по интентам
- Воронку продаж
- Стратегии продвижения
- Топ-50 запросов

### 4. Графы (если включен --без --skip-graph)

**`queries_graph.graphml`** и **`queries_graph.gexf`** - для визуализации в Gephi

Как открыть:
1. Скачайте Gephi: https://gephi.org/
2. File → Open → выберите .gexf файл
3. Layout → ForceAtlas 2 → Run
4. Appearance → Nodes → Partition → community (цвета по сообществам)

## 🎯 Практические сценарии

### Сценарий 1: Быстрая оценка семантического ядра

Цель: за 2 минуты понять структуру запросов

```bash
python main.py --skip-embeddings --skip-graph --skip-topics --skip-hierarchical --skip-forms
```

Что смотреть:
1. `statistics.json` - общая картина
2. `seo_analysis_top1000.csv` - приоритетные запросы
3. `dashboard.html` - визуализация

### Сценарий 2: Подготовка контент-плана

Цель: сгруппировать запросы по темам для создания статей

```bash
python main.py --skip-embeddings --skip-graph
```

Что делать:
1. Открыть `clusters_summary.csv`
2. Для каждого кластера посмотреть:
   - Количество запросов
   - Общую частотность
   - Примеры запросов
3. Отфильтровать по `funnel_stage` = "Interest" или "Awareness"
4. Создать статью на каждый кластер

### Сценарий 3: Оптимизация структуры сайта

Цель: определить какие страницы нужно создать

```bash
python main.py
```

Что делать:
1. Открыть `seo_analysis_full.csv`
2. Отсортировать по `total_frequency` в кластерах
3. Группировать по `target_page_type`:
   - `category` → создать категорию
   - `article` → написать статью
   - `commercial` → коммерческая страница
4. Использовать `suggested_url` как основу для URL

### Сценарий 4: Поиск Quick Wins

Цель: найти запросы для быстрого результата

```bash
python main.py --skip-embeddings
```

Что делать:
1. Открыть `seo_analysis_full.csv`
2. Отфильтровать по `difficulty_cluster` = "quick_wins"
3. Отсортировать по `frequency_world`
4. Выбрать топ-50
5. Создать/оптимизировать страницы под эти запросы

### Сценарий 5: Конкурентный анализ

Цель: понять где высокая конкуренция

```bash
python main.py
```

Что смотреть:
1. `difficulty_cluster` = "competitive" - высококонкурентные запросы
2. `pagerank_score` > 0.01 - важные узлы в графе (хабы)
3. `is_brand_query` = True - брендовые запросы конкурентов

## 🔧 Расширенные настройки

### Изменение порогов кластеризации

Отредактируйте `seo_analyzer/core/config.py`:

```python
CLUSTERING_CONFIG = {
    "kmeans": {
        "n_clusters_range": (5, 50),  # Диапазон кластеров
    },
    "graph": {
        "similarity_threshold": 0.5,  # Порог схожести для графа
    }
}
```

### Добавление своих словарей

1. Создайте файл в `keyword_group/my_dictionary.txt`
2. Добавьте слова (по одному на строку)
3. Добавьте в `seo_analyzer/core/config.py`:

```python
KEYWORD_DICTIONARIES = {
    # ... существующие
    "my_category": {
        "file": "my_dictionary.txt",
        "weight": 3,
        "flag": "is_my_category"
    }
}
```

### Фильтрация нежелательных запросов

Добавьте слова в `keywords_stop/Adult_hard.txt` (по одному на строку)

## ❓ FAQ

**Q: Анализ занимает слишком много времени**

A: Используйте флаги для отключения медленных операций:
```bash
python main.py --skip-embeddings --skip-graph --skip-forms
```

**Q: Ошибка "Out of memory"**

A: Уменьшите размер батча в конфиге или обработайте только топ запросов

**Q: Не находятся бренды**

A: Бренды определяются по:
- Латинице (Sigur, Hikvision)
- CamelCase (iPhone, YouTube)
- Морфологии (имена собственные)

Если не находится - добавьте в код вручную в `brand_detector.py`

**Q: Неправильные названия кластеров**

A: Названия генерируются автоматически по топ-терминам.
Можно вручную переименовать в CSV или настроить в `semantic_clusterer.py`

**Q: Как экспортировать только нужные колонки?**

A: Отредактируйте `_get_export_columns()` в `csv_exporter.py`

## 📞 Поддержка

При возникновении проблем:
1. Проверьте версию Python (`python --version` должно быть >= 3.11)
2. Переустановите зависимости: `pip install -r requirements.txt --force-reinstall`
3. Запустите тест: `python test_quick.py`

## 🎓 Дополнительные материалы

- [Документация NetworkX](https://networkx.org/)
- [Scikit-learn User Guide](https://scikit-learn.org/stable/user_guide.html)
- [Sentence Transformers](https://www.sbert.net/)
- [Gephi Tutorial](https://gephi.org/users/)


