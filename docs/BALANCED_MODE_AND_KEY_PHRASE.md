# ⚖️ Режим BALANCED и ключевая фраза запроса

## 1. Что значит режим BALANCED?

### Краткий ответ:
**Да, похожесть по общим URL в SERP выдаче.**

Режим BALANCED означает, что новый запрос должен иметь **минимум 7 общих URL** (по умолчанию) с **минимум 50%** запросов, уже находящихся в кластере.

---

## Три режима кластеризации

### 1. STRICT (строгий) 🔒
```python
mode = "strict"
```
- Запрос должен быть похож на **КАЖДЫЙ** запрос в кластере
- Минимум 7 общих URL с **каждым** запросом кластера
- **Результат**: Очень маленькие, но максимально точные кластеры

**Пример:**
```
Кластер: ["скуд купить", "скуд цена"]

Новый запрос: "скуд заказать"
→ Проверяем:
  - "скуд заказать" ↔ "скуд купить" = 8 общих URL ✅
  - "скуд заказать" ↔ "скуд цена" = 5 общих URL ❌
→ НЕ добавляем (нужно 7+ общих URL с КАЖДЫМ)
```

---

### 2. BALANCED (сбалансированный) ⚖️ ← **По умолчанию**
```python
mode = "balanced"
```
- Запрос должен быть похож **хотя бы на ОДИН** запрос в кластере
- Минимум 7 общих URL хотя бы с одним запросом кластера
- **Результат**: Оптимальные кластеры, группировка по SERP

**Пример:**
```
Кластер: ["скуд купить", "скуд цена", "скуд москва", "скуд установка"]

Новый запрос: "скуд заказать"
→ Проверяем:
  - "скуд заказать" ↔ "скуд купить" = 8 общих URL ✅

→ Есть хотя бы ОДИН похожий запрос ✅
→ ДОБАВЛЯЕМ в кластер!
```

**Код:**
```python
# seo_analyzer/clustering/serp_advanced_clusterer.py
elif self.mode == self.MODE_BALANCED:
    # УПРОЩЕННАЯ ЛОГИКА: достаточно схожести хотя бы с ОДНИМ запросом кластера
    # Проверка общих URL (>=7) уже выполнена в _calculate_weighted_similarity
    return similar_count > 0
```

---

### 3. SOFT (мягкий) 🌊
```python
mode = "soft"
```
- Достаточно похожести хотя бы с **ОДНИМ** запросом в кластере
- Минимум 7 общих URL хотя бы с одним запросом
- **Результат**: Большие кластеры, но могут быть неточности

**Пример:**
```
Кластер: ["скуд купить", "скуд цена", "скуд москва"]

Новый запрос: "скуд заказать"
→ Проверяем:
  - "скуд заказать" ↔ "скуд купить" = 8 общих URL ✅
  
→ Похож хотя бы на 1 запрос ✅
→ ДОБАВЛЯЕМ (даже если с остальными нет схожести)
```

---

## Как определяется "похожесть по URL"?

### 1. Сравнение SERP выдачи

```python
# Для каждой пары запросов:
query1_urls = ["site1.ru", "site2.ru", "site3.ru", ...]  # Топ-30 SERP
query2_urls = ["site1.ru", "site2.ru", "shop4.ru", ...]  # Топ-30 SERP

# Считаем пересечение
common_urls = set(query1_urls) & set(query2_urls)
common_count = len(common_urls)  # Например, 8 общих URL
```

### 2. Взвешенная похожесть (учитываются позиции)

Если `position_weights = True` (по умолчанию):

```python
# URL в топ-3 весят в 3 раза больше
pos_weights = {
    0-2:   3.0,  # Топ-3
    3-9:   2.0,  # Топ-10
    10-19: 1.5,  # Топ-20
    20-29: 1.0   # Топ-30
}

# Пример:
# Общий URL на позиции 1 и 2 → вес = 3.0
# Общий URL на позиции 15 и 18 → вес = 1.5
# Итоговый score = сумма весов
```

### 3. Порог схожести

```python
min_common_urls = 7  # По умолчанию

# Запросы считаются похожими если:
if weighted_score >= 7:  # или common_count >= 7
    return True  # ПОХОЖИ ✅
```

---

## Почему режим BALANCED лучше?

| Режим | Размер кластеров | Точность | Проблемы |
|-------|------------------|----------|----------|
| **STRICT** | Очень маленькие (2-5) | Максимальная | Слишком дробно, много кластеров |
| **BALANCED** ⚖️ | Средние (5-15) | Высокая | ✅ Оптимально |
| **SOFT** | Большие (10-50+) | Средняя | Может смешивать разные темы |

### Пример сравнения:

**1000 запросов:**
- **STRICT**: 500 кластеров (слишком дробно)
- **BALANCED**: 150 кластеров (✅ оптимально)
- **SOFT**: 50 кластеров (слишком крупно)

---

## 2. Ключевая фраза (Key Phrase)

### Что это?

**Главное слово/фраза запроса**, которое показывает **что именно** хочет пользователь.

### Как определяется?

```python
# seo_analyzer/analysis/key_phrases_extractor.py
# Используется Natasha с синтаксическим анализом (dependency parsing)

# Приоритет определения:
1. Именные группы через синтаксическое дерево (noun chunks)
   - amod (adjectival modifier): "электронный замок"
   - nmod (nominal modifier): "система контроля доступа"
   
2. Все существительные (максимум 3)
   "система контроль доступ"
   
3. Главные слова (существительные + глаголы)
   "купить скуд"
   
4. Весь запрос без стоп-слов
```

### Примеры:

| Запрос | Ключевая фраза |
|--------|----------------|
| `elsys mb light 2a 00 tп контроллер сетевой скуд` | `контроллер сетевой скуд` |
| `купить скуд в москве` | `скуд` |
| `электронный замок для офиса` | `электронный замок` |
| `система контроля и управления доступом` | `система контроля` |
| `как установить скуд самостоятельно` | `установить скуд` |

### В JSON:

```json
{
  "keyword": "elsys mb light 2a 00 tп контроллер сетевой скуд",
  "key_phrase": "контроллер сетевой скуд",
  "frequency_world": 4,
  "frequency_exact": 1,
  "main_intent": "commercial"
}
```

### Зачем нужна ключевая фраза?

1. **Понимание намерения**: Что именно ищет пользователь
2. **Группировка по смыслу**: Кластеры по типу продукта/услуги
3. **Title и H1**: Использовать в заголовках страниц
4. **Контент-план**: Понять о чем писать на странице

---

## Где используется?

### 1. В DataFrame (этап 2 - препроцессинг)
```python
# pipeline/stages/preprocessor.py
key_phrases_extractor = KeyPhrasesExtractor()
analyzer.df['key_phrase'] = analyzer.df['keyword'].apply(
    key_phrases_extractor.get_key_phrase
)
```

### 2. В JSON экспорте
```json
{
  "keyword": "контроллер скуд болид",
  "key_phrase": "контроллер скуд",
  "frequency_world": 150,
  "main_intent": "commercial"
}
```

### 3. В Excel (если экспортируется)
```
| keyword                        | key_phrase        | frequency |
|--------------------------------|-------------------|-----------|
| контроллер скуд болид          | контроллер скуд   | 150       |
| купить контроллер скуд         | контроллер скуд   | 80        |
```

---

## Итоговая структура JSON с key_phrase

```json
{
  "name": "контроллер скуд",
  "cluster_id": 42,
  "keywords": [
    {
      "keyword": "elsys mb light 2a 00 tп контроллер сетевой скуд",
      "key_phrase": "контроллер сетевой скуд",
      "frequency_world": 4,
      "frequency_exact": 1,
      "main_intent": "commercial",
      "funnel_stage": "Interest",
      
      "serp_avg_price": 7960.0,
      "serp_currency": "RUR",
      
      "case_forms": {
        "nominative": "контроллер сетевой скуд",
        "genitive": "контроллера сетевого скуда",
        "dative": "контроллеру сетевому скуду",
        "accusative": "контроллер сетевой скуд",
        "instrumental": "контроллером сетевым скудом",
        "prepositional": "контроллере сетевом скуде"
      }
    }
  ]
}
```

---

## Файлы изменены

1. `seo_analyzer/export/json_exporter.py` - добавлено `'key_phrase'` в список экспортируемых полей

---

## Проверка

```bash
# Запустить анализ
python main.py

# Проверить JSON
# output/groups/<группа>/seo_analysis.json

# Найти key_phrase:
# "key_phrase": "контроллер скуд"
```

---

✅ **Режим BALANCED использует похожесть по URL, а ключевая фраза теперь экспортируется в JSON!**

