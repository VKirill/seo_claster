# 🏷️ Экспорт найденных брендов

## Автоматический экспорт

Начиная с этой версии, анализатор **автоматически экспортирует найденные бренды** в отдельные файлы.

## Что экспортируется?

### 1. `brands.csv` - Все найденные бренды

Полный список всех брендов, обнаруженных в запросах (топ-1000).

**Колонки:**
- `brand` - название бренда
- `count` - количество упоминаний в запросах

**Пример:**
```csv
brand,count
sigur,245
rubezh,189
bolid,156
perco,134
sphinx,98
```

### 2. `brands_top100.csv` - Топ-100 брендов

Самые популярные бренды для быстрого анализа.

**Формат:** такой же, как `brands.csv`, но только топ-100.

### 3. `branded_queries.csv` - Брендовые запросы

Все запросы, содержащие бренды, со всеми метаданными.

**Колонки:**
- Все стандартные колонки из `seo_analysis_full.csv`
- `is_brand` = True
- `detected_brand` - основной бренд
- `brand_confidence` - уверенность определения
- И другие...

**Пример:**
```csv
keyword,frequency_world,frequency_exact,is_brand,detected_brand,brand_confidence
sigur скуд,1218,334,True,sigur,0.95
rubezh 08,890,245,True,rubezh,0.90
bolid c2000,756,189,True,bolid,0.92
```

## Где найти файлы?

Все файлы сохраняются в папку `output/`:

```
output/
├── seo_analysis_full.csv
├── seo_analysis_top1000.csv
├── clusters_summary.csv
├── brands.csv              ← Все бренды
├── brands_top100.csv       ← Топ-100 брендов
├── branded_queries.csv     ← Брендовые запросы
├── seo_analysis_hierarchy.json
├── statistics.json
├── queries_graph.graphml
├── queries_graph.gexf
└── dashboard.html
```

## Как это работает?

### Автоматически

Просто запустите анализ:

```bash
run.bat
```

**Вывод в консоли:**
```
💾 ЭТАП 6: Экспорт результатов
--------------------------------------------------------------------------------
💾 Экспорт в CSV: seo_analysis_full.csv...
✓ Экспортировано 10450 запросов
💾 Экспорт топ-1000 запросов: seo_analysis_top1000.csv...
✓ Экспортировано 1000 запросов
💾 Экспорт сводки кластеров: clusters_summary.csv...
✓ Экспортировано 5 кластеров
💾 Экспорт брендов: brands.csv...
✓ Экспортировано 87 брендов                         ← Новое!
  - brands.csv (все бренды)
  - brands_top100.csv (топ-100)
  - branded_queries.csv (356 брендовых запросов)
```

### Если бренды не найдены

Если в семантике нет брендов:

```
💾 Экспорт брендов: brands.csv...
  ℹ️  Бренды не найдены
```

Файлы не создаются.

## Примеры использования

### Анализ брендов в Excel

1. Откройте `brands.csv` в Excel
2. Отсортируйте по колонке `count`
3. Постройте диаграмму топ-10

### Фильтрация брендовых запросов

```python
import pandas as pd

# Загружаем брендовые запросы
df = pd.read_csv('output/branded_queries.csv')

# Фильтруем по конкретному бренду
sigur_queries = df[df['detected_brand'] == 'sigur']
print(f"Запросов с Sigur: {len(sigur_queries)}")

# Группируем по брендам
by_brand = df.groupby('detected_brand').agg({
    'frequency_world': 'sum',
    'keyword': 'count'
}).sort_values('frequency_world', ascending=False)

print(by_brand)
```

### Поиск конкурентов

```python
import pandas as pd

# Загружаем бренды
brands_df = pd.read_csv('output/brands.csv')

# Топ-5 брендов-конкурентов
top_brands = brands_df.head(5)
print("Основные конкуренты:")
for _, row in top_brands.iterrows():
    print(f"  {row['brand']}: {row['count']} упоминаний")
```

### Анализ брендовости ниши

```python
import pandas as pd

# Загружаем данные
df_all = pd.read_csv('output/seo_analysis_full.csv')
df_brands = pd.read_csv('output/branded_queries.csv')

# Процент брендовых запросов
brand_percent = (len(df_brands) / len(df_all)) * 100
print(f"Брендовость ниши: {brand_percent:.1f}%")

# Интерпретация
if brand_percent < 10:
    print("Низкая брендовость - проще продвигать")
elif brand_percent < 30:
    print("Средняя брендовость - сбалансированная ниша")
else:
    print("Высокая брендовость - сложная конкуренция")
```

## Что считается брендом?

Анализатор **автоматически** определяет бренды без жесткого списка. Работает на **любой семантике** (СКУД, окна, ворота, мебель и т.д.).

### Универсальные правила определения:

#### 1. Латиница с заглавной буквы
```
Sigur, Rubezh, Bolid, Rehau, DoorHan → бренды
```

#### 2. Латиница строчными (≥4 символа)
```
sigur, rubezh, perco, rehau, doorhan → бренды
```

#### 3. Кириллица с заглавной буквы (≥4 символа)
```
Рубеж, Болид, Ростелеком, Века, Алютех → бренды
```

#### 4. CamelCase
```
iPhone, iPad, DoorHan → бренды
```

#### 5. Смешанные скрипты
```
Латиница + кириллица в одном слове → бренд
```

#### 6. Модели с цифрами
```
C2000, T-Mobile, Болид-С2000 → бренды
```

#### 7. Морфология
```
Имена собственные (не гео) → бренды
```

### Что НЕ считается брендом:

- ❌ Географические названия (Москва, Россия)
- ❌ Служебные слова (в, на, с, для)
- ❌ Общие слова (купить, система, цена, монтаж)
- ❌ Слова < 4 символов (кроме с заглавной)

### Примеры работы на разных нишах:

**СКУД:**
- ✅ Sigur, Рубеж, Болид, Parsec
- ❌ скуд, контроллер, система

**Пластиковые окна:**
- ✅ Rehau, KBE, Века, Veka
- ❌ окна, профиль, пластик

**Ворота:**
- ✅ DoorHan, Алютех, Alutech
- ❌ ворота, откатные, секционные

**Универсальность:**
Детектор работает на **любой семантике** без настройки!

## Настройки экспорта

### Количество брендов

По умолчанию экспортируется топ-1000. Чтобы изменить, отредактируйте `main.py`:

```python
# Строка ~454
all_brands = self.brand_detector.get_top_brands(1000)  # Измените 1000 на нужное
```

### Топ-100

Чтобы изменить количество в топ-файле:

```python
# Строка ~461
brands_df.head(100).to_csv(...)  # Измените 100 на нужное
```

### Порог уверенности

Брендовые запросы включают только запросы с `brand_confidence >= 0.7`.

Чтобы изменить порог, отредактируйте `seo_analyzer/classification/brand_detector.py`:

```python
# Строка ~137
'is_brand_query': max_confidence >= 0.7,  # Измените 0.7 на нужное
```

## Статистика в `statistics.json`

В файл `statistics.json` также попадает информация о брендах:

```json
{
  "classification": {
    "total_branded_queries": 356,
    "brand_percentage": 3.4,
    "unique_brands": 87
  }
}
```

## Анализ в дашборде

HTML дашборд (`dashboard.html`) содержит визуализацию:
- Топ-10 брендов (диаграмма)
- Процент брендовых запросов
- Распределение по брендам

## Часто задаваемые вопросы

### Q: Почему некоторые бренды не определились?

**A:** Возможные причины:
1. Бренд написан строчными буквами
2. Бренд есть в списке технических терминов
3. Бренд совпадает с географическим названием
4. Низкая уверенность определения (< 0.7)

### Q: Можно ли отключить экспорт брендов?

**A:** Технически да, но не рекомендуется. Файлы создаются, только если бренды найдены.

### Q: Как добавить свой бренд в исключения?

**A:** Отредактируйте `seo_analyzer/classification/brand_detector.py`:

```python
# Строка ~28
self.tech_terms = {
    'скуд', 'ворота', ...,
    'ваш_термин'  # Добавьте сюда
}
```

### Q: Файлы слишком большие, можно ли уменьшить?

**A:** Да:
1. Уменьшите топ-N в `get_top_brands(1000)`
2. Увеличьте порог уверенности для брендовых запросов

## Дополнительные материалы

📖 **Документация:**
- [README.md](README.md) - основная документация
- [USAGE_GUIDE.md](USAGE_GUIDE.md) - руководство пользователя
- [FILTERING.md](FILTERING.md) - фильтрация запросов

📖 **Примеры:**
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт

---

**Итог:** Бренды экспортируются автоматически и доступны для анализа! 🏷️

