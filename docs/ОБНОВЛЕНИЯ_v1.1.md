# 🔄 Обновления v1.1

## Дата: 2025-12-07

### ✨ Новые возможности

#### 1. API ключ через переменную окружения

Теперь можно не передавать API ключ в командной строке:

```bash
# Windows
set XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270
python main.py data.csv --enable-serp

# Linux/Mac
export XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270
python main.py data.csv --enable-serp
```

**Приоритет:**
1. `--xmlstock-api-key` (аргумент)
2. `XMLSTOCK_API_KEY` (переменная окружения)

#### 2. Название кластера по максимальной !частотности

**Раньше:** Название формировалось из топ-3 терминов TF-IDF  
**Теперь:** Название = запрос с максимальной `frequency_exact` (точная частотность)

**Пример:**

Кластер содержит запросы:
- "купить скуд" (frequency_exact: 500)
- "система контроля доступа купить" (frequency_exact: 1200) ⭐
- "скуд цена" (frequency_exact: 300)

**Название кластера:** "система контроля доступа купить"

**Логика:** Запрос с максимальной !частотностью = самый точный и востребованный

### 🔧 Технические детали

#### Изменения в semantic_clusterer.py

Метод `assign_cluster_names()` теперь принимает параметр `use_exact_frequency`:

```python
def assign_cluster_names(
    self, 
    df: pd.DataFrame,
    use_exact_frequency: bool = True  # По умолчанию True
) -> pd.DataFrame:
    """
    use_exact_frequency=True:  Название = запрос с макс frequency_exact
    use_exact_frequency=False: Название = топ-термины TF-IDF (старый способ)
    """
```

#### Изменения в main.py

```python
# Добавлена проверка API ключа из переменной окружения
api_key = self.args.xmlstock_api_key or os.getenv('XMLSTOCK_API_KEY')

# Использование нового параметра
self.df = self.semantic_clusterer.assign_cluster_names(
    self.df,
    use_exact_frequency=True
)
```

### 📊 Влияние на результаты

#### Excel файл

**Лист "Clusters Summary":**
- Колонка "Main Query" теперь содержит запрос с максимальной !частотностью
- Более точные и релевантные названия кластеров

#### CSV файлы

**Колонка `cluster_name`:**
- Содержит реальный запрос вместо комбинации терминов
- Готов к использованию "как есть" для контента

### 🎯 Преимущества

1. **Точнее определяет суть кластера**
   - Реальный запрос пользователей
   - Не синтетическое название

2. **Готов к использованию**
   - Название = H1 для страницы
   - Не требует редактирования

3. **SEO-оптимизирован**
   - Максимальная !частотность = высокий спрос
   - Точное соответствие запросам

### 📝 Примеры

#### До (v1.0):
```csv
cluster_id,cluster_name,queries_count
1,"система / контроль / доступ",45
2,"купить / скуд / цена",32
```

#### После (v1.1):
```csv
cluster_id,cluster_name,queries_count
1,"система контроля и управления доступом",45
2,"купить систему скуд с установкой",32
```

### 🔄 Обратная совместимость

Если нужно вернуть старый способ (топ-термины TF-IDF):

```python
# В main.py строка 341
self.df = self.semantic_clusterer.assign_cluster_names(
    self.df,
    use_exact_frequency=False  # Используем топ-термины
)
```

### 🧪 Тестирование

Все тесты пройдены успешно:

```bash
python test_serp_kei_integration.py
# Результат: 6/6 ✅
```

### 📚 Обновленная документация

- ✅ БЫСТРЫЙ_СТАРТ.md - добавлен раздел про переменные окружения
- ✅ ОБНОВЛЕНИЯ_v1.1.md (этот файл)

### 🚀 Использование

```bash
# Вариант 1: С API ключом в команде
python main.py data.csv \
  --enable-serp \
  --xmlstock-api-key b3c2f28ec0a90b44e486af55c2f6b270

# Вариант 2: С переменной окружения (удобнее)
export XMLSTOCK_API_KEY=b3c2f28ec0a90b44e486af55c2f6b270
python main.py data.csv --enable-serp
```

### 📈 Что дальше?

**Возможные улучшения v1.2:**
1. Выбор метода названия кластера через CLI (`--cluster-naming exact|tfidf`)
2. Комбинированный подход (запрос + топ-термины)
3. Автоматическое определение лучшего метода для каждого кластера

---

**Версия:** 1.1  
**Предыдущая:** 1.0  
**Статус:** ✅ Готово к использованию


