# 🚀 Оптимизация SERP кластеризации

## 🐌 Текущая проблема

**Алгоритмическая сложность: O(n² × m) или хуже**

```python
for query in queries_with_serp:  # N = 5288 запросов
    for cid in range(cluster_id):  # До N кластеров
        for cluster_query in cluster_queries:  # M = до 100 запросов в кластере
            # Сравнение URL списков
            # Семантическая проверка
            # Weighted similarity calculation
```

**Итого:** ~5288 × 2000 × 50 = **528 миллионов операций!**

Key Collector делает это за секунды потому что использует **O(n log n)** с хэш-таблицами.

---

## 🎯 Решения (от простого к сложному)

### 1. ⚡ БЫСТРОЕ РЕШЕНИЕ: Отключить семантический анализ ✅ СДЕЛАНО

**Время:** 2 минуты  
**Ускорение:** 2-3x  
**Статус:** ✅ Применено в `pipeline/stages/clusterer.py`

```python
# В config или аргументах добавить:
semantic_check=False
```

Семантический анализ (`_are_semantically_different`) проверяет каждую пару запросов на:
- Географическую привязку (Москва/СПб)
- Типы продуктов
- Совместимость слов

Это добавляет **значительные накладные расходы** на каждое сравнение.

---

### 1.5. 🚀 ОПТИМИЗАЦИЯ ГЕОГРАФИИ: Natasha NER ✅ СДЕЛАНО

**Время:** 15 минут  
**Ускорение:** 5-10x для географических проверок  
**Статус:** ✅ Применено в `seo_analyzer/clustering/semantic_checker.py`

**Было:**
```python
# Regex поиск по словарям + pymorphy3 склонение
# Генерация всех падежей для каждого города
# Медленно и требует поддержки словарей
```

**Стало:**
```python
# Natasha NER автоматически определяет города в любых падежах
doc = Doc(query)
doc.segment(self.segmenter)
doc.tag_ner(self.ner_tagger)

for span in doc.spans:
    if span.type == LOC:
        locations.append(span.text)
```

**Преимущества:**
- ✅ Автоматически распознает ВСЕ города (не только из словаря)
- ✅ Работает со всеми падежами из коробки
- ✅ В 5-10 раз быстрее regex + pymorphy3
- ✅ Не требует поддержки словарей
- ✅ Кэшируется через @lru_cache(maxsize=10000)

---

### 2. 🔥 СРЕДНЯЯ ОПТИМИЗАЦИЯ: URL хэш-индекс ✅ СДЕЛАНО

**Время:** 30 минут  
**Ускорение:** 10-20x  
**Статус:** ✅ Применено в `seo_analyzer/clustering/serp_advanced_clusterer.py`

Вместо сравнения списков URL для каждой пары запросов, создается **инвертированный индекс**:

```python
# БЫЛО (медленно):
for query in queries:  # N запросов
    for cid in range(cluster_id):  # До N кластеров
        for cluster_query in cluster_queries:  # До 100 запросов
            # Сравнение URL списков
# Сложность: O(n² × m) или хуже

# СТАЛО (быстро):
# 1. Строим индекс один раз
url_to_queries = defaultdict(set)
for query, urls in query_urls_dict.items():
    for url in urls[:top_positions]:
        url_to_queries[url].add(query)

# 2. Быстрый поиск кандидатов через индекс
for query in queries:
    # Находим только ПОХОЖИЕ запросы (не все!)
    candidates = Counter()
    for url in query_urls[:top_positions]:
        for candidate in url_to_queries[url]:
            candidates[candidate] += 1
    
    # Проверяем только кандидатов
    for similar_query in candidates:
        if candidates[similar_query] >= min_common_urls:
            # Добавляем в кластер
```

**Сложность:** O(n × k), где k = среднее кол-во URL (~30)

**Что изменилось:**
- ✅ Построение инвертированного индекса URL → запросы
- ✅ Быстрый поиск кандидатов через индекс (вместо проверки всех)
- ✅ Проверка совместимости только с кандидатами
- ✅ Прогресс-бар для наглядности

**Ожидаемое ускорение:**
- Для 5,000 запросов: с **~15 минут** до **~30-60 секунд**
- Для 10,000 запросов: с **~60 минут** до **~2-3 минуты**

---

### 3. 🚀 МАКСИМАЛЬНАЯ ОПТИМИЗАЦИЯ: Граф + Union-Find

**Время:** 2-3 часа  
**Ускорение:** 50-100x

Построить **граф схожести** один раз, затем использовать **Union-Find** для кластеризации:

```python
# 1. Построить граф (один раз, O(n²) но с хэшами)
edges = []
for i, query1 in enumerate(queries):
    candidates = find_similar_queries_fast(query1)  # Используем индекс
    for query2 in candidates:
        if query2_idx > i:  # Избегаем дубликатов
            edges.append((query1, query2, similarity_score))

# 2. Кластеризация (O(n log n) с Union-Find)
uf = UnionFind()
for q1, q2, score in sorted(edges, key=lambda x: -x[2]):
    if should_merge(q1, q2, mode):  # Режим: strict/balanced/soft
        uf.union(q1, q2)
```

---

### 4. 🎯 ИДЕАЛЬНОЕ РЕШЕНИЕ: Иерархическая кластеризация с MinHash

**Время:** 1 день  
**Ускорение:** 100-500x (как Key Collector)

Использовать **LSH (Locality Sensitive Hashing)** с **MinHash**:

```python
from datasketch import MinHash, MinHashLSH

# 1. Создать MinHash для каждого запроса (O(n))
lsh = MinHashLSH(threshold=0.7, num_perm=128)
for query, urls in query_urls_dict.items():
    m = MinHash(num_perm=128)
    for url in urls:
        m.update(url.encode('utf-8'))
    lsh.insert(query, m)

# 2. Быстрый поиск похожих (O(1) amortized)
for query in queries:
    similar = lsh.query(minhas h[query])  # Мгновенно!
    # Группировка
```

**Преимущества:**
- O(n) вместо O(n²)
- Работает с миллионами запросов
- Именно так работает Key Collector

---

## 📊 Сравнение решений

| Решение | Время внедрения | Ускорение | Сложность кода | Статус |
|---------|----------------|-----------|----------------|--------|
| Отключить semantic_check | 2 мин | 2-3x | Очень простое | ✅ Готово |
| Natasha для географии | 15 мин | +20% | Простое | ✅ Готово |
| URL хэш-индекс | 30 мин | 10-20x | Простое | ✅ Готово |
| Граф + Union-Find | 2-3 часа | 50-100x | Средняя | 📝 План |
| MinHash LSH | 1 день | 100-500x | Сложная | 📝 План |

---

## 🛠️ Немедленные действия

### Вариант A: Временное решение (прямо сейчас)

Отредактировать `pipeline/stages/clusterer.py` или где вызывается кластеризация:

```python
clusterer = AdvancedSERPClusterer(
    min_common_urls=7,
    top_positions=30,
    mode="balanced",
    position_weights=True,
    semantic_check=False,  # ← ОТКЛЮЧИТЬ
    min_cluster_cohesion=0.8
)
```

### Вариант B: Правильное решение (30 минут)

Реализовать хэш-индекс в `AdvancedSERPClusterer`:

1. Добавить метод `_build_url_index()`
2. Использовать индекс в `_can_add_to_cluster()`
3. Убрать вложенный цикл по cluster_queries

---

## 💡 Рекомендация

**Для быстрого результата:**
1. Сейчас: отключить `semantic_check=False`
2. Завтра: реализовать URL хэш-индекс
3. Через неделю: внедрить MinHash LSH для масштабирования

**Ожидаемое время:**
- Сейчас: ~15-20 минут для 5292 запросов
- После отключения semantic: ~5-8 минут
- После хэш-индекса: ~30-60 секунд
- После MinHash: ~5-10 секунд (как Key Collector)

