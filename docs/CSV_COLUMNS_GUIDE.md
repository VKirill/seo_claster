# 📊 Руководство по колонкам CSV

## Главный файл: `seo_analysis_full.csv`

Это основной файл для работы с готовой семантикой. Содержит всю кластеризацию и метаданные.

### 🎯 Самые важные колонки

| Колонка | Описание | Пример | Для чего |
|---------|----------|--------|----------|
| `keyword` | Исходный запрос | "sigur скуд цена" | Основа для всего |
| `semantic_cluster_id` | **Группа/кластер** | 3 | Группировка запросов |
| `cluster_name` | Название группы | "покупка скуд" | Понятное название |
| `frequency_world` | Частотность | 1218 | Приоритет запроса |
| `main_intent` | Интент | "commercial" | Тип запроса |
| `funnel_stage` | Этап воронки | "Interest" | Где клиент в воронке |

### 📋 Полный список колонок

#### 🔑 Базовые

| Колонка | Описание | Пример |
|---------|----------|--------|
| `keyword` | Исходный запрос | "купить Sigur скуд" |
| `normalized` | Нормализованный | "sigur скуда купить" |
| `lemmatized` | Лемматизированный | "купить sigur скуд" |
| `words_count` | Количество слов | 3 |
| `frequency_world` | Частотность (показы) | 1218 |
| `frequency_exact` | Точная частотность (!) | 334 |

#### 🎯 Кластеризация (ГЛАВНОЕ!)

| Колонка | Описание | Пример | Зачем |
|---------|----------|--------|-------|
| `semantic_cluster_id` | **ID группы** | 3 | Группировка запросов |
| `cluster_name` | **Название группы** | "покупка скуд" | Понятное название |
| `topic_id` | ID темы (LDA) | 5 | Дополнительная группировка |
| `topic_name` | Название темы | "системы безопасности" | Тематика |
| `hierarchical_level1` | Уровень 1 (крупные группы) | 2 | Широкие категории |
| `hierarchical_level2` | Уровень 2 (средние) | 7 | Подкатегории |
| `hierarchical_level3` | Уровень 3 (детальные) | 15 | Микрогруппы |

#### 💡 Интент и тип

| Колонка | Описание | Значения |
|---------|----------|----------|
| `main_intent` | Основной интент | commercial, informational, navigational, transactional |
| `commercial_score` | Балл коммерции | 0.0-1.0 |
| `funnel_stage` | Этап воронки | Awareness, Interest, Consideration, Decision, Purchase |
| `target_page_type` | Тип целевой страницы | category, product, article, commercial |

#### 🏷️ Бренды

| Колонка | Описание | Пример |
|---------|----------|--------|
| `detected_brand` | Найденный бренд | "Sigur" |
| `is_brand_query` | Брендовый запрос? | True/False |

#### 🌍 География

| Колонка | Описание | Пример |
|---------|----------|--------|
| `has_geo` | Есть гео? | True/False |
| `geo_city` | Город | "Москва" |
| `geo_country` | Страна | "Россия" |

#### 🎲 Дополнительные

| Колонка | Описание | Зачем |
|---------|----------|-------|
| `difficulty_score` | Сложность продвижения | Оценка конкуренции |
| `suggested_url` | Предлагаемый URL | Куда вести |
| `query_pattern` | Паттерн запроса | Шаблон структуры |
| `is_commercial` | Коммерческий? | Быстрая фильтрация |
| `is_wholesale` | Оптовый? | B2B запросы |

#### 📝 Падежные формы (если включены)

| Колонка | Описание |
|---------|----------|
| `form_nominative` | Именительный (кто/что) |
| `form_genitive` | Родительный (кого/чего) |
| `form_dative` | Дательный (кому/чему) |
| `form_accusative` | Винительный (кого/что) |
| `form_instrumental` | Творительный (кем/чем) |
| `form_prepositional` | Предложный (о ком/чем) |

## 💼 Практические примеры использования

### 1. Группировка по кластерам

```python
import pandas as pd

df = pd.read_csv('output/seo_analysis_full.csv')

# Группируем по кластерам
clusters = df.groupby(['semantic_cluster_id', 'cluster_name']).agg({
    'keyword': 'count',
    'frequency_world': 'sum'
}).rename(columns={'keyword': 'запросов', 'frequency_world': 'частотность'})

print(clusters.sort_values('частотность', ascending=False))
```

### 2. Фильтрация коммерческих запросов

```python
# Только коммерческие
commercial = df[df['main_intent'] == 'commercial']

# По этапам воронки
decision_stage = df[df['funnel_stage'] == 'Decision']

# Высокочастотные коммерческие
hot = df[(df['main_intent'] == 'commercial') & (df['frequency_world'] > 100)]
```

### 3. Анализ брендовых запросов

```python
# Все брендовые
branded = df[df['is_brand_query'] == True]

# По конкретному бренду
sigur = df[df['detected_brand'] == 'sigur']

# Брендовые vs небрендовые
brand_stats = df.groupby('is_brand_query')['frequency_world'].sum()
```

### 4. Создание структуры сайта

```python
# Группируем по целевым страницам
by_page = df.groupby(['target_page_type', 'suggested_url']).agg({
    'keyword': 'count',
    'frequency_world': 'sum',
    'main_intent': lambda x: x.mode()[0]
})

# Создаем карту сайта
for page_type in df['target_page_type'].unique():
    pages = df[df['target_page_type'] == page_type]
    print(f"\n{page_type}:")
    print(pages[['suggested_url', 'cluster_name', 'frequency_world']].head(10))
```

### 5. Экспорт для контент-плана

```python
# Топ кластеров для контента
content_plan = df.groupby('cluster_name').agg({
    'keyword': lambda x: ', '.join(x.head(5)),  # Топ-5 запросов
    'frequency_world': 'sum',
    'main_intent': lambda x: x.mode()[0],
    'funnel_stage': lambda x: x.mode()[0],
    'suggested_url': 'first'
}).reset_index()

content_plan = content_plan.sort_values('frequency_world', ascending=False)
content_plan.to_excel('content_plan.xlsx', index=False)
```

### 6. Сегментация по географии

```python
# По городам
by_city = df[df['has_geo'] == True].groupby('geo_city').agg({
    'keyword': 'count',
    'frequency_world': 'sum'
})

# Для каждого города - свои запросы
for city in df['geo_city'].dropna().unique():
    city_queries = df[df['geo_city'] == city]
    print(f"\n{city}: {len(city_queries)} запросов, {city_queries['frequency_world'].sum()} показов")
```

## 🎯 Рекомендации по использованию

### Для SEO-специалиста

**Основные колонки:**
- `keyword` - запрос
- `semantic_cluster_id` + `cluster_name` - группировка
- `frequency_world` - частотность
- `main_intent` - тип запроса
- `funnel_stage` - этап воронки
- `suggested_url` - куда вести

**Задачи:**
1. Группировка семантики по страницам
2. Создание структуры сайта
3. Приоритизация запросов
4. Контент-план

### Для контент-менеджера

**Основные колонки:**
- `cluster_name` - тема материала
- `keyword` - ключевые слова
- `main_intent` - тип контента
- `funnel_stage` - для кого статья
- `form_*` - вариации для текста

**Задачи:**
1. Темы для статей
2. Ключевые слова для текста
3. Склонения для естественности
4. Приоритет по частотности

### Для маркетолога

**Основные колонки:**
- `funnel_stage` - этап воронки
- `frequency_world` - потенциал трафика
- `is_commercial` - готовность к покупке
- `detected_brand` - конкуренты
- `geo_city` - география

**Задачи:**
1. Оценка рынка
2. Анализ конкурентов (бренды)
3. Приоритизация регионов
4. Воронка продаж

### Для руководителя проекта

**Основные колонки:**
- `semantic_cluster_id` - количество групп
- `frequency_world` - общий потенциал
- `target_page_type` - структура сайта
- `difficulty_score` - сложность

**Задачи:**
1. Оценка объема работ
2. Планирование бюджета
3. Приоритизация задач
4. Распределение ресурсов

## 📈 Какие метрики считать

### Общие

```python
# Всего запросов
total_queries = len(df)

# Общая частотность
total_frequency = df['frequency_world'].sum()

# Количество кластеров
n_clusters = df['semantic_cluster_id'].nunique()

# Средняя частотность
avg_frequency = df['frequency_world'].mean()
```

### По типам

```python
# Распределение по интентам
intent_dist = df['main_intent'].value_counts()

# Распределение по воронке
funnel_dist = df['funnel_stage'].value_counts()

# Брендовость ниши
brand_percent = (df['is_brand_query'].sum() / len(df)) * 100
```

### По кластерам

```python
# Топ кластеров
top_clusters = df.groupby('cluster_name')['frequency_world'].sum().sort_values(ascending=False).head(10)

# Размеры кластеров
cluster_sizes = df['semantic_cluster_id'].value_counts()

# Средняя частотность в кластере
cluster_avg = df.groupby('cluster_name')['frequency_world'].mean().sort_values(ascending=False)
```

## 🚀 Быстрый старт

```python
import pandas as pd

# Загрузка
df = pd.read_csv('output/seo_analysis_full.csv')

# Просмотр структуры
print(f"Всего запросов: {len(df)}")
print(f"Колонок: {len(df.columns)}")
print(f"Кластеров: {df['semantic_cluster_id'].nunique()}")

# Топ-10 кластеров
print("\nТоп-10 кластеров:")
top = df.groupby('cluster_name').agg({
    'keyword': 'count',
    'frequency_world': 'sum'
}).sort_values('frequency_world', ascending=False).head(10)
print(top)

# Распределение по интентам
print("\nИнтенты:")
print(df['main_intent'].value_counts())

# Первые запросы каждого кластера
print("\nПримеры из каждого кластера:")
for cluster in df['cluster_name'].unique()[:5]:
    cluster_df = df[df['cluster_name'] == cluster]
    print(f"\n{cluster} ({len(cluster_df)} запросов):")
    print(cluster_df['keyword'].head(3).tolist())
```

## 📚 Дополнительные материалы

- [README.md](README.md) - основная документация
- [USAGE_GUIDE.md](USAGE_GUIDE.md) - руководство пользователя
- [BRANDS_EXPORT_GUIDE.md](BRANDS_EXPORT_GUIDE.md) - работа с брендами
- [FILTERING.md](FILTERING.md) - фильтрация запросов

---

**Итог:** Главное - это `semantic_cluster_id` + `cluster_name` для группировки, и `frequency_world` + `main_intent` для приоритизации! 🎯


