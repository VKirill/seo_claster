# 🚀 Быстрый старт SEO Analyzer

## Шаг 1: Установка

```bash
pip install -r requirements.txt
```

## Шаг 2: Проверка

```bash
python test_quick.py
```

Ожидаемый результат:
```
✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ!
```

## Шаг 3: Запуск анализа

### Вариант А: Быстрый (2-3 минуты)

```bash
python main.py --skip-embeddings --skip-graph --skip-topics --skip-hierarchical --skip-forms
```

### Вариант Б: Стандартный (5-7 минут)

```bash
python main.py --skip-embeddings --skip-graph
```

### Вариант В: Полный (8-12 минут, требует дополнительных библиотек)

Сначала установите:
```bash
pip install sentence-transformers python-louvain matplotlib plotly
```

Затем запустите:
```bash
python main.py
```

## Шаг 4: Просмотр результатов

Откройте папку `output/`:

1. **`dashboard.html`** - откройте в браузере для визуализации
2. **`seo_analysis_full.csv`** - полная таблица с результатами
3. **`statistics.json`** - общая статистика

## 📊 Что вы получите:

- ✅ 11,949 проанализированных запросов
- ✅ Классификация по интентам (commercial/informational/navigational)
- ✅ Распределение по воронке продаж
- ✅ Семантические кластеры с автоматическими названиями
- ✅ Оценка сложности продвижения
- ✅ Рекомендации по целевым страницам
- ✅ Генерация URL для каждого запроса

## 🎯 Следующие шаги:

1. Изучите `USAGE_GUIDE.md` для детальных инструкций
2. Откройте `seo_analysis_full.csv` в Excel/Google Sheets
3. Используйте фильтры для сегментации запросов
4. Создайте контент-план на основе кластеров

## ⚠️ Возможные проблемы:

**Ошибка импорта модуля:**
```bash
pip install -r requirements.txt --force-reinstall
```

**Ошибка кодировки CSV:**
- Убедитесь, что файл `semantika/скуд.csv` в кодировке UTF-8

**Долгая работа:**
- Используйте флаги `--skip-*` для отключения медленных операций

## 📞 Нужна помощь?

Смотрите:
- `README.md` - общая информация
- `USAGE_GUIDE.md` - подробное руководство
- Запустите `python test_quick.py` для диагностики


