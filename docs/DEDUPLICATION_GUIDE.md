# 🔄 Руководство по дедупликации запросов

## Обзор

SEO Analyzer использует **двухуровневую систему дедупликации** для удаления дублирующихся запросов:

1. **Точная дедупликация** - удаляет идентичные запросы после нормализации
2. **Продвинутая дедупликация** - находит неявные дубли (разный порядок слов, предлоги)

---

## 🎯 Уровень 1: Точная дедупликация

### Как работает:
- Нормализует запросы (приводит к леммам, убирает лишнее)
- Группирует идентичные после нормализации
- Оставляет запрос с максимальной частотностью

### Пример:
```
"купить контроллер скуд" → "купить контроллер скуд"
"купи контроллер скуда"  → "купить контроллер скуд"  ← ДУБЛЬ (удаляется)
```

---

## 🚀 Уровень 2: Продвинутая дедупликация (НЕЯВНЫЕ ДУБЛИ)

### Правила поиска дублей:

#### 1. Рассматриваем только уникальные слова

**Не важно количество слов** - важны сами слова:

```
"кабель кабель для скуд"  → { кабель, для, скуд }
"кабель для скуд"         → { кабель, для, скуд }  ← ДУБЛЬ
```

#### 2. Не учитываем спецсимволы

**Только буквы, цифры и пробелы**:

```
"скуд-контроллер"    → { скуд, контроллер }
"скуд. контроллер"   → { скуд, контроллер }  ← ДУБЛЬ
"скуд (контроллер)"  → { скуд, контроллер }  ← ДУБЛЬ
```

#### 3. Используем список исключений (стоп-слова)

**Файл:** `keywords_settings/stop_keywords.txt`

**Игнорируются:** предлоги, союзы, местоимения (для, на, и, с, по, от, к...)

```
"кабель для скуд"    → { кабель, скуд }
"кабель под скуд"    → { кабель, скуд }  ← ДУБЛЬ
"скуд кабель"        → { кабель, скуд }  ← ДУБЛЬ
"для скуд кабель"    → { кабель, скуд }  ← ДУБЛЬ
```

**Стоп-слова из файла:**
- Русские: а, без, в, для, до, за, и, из, к, на, над, не, о, об, от, по, под, при, про, с, у, я, мы, ты, вы, он, она, они...
- Английские: a, the, at, on, for, with...

---

### Правила удаления дублей:

#### Случай 1: Несколько фраз имеют макс. значение

**Пример:**
```
Группа: { кабель, скуд }
- "кабель для скуд"  (частота: 241)  ← MAX
- "кабель скуд"      (частота: 241)  ← MAX
- "скуд кабель"      (частота: 120)
```

**Действие:**
- Оставляем все фразы с макс. частотностью **кроме одной**
- Результат: "кабель для скуд" (241) остается, остальные удаляются

#### Случай 2: Все фразы имеют одинаковое значение

**Пример:**
```
Группа: { 1с, скуд }
- "скуд 1с"      (частота: 50)
- "1с скуд"      (частота: 50)
- "скуд и 1с"    (частота: 50)
```

**Действие:**
- Оставляем все **кроме одной** (случайный выбор)
- Результат: "скуд 1с" (50) остается, остальные удаляются

#### Случай 3: Только одна фраза с макс. значением

**Пример:**
```
Группа: { контроллер, скуд }
- "контроллер скуд"        (частота: 300)  ← MAX
- "скуд контроллер"        (частота: 150)
- "контроллер для скуд"    (частота: 80)
```

**Действие:**
- Оставляем фразу с макс. частотностью
- Удаляем все остальные
- Результат: "контроллер скуд" (300) остается

---

## 📊 Результаты дедупликации

### Пример анализа (скуд.csv):

```
Исходных запросов:        11,949

Фильтрация:
├─ Стоп-слова:               -14
├─ Соотношение ЧЧ:          -637
└─ Короткие:                  -0
                          -------
После фильтрации:         11,298

Дедупликация:
├─ Точные дубли:            -991
└─ Неявные дубли:           -710  ← 619 групп
                          -------
Итого уникальных:          9,597  ✅
```

### Экономия:
- **Удалено 710 неявных дублей** (6.3% от базы)
- **619 групп** объединено
- **Средний размер группы:** 2.2 запроса

---

## 📁 Экспортируемые файлы

### 1. `implicit_duplicates_removed.csv`

**Список удаленных дублей:**

| Колонка | Описание |
|---------|----------|
| `keyword` | Удаленный запрос |
| `frequency_world` | Частотность |
| `frequency_exact` | Точная частотность |
| `signature` | Сигнатура группы (набор уникальных слов) |
| `reason` | Причина удаления (`implicit_duplicate`) |

**Зачем нужно:**
- Проверить корректность удаления
- Найти ошибочно удаленные запросы
- Вернуть важные запросы

### 2. `implicit_duplicates_groups.csv`

**Все группы дублей (включая оставленные):**

| Колонка | Описание |
|---------|----------|
| `signature` | Сигнатура группы |
| `group_size` | Размер группы |
| `rank_in_group` | Ранг в группе (1 = самый частотный) |
| `keyword` | Запрос |
| `frequency_world` | Частотность |
| `frequency_exact` | Точная частотность |
| `is_max_freq` | Максимальная частотность в группе? |

**Зачем нужно:**
- Анализ всех найденных групп
- Ручная проверка дублей
- Корректировка стоп-слов

---

## 🔍 Анализ результатов

### Проверка групп дублей:

```python
import pandas as pd

# Загружаем группы
groups_df = pd.read_csv('output/implicit_duplicates_groups.csv', encoding='utf-8-sig')

# Находим большие группы (потенциальные проблемы)
large_groups = groups_df[groups_df['group_size'] > 5]
print(f"Групп размером > 5: {large_groups['signature'].nunique()}")

# Смотрим конкретную группу
signature = 'кабель скуд'
group = groups_df[groups_df['signature'] == signature]
print(f"\nГруппа '{signature}':")
print(group[['rank_in_group', 'keyword', 'frequency_world', 'is_max_freq']])
```

### Поиск ошибочно удаленных:

```python
# Загружаем удаленные
removed_df = pd.read_csv('output/implicit_duplicates_removed.csv', encoding='utf-8-sig')

# Фильтруем высокочастотные (> 100)
high_freq_removed = removed_df[removed_df['frequency_world'] > 100]
print(f"Удалено высокочастотных (> 100): {len(high_freq_removed)}")
print(high_freq_removed[['keyword', 'frequency_world', 'signature']])
```

### Статистика по группам:

```python
# Распределение размеров групп
group_sizes = groups_df.groupby('signature')['group_size'].first()
print("Распределение размеров групп:")
print(group_sizes.value_counts().sort_index())

# Топ-10 самых больших групп
top_groups = groups_df.groupby('signature').agg({
    'group_size': 'first',
    'frequency_world': 'sum'
}).sort_values('group_size', ascending=False).head(10)
print("\nТоп-10 самых больших групп:")
print(top_groups)
```

---

## ⚙️ Настройка дедупликации

### Отключить продвинутую дедупликацию:

Если нужно оставить все варианты запросов, закомментируйте в `main.py`:

```python
# Продвинутая дедупликация (неявные дубли)
# print("🔄 Поиск неявных дублей...")
# ...
```

### Изменить стоп-слова:

Отредактируйте `keywords_settings/stop_keywords.txt`:

```
# Добавить стоп-слово:
мой
твой

# Удалить стоп-слово:
# для  ← закомментировать
```

**После изменений:** перезапустить анализ

### Изменить стратегию удаления:

В `seo_analyzer/clustering/advanced_deduplicator.py`:

```python
def remove_duplicates(
    self,
    df: pd.DataFrame,
    keep_strategy: str = 'max_freq_random'  # Изменить здесь
):
    ...
```

**Доступные стратегии:**
- `max_freq_random` - оставить все с макс. частотностью кроме одной
- `all_same_random` - если все одинаковые, оставить все кроме одной

---

## 💡 Советы и best practices

### ✅ Рекомендуется:

1. **Всегда проверяйте `implicit_duplicates_groups.csv`**
   - Особенно первый раз для новой ниши
   - Ищите ошибочные объединения

2. **Настраивайте стоп-слова под нишу**
   - Добавьте специфичные предлоги
   - Удалите важные для ниши слова

3. **Проверяйте большие группы**
   - Группы > 10 запросов могут быть ошибочными
   - Возможно, нужны дополнительные стоп-слова

4. **Анализируйте удаленные высокочастотные**
   - Удаленные запросы с частотностью > 100 требуют внимания
   - Возможно, стоит вернуть

### ❌ Не рекомендуется:

1. **Полностью доверять автоматике**
   - Всегда проверяйте результаты
   - Особенно для новых ниш

2. **Удалять слишком много стоп-слов**
   - Можно объединить слишком разные запросы
   - Лучше меньше, чем больше

3. **Игнорировать файлы с дублями**
   - Они созданы для ручной проверки
   - 5-10 минут проверки экономят часы потом

---

## 🐛 Решение проблем

### Проблема: Объединяются разные запросы

**Пример:**
```
"купить скуд"      ← Коммерческий
"установка скуд"   ← Услуга
Объединены в группу: { купить, скуд, установка }
```

**Решение:**
- Добавить "купить" или "установка" в стоп-слова? **НЕТ!**
- Это нормально - разные интенты различаются на уровне классификации
- После дедупликации идет определение интента

### Проблема: Не объединяются похожие запросы

**Пример:**
```
"контроллер СКУД IRZ"    ← Не объединяется
"IRZ контроллер СКУД"    ← с этим
```

**Причина:** Разные бренды или модели

**Решение:**
- Это корректное поведение
- Запросы с конкретными брендами должны быть отдельно

### Проблема: Удален важный запрос

**Решение:**
1. Найти его в `implicit_duplicates_removed.csv`
2. Посмотреть `signature` и группу
3. Проверить в `implicit_duplicates_groups.csv`
4. Если действительно важен - добавить стоп-слова для разделения

---

## 📚 Дополнительные материалы

- [README.md](README.md) - основная документация
- [FILTERING.md](FILTERING.md) - фильтрация запросов
- [CSV_COLUMNS_GUIDE.md](CSV_COLUMNS_GUIDE.md) - описание всех колонок
- [HTML_DASHBOARD_GUIDE.md](HTML_DASHBOARD_GUIDE.md) - работа с дашбордом

---

**Итог:** Продвинутая дедупликация экономит время и улучшает качество семантического ядра, объединяя запросы которые на самом деле ищут одно и то же! 🎯


