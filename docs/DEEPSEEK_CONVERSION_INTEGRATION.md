# 🤖 Интеграция DeepSeek AI для расчёта конверсии

## 📋 Описание

Автоматический расчёт **стоимости лида** и **процента конверсии** на основе анализа топ-15 высокочастотных запросов через DeepSeek AI.

---

## 🎯 Что рассчитывается

### Входные данные:
- **15 самых частотных запросов** из вашего файла (по `frequency_exact`)
- Пример: "скуд купить - 468 показов", "скуд цена - 180 показов"

### Выходные данные:
```json
{
  "lead_cost_rub": 2500,
  "conversion_rate_percent": 2.5
}
```

### Использование результатов:
- `lead_cost_rub` → `direct_cost_per_conversion` (стоимость конверсии)
- `conversion_rate_percent` → расчёт ROI и прибыльности

---

## 🔧 Настройка

### 1. Получите API ключ DeepSeek

1. Зарегистрируйтесь на https://platform.deepseek.com/
2. Получите API ключ
3. Добавьте в `config_local.py`:

```python
DEEPSEEK_API_KEY = "sk-your-api-key-here"
DEEPSEEK_ENABLED = True
DEEPSEEK_TOP_N_QUERIES = 15  # Количество запросов для анализа
```

### 2. Промпт находится в файле

`prompts/deepseek_lead_cost_analysis.txt` — можете редактировать под свои нужды.

---

## 📊 Использование в коде

### Автоматическое использование (рекомендуется)

Добавьте в `pipeline/stages/yandex_direct_enricher.py` перед расчётом метрик:

```python
from seo_analyzer.analysis.deepseek_conversion_estimator import estimate_conversion_for_dataframe

# Получаем конверсию через DeepSeek
if config.get('deepseek_enabled', False):
    lead_cost, conversion_rate = estimate_conversion_for_dataframe(
        analyzer.df,
        api_key=config.get('deepseek_api_key'),
        top_n=config.get('deepseek_top_n', 15)
    )
    
    # Используем полученные значения вместо дефолтных
    avg_check = lead_cost  # или lead_cost * 2 для более консервативной оценки
    conversion_rate = conversion_rate
```

### Ручное использование

```python
from seo_analyzer.analysis.deepseek_conversion_estimator import DeepSeekConversionEstimator

estimator = DeepSeekConversionEstimator(api_key="your_key", top_n=15)

queries = [
    {'keyword': 'скуд купить', 'frequency_exact': 468},
    {'keyword': 'скуд цена', 'frequency_exact': 180},
    # ... ещё 13 запросов
]

lead_cost, conversion_rate = estimator.estimate_from_queries(queries)
print(f"Стоимость лида: {lead_cost} руб")
print(f"Конверсия: {conversion_rate}%")
```

---

## 🔄 Fallback (если DeepSeek недоступен)

При ошибках или отсутствии API ключа используются дефолтные значения:
- `lead_cost_rub`: 2500 руб
- `conversion_rate_percent`: 2.0%

---

## 💰 Стоимость

DeepSeek API очень дёшев:
- ~$0.14 за 1M входных токенов
- ~$0.28 за 1M выходных токенов

**Один запрос:** ~500 токенов = $0.0001 (0.01 руб) 🎉

---

## 🌡️ Параметр Temperature

Используется значение **temperature = 1.0** (рекомендация DeepSeek для Data Analysis).

| Use Case | Temperature | Наш выбор |
|----------|-------------|-----------|
| Coding / Math | 0.0 | |
| **Data Analysis** | **1.0** | **✅ Используем** |
| General Conversation | 1.3 | |
| Translation | 1.3 | |
| Creative Writing | 1.5 | |

Почему 1.0? Наша задача — анализ данных (категория "Data Cleaning / Data Analysis"), где требуется баланс между точностью и учётом контекста.

---

## 🧪 Тестирование

```bash
python -c "
from seo_analyzer.analysis.deepseek_conversion_estimator import DeepSeekConversionEstimator
estimator = DeepSeekConversionEstimator(api_key='your_key')
queries = [{'keyword': 'тест', 'frequency_exact': 100}]
print(estimator.estimate_from_queries(queries))
"
```

---

## ⚙️ Настройка промпта

Редактируйте `prompts/deepseek_lead_cost_analysis.txt` для:
- Изменения инструкций анализа
- Добавления специфики вашей ниши
- Настройки формата ответа

---

## 📝 Примечания

- Анализ выполняется **один раз** при запуске pipeline с `--enable-direct`
- Результаты применяются ко **всем запросам** в DataFrame
- Для разных ниш можно использовать разные промпты

---

## 🚀 Пример запуска

```bash
# С DeepSeek интеграцией
python main.py semantika/скуд.csv --enable-direct

# Система автоматически:
# 1. Отберёт топ-15 ВЧ запросов
# 2. Отправит в DeepSeek
# 3. Получит lead_cost и conversion_rate
# 4. Использует для расчёта Direct метрик
```

---

## ❓ FAQ

**Q: Обязательно ли использовать DeepSeek?**  
A: Нет, это опционально. Без него используются дефолтные значения.

**Q: Можно ли использовать другую AI модель?**  
A: Да, просто измените API_URL и MODEL в `deepseek_conversion_estimator.py`.

**Q: Как часто нужно делать запросы?**  
A: Обычно достаточно 1 раз при первом анализе, затем можно кэшировать результаты.

---

## 📚 Дополнительно

- Документация DeepSeek API: https://platform.deepseek.com/api-docs/
- Конфигурация: `prompts/deepseek_integration_config.txt`
- Код: `seo_analyzer/analysis/deepseek_conversion_estimator.py`

