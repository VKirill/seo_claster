# Кэширование интента в query_cache.db 🚀

## Проблема

При каждом перезапуске анализа интент классифицировался заново для всех 56,923 запросов, что занимало ~9 минут:

```
Классификация интента:   5%|█████▋  | 2724/56923 [00:26<09:08, 98.72it/s]
```

## Решение ✅

Интент теперь **кэшируется в `output/query_cache.db`** вместе с другими предобработанными данными:

- `main_intent` - основной интент (commercial/informational/commercial_geo)
- `commercial_score` - скор коммерческого интента
- `informational_score` - скор информационного интента
- `navigational_score` - скор навигационного интента

## Как это работает

### 1. Первый запуск (классификация)

```bash
python main.py
```

**Вывод:**
```
🔄 Классификация интента...
Классификация интента: 100%|████████████████| 56923/56923 [09:08<00:00, 103.81it/s]
✓ Интент классифицирован
💾 Кэш сохранён: 56923 запросов для группы 'видеонаблюдение'
```

### 2. Второй запуск (из кэша)

```bash
python main.py
```

**Вывод:**
```
✓ Интент загружен из кэша (56923 запросов)
```

⚡ **Мгновенно вместо 9 минут!**

### 3. Частичное обновление

Если добавили новые запросы в CSV:

```
🔄 Классификация интента (50000/56923 из кэша)...
Классификация интента: 100%|████████████████| 6923/6923 [00:40<00:00, 171.23it/s]
✓ Интент классифицирован (6923 новых)
```

## Автоматическая миграция

При первом запуске с обновлённым кодом:

```
🔄 Миграция: добавляем колонки интента в query_cache.db...
✓ Колонки интента добавлены
```

Миграция происходит **автоматически** при инициализации `QueryCacheDatabase`.

## Ручная миграция (опционально)

Если хотите обновить существующую БД вручную:

```bash
python migrate_query_cache_intent.py
```

## Когда кэш НЕ используется

Кэш интента **игнорируется** и пересоздаётся в следующих случаях:

1. **CSV файл изменился** (изменился MD5 hash)
2. **Очистка кэша** командой:
   ```bash
   python manage_query_cache.py
   # Выберите: 3. Очистить кэш группы
   ```
3. **Удаление БД**:
   ```bash
   del output\query_cache.db
   ```

## Схема БД

```sql
CREATE TABLE cached_queries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    group_name TEXT NOT NULL,
    
    -- Исходные данные
    keyword TEXT NOT NULL,
    frequency_world INTEGER DEFAULT 0,
    frequency_exact INTEGER DEFAULT 0,
    
    -- Предобработка
    normalized TEXT,
    lemmatized TEXT,
    words_count INTEGER DEFAULT 0,
    
    -- ⭐ НОВОЕ: Intent classification (кэшируем чтобы не пересчитывать)
    main_intent TEXT,
    commercial_score REAL DEFAULT 0.0,
    informational_score REAL DEFAULT 0.0,
    navigational_score REAL DEFAULT 0.0,
    
    -- Дополнительные поля
    extra_data TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (group_name) REFERENCES query_groups(group_name)
);
```

## Преимущества 🎯

1. **⚡ Скорость** - классификация только один раз
2. **🔄 Переклассификация** - можно быстро пересчитывать кластеры без ожидания
3. **💾 Экономия** - не нужно каждый раз классифицировать 50k+ запросов
4. **📊 Консистентность** - интент одинаковый при каждом запуске
5. **🛠️ Отладка** - можно экспериментировать с настройками кластеризации

## Что кэшируется в query_cache.db

| Этап | Кэшируется | Время экономии |
|------|------------|----------------|
| Нормализация | ✅ normalized, lemmatized | ~30 сек |
| NER | ✅ ner_entities, ner_locations | ~1-2 мин |
| Key Phrases | ✅ main_words, key_phrase | ~10 сек |
| **Интент** | ✅ **main_intent, scores** | **~9 мин** |
| Кластеризация | ❌ Пересчитывается каждый раз | - |
| SERP анализ | ✅ Отдельная БД serp_data.db | ~5-30 мин |

## Логика классификации

```python
# В pipeline/stages/classifier.py

# 1. Проверяем есть ли интент в кэше
if has_cached_intent and all_queries_classified:
    print("✓ Интент загружен из кэша")
    
# 2. Частичная классификация (новые запросы)
elif has_cached_intent and some_queries_missing:
    print(f"🔄 Классификация ({cached}/{total} из кэша)...")
    # Классифицируем только новые
    
# 3. Полная классификация (кэша нет)
else:
    print("🔄 Классификация интента...")
    # Классифицируем все запросы
```

## Связь с другими кэшами

### query_cache.db (быстрая предобработка)
- Нормализация, NER, интент
- Пересоздаётся при изменении CSV

### serp_data.db (SERP данные)
- XML ответы от xmlstock
- TTL 30 дней, не зависит от CSV

### Yandex Direct кэш (частотности)
- CPC, конкуренция, клики
- Общий для всех групп

## Когда нужно сбросить кэш интента

Сбрасывайте кэш если:

1. **Изменили файлы keyword_group/** (коммерческие/инфо маркеры)
2. **Изменили intent_weights.txt** (веса интентов)
3. **Обновили логику IntentClassifier** (новая версия)
4. **Добавили новые города** в `city_aliases.xml`

```bash
# Очистка кэша конкретной группы
python manage_query_cache.py
# → 3. Очистить кэш группы → видеонаблюдение
```

## Производительность

### До (без кэша интента)
```
Загрузка CSV          : 2 сек
Нормализация          : кэш ✅
NER                   : кэш ✅  
Классификация интента : 9 минут ❌
SERP анализ           : кэш ✅
Кластеризация         : 5 минут
──────────────────────────────
ИТОГО                 : ~14 минут
```

### После (с кэшем интента)
```
Загрузка CSV          : 2 сек
Нормализация          : кэш ✅
NER                   : кэш ✅  
Классификация интента : кэш ✅
SERP анализ           : кэш ✅
Кластеризация         : 5 минут
──────────────────────────────
ИТОГО                 : ~5 минут
```

⚡ **Ускорение: 2.8x**

## Примеры использования

### Эксперимент с параметрами кластеризации

```bash
# Запуск 1: min_common_urls = 8
python main.py
# 14 минут (классификация интента + кластеризация)

# Запуск 2: min_common_urls = 10
python main.py  
# 5 минут (только кластеризация, интент из кэша!) ✅

# Запуск 3: min_common_urls = 6
python main.py
# 5 минут (только кластеризация, интент из кэша!) ✅
```

### Обновление CSV файла

```bash
# Добавили 100 новых запросов в semantika/видеонаблюдение.csv
python main.py

# Вывод:
# 🔄 CSV изменился, обновляем кэш...
# 🔄 Классификация интента (56823/56923 из кэша)...
# ✓ Интент классифицирован (100 новых)
```

## Статистика кэша

```bash
# Посмотреть информацию о кэше
python manage_query_cache.py
# → 1. Показать все группы
```

**Вывод:**
```
Группа: видеонаблюдение
├─ Запросов: 56923
├─ CSV hash: a1b2c3d4...
├─ Интент закэширован: ✅
├─ Обновлён: 2025-12-08 15:30:42
└─ Размер: 45.2 MB
```

## См. также

- [QUERY_CACHE_SYSTEM.md](QUERY_CACHE_SYSTEM.md) - Полное описание системы кэширования
- [SERP_KEI_EXCEL_GUIDE.md](SERP_KEI_EXCEL_GUIDE.md) - Кэш SERP данных
- [DATABASE_MIGRATION.md](DATABASE_MIGRATION.md) - Миграции БД

---

**Вопросы?** См. `manage_query_cache.py` или документацию в `seo_analyzer/core/cache/query_cache_db.py`

