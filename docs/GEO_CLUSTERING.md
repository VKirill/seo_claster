# 🌍 Географическая сегментация кластеров

## 🎯 Проблема

**Раньше:** Запросы с разной географией могли попасть в один кластер, если у них были похожие URL:
```
❌ "скуд москва" + "скуд спб" → один кластер
❌ "купить турникет краснодар" + "купить турникет казань" → один кластер
```

Это **неправильно**, потому что:
- Разная география = разная целевая аудитория
- Нужны разные лендинги для разных городов
- SEO оптимизация отличается

## ✅ Решение: Географическая сегментация

**Теперь:** Запросы кластеризуются **отдельно по городам**:
```
✅ "скуд москва" → кластер "Москва - СКУД"
✅ "скуд спб" → кластер "СПб - СКУД"
✅ "купить турникет краснодар" → кластер "Краснодар - турникеты"
✅ "купить турникет казань" → кластер "Казань - турникеты"
```

---

## 🔧 Как это работает

### 1. **Определение географии через Natasha NER**

```python
# Для каждого запроса определяем географию
query_geo_dict = {}
for query in queries:
    geo = semantic_checker.extract_geo(query)  # Natasha NER
    query_geo_dict[query] = geo
    
# Примеры:
"скуд москва" → "москва"
"турникет в спб" → "санкт-петербург"  
"купить контроллер" → None (без географии)
```

### 2. **Группировка по географическим сегментам**

```python
geo_groups = {
    'москва': ['скуд москва', 'турникет москва', ...],
    'санкт-петербург': ['скуд спб', 'контроллер питер', ...],
    'краснодар': ['купить турникет краснодар', ...],
    '__no_geo__': ['скуд цена', 'что такое скуд', ...]  # Без географии
}
```

### 3. **Отдельная кластеризация каждого сегмента**

```python
for geo_segment, geo_queries in geo_groups.items():
    # Кластеризуем только запросы ЭТОГО города
    for query in geo_queries:
        # Ищем похожие запросы ТОЛЬКО среди того же города
        similar = find_similar_queries(query, same_geo=geo_segment)
        # Добавляем в кластер только если та же география
```

### 4. **Фильтрация кандидатов по географии**

```python
# При поиске похожих запросов через URL индекс:
similar_queries = find_similar_queries_fast(query, url_index)

# ФИЛЬТРУЕМ: оставляем только тот же город!
query_geo = "москва"
similar_same_geo = {
    sq: score for sq, score in similar_queries.items()
    if query_geo_dict[sq] == query_geo  # ← ВОТ ТУТ ФИЛЬТР!
}
```

---

## 📊 Пример работы

### Входные запросы:
```
1. скуд москва           (freq: 1000, urls: [a, b, c])
2. система скуд москва   (freq: 800,  urls: [a, b, d])
3. скуд спб              (freq: 900,  urls: [a, b, c])
4. система скуд питер    (freq: 700,  urls: [a, b, e])
5. что такое скуд        (freq: 5000, urls: [x, y, z])
```

### Результат кластеризации:

#### Кластер 1: "Москва - СКУД" ✅
```
- скуд москва (главный)
- система скуд москва
```
**География:** москва  
**Общие URL:** a, b

#### Кластер 2: "СПб - СКУД" ✅
```
- скуд спб (главный)
- система скуд питер
```
**География:** санкт-петербург  
**Общие URL:** a, b

#### Кластер 3: "Что такое СКУД" ✅
```
- что такое скуд
```
**География:** нет  
**Тип:** информационный

---

## 🎨 Новые колонки в DataFrame

После кластеризации добавляются **2 новые колонки**:

### 1. `cluster_geo`
География конкретного запроса (из Natasha NER)

| keyword | cluster_geo |
|---------|-------------|
| скуд москва | москва |
| турникет в спб | санкт-петербург |
| купить скуд | None |

### 2. `cluster_geo_label`
География всего кластера

| keyword | cluster_id | cluster_geo_label |
|---------|-----------|-------------------|
| скуд москва | 1 | москва |
| система скуд москва | 1 | москва |
| скуд спб | 2 | санкт-петербург |
| купить скуд | 3 | None |

---

## 📈 Статистика при запуске

**В консоли вы увидите:**
```
🌍 Определение географии запросов через Natasha NER...
✓ Найдено географических сегментов: 15
  - москва: 234 запросов
  - санкт-петербург: 156 запросов
  - краснодар: 89 запросов
  - казань: 67 запросов
  - новосибирск: 54 запросов
  - без географии: 3892 запросов

🔗 Построение кластеров (balanced режим, FAST, с гео-сегментацией)...
  📍 Кластеризация сегмента: москва (234 запросов)
      Обработано: 234/5292 (4%)
  📍 Кластеризация сегмента: санкт-петербург (156 запросов)
      Обработано: 390/5292 (7%)
  ...
  
✓ Создано кластеров: 1456
✓ Средний размер кластера: 3.6 запросов
✓ Кластеров с географией: 78
✓ Кластеров без географии: 1378
```

---

## 🔥 Преимущества

### 1. **Точность кластеризации**
- ✅ Запросы с разной географией **никогда не смешиваются**
- ✅ Каждый город получает свои уникальные кластеры
- ✅ Легко создавать целевые лендинги под города

### 2. **SEO эффективность**
- ✅ Отдельные страницы для каждого города
- ✅ Локальная оптимизация контента
- ✅ Лучше ранжирование в гео-поиске

### 3. **Контент-стратегия**
- ✅ Понятно какие темы популярны в каком городе
- ✅ Можно приоритизировать крупные города
- ✅ Видно специфику региональных запросов

### 4. **Производительность**
- ⚡ Кластеризация **быстрее** (меньше сравнений внутри сегмента)
- ⚡ Natasha определяет географию **мгновенно** (кэш 10,000 запросов)
- ⚡ URL индекс работает **только внутри сегмента**

---

## 🎯 Use Cases

### Интернет-магазин с доставкой по городам
```
✅ Москва: "купить турникет москва" → отдельная страница
✅ СПб: "купить турникет спб" → отдельная страница  
✅ Краснодар: "купить турникет краснодар" → отдельная страница
```

### Услуги с региональной привязкой
```
✅ "установка скуд москва" 
✅ "монтаж турникета казань"
✅ "настройка контроллера новосибирск"
```

### B2B с филиалами
```
✅ "поставщик скуд санкт-петербург"
✅ "производитель турникетов екатеринбург"
```

---

## 💡 Рекомендации

### 1. Проверяйте колонку `cluster_geo_label`
Она показывает к какому городу относится весь кластер.

### 2. Фильтруйте по городам
```python
# Получить все кластеры Москвы
moscow_clusters = df[df['cluster_geo_label'] == 'москва']

# Топ-10 кластеров по городам
for geo in df['cluster_geo_label'].unique():
    if geo:
        city_df = df[df['cluster_geo_label'] == geo]
        print(f"{geo}: {len(city_df)} запросов")
```

### 3. Экспорт по городам
```python
# Создать отдельные файлы для каждого города
for geo in df['cluster_geo_label'].unique():
    if geo:
        city_df = df[df['cluster_geo_label'] == geo]
        city_df.to_csv(f'clusters_{geo}.csv')
```

---

## 🔧 Настройки

Географическая сегментация **включена по умолчанию** и работает автоматически.

Если нужно отключить:
```python
# В коде кластеризатора
semantic_check = False  # Отключает ВСЮ семантику, включая гео
```

---

## 📚 Связанные документы

- `SERP_CLUSTERING_OPTIMIZATION.md` - Оптимизация скорости
- `OPTIMIZATION_SUMMARY.md` - Общее резюме оптимизаций
- `semantic_checker.py` - Natasha NER для определения географии

---

**Дата:** 7 декабря 2025  
**Версия:** 2.0 с географической сегментацией

