# Условный экспорт колонок Yandex Direct

## 📊 Логика добавления колонок в Excel/CSV

### ⚠️ Важно: `KEI Цена визита` НЕ добавляется

**Причина:** Для расчёта точной цены визита нужны реальные данные CPC из Yandex Direct.  
**Без Direct данных** невозможно корректно рассчитать стоимость визита.

**Решение:** 
- ❌ `KEI Цена визита` **НИКОГДА не добавляется** (нет точных данных)
- ✅ При наличии Direct → добавляются **точные метрики**: `KEI Direct: Прибыльность`, `KEI Direct: Бюджет на 100 кликов`

### Если Client ID/Secret ПУСТЫЕ

```python
YANDEX_DIRECT_CLIENT_ID = ""
YANDEX_DIRECT_CLIENT_SECRET = ""
```

**Результат:**
- ✅ Анализ Yandex Direct **пропускается** без вопросов
- ❌ `KEI Цена визита` **НЕ добавляется** (нет данных для расчёта)
- ❌ Колонки Direct НЕ добавляются

### Если Client ID/Secret ЗАПОЛНЕНЫ, но токена нет

```python
YANDEX_DIRECT_CLIENT_ID = "2bce30a579f546e8ae1f530379caddcd"
YANDEX_DIRECT_CLIENT_SECRET = "3aac3ee9bbfc4fa8afd1f1857f27f284"
YANDEX_DIRECT_TOKEN = ""  # Пусто
```

**При запуске `main.py`:**
```
⚠️  YANDEX DIRECT: Токен не найден

Варианты:
  1. Получить токен сейчас (откроется браузер)
  2. Пропустить (Yandex Direct будет отключен)
  3. Получить позже

Выберите (1/2/3): _
```

**При выборе "2" (пропустить):**
- ✅ Анализ Direct пропускается
- ❌ `KEI Цена визита` **НЕ добавляется** (нет данных для расчёта)
- ❌ Колонки Direct НЕ добавляются

**При выборе "1" (получить токен):**
- ✅ Токен получается и сохраняется
- ✅ Анализ Direct выполняется
- ❌ `KEI Цена визита` НЕ добавляется
- ✅ Добавляются **точные Direct метрики** вместо оценочной цены визита

### Если токен заполнен и есть данные

```python
YANDEX_DIRECT_TOKEN = "y0_AgAAAAB1HybhAAv66QAAAAEIDEnJ..."
```

**Результат:**
- ✅ Анализ Direct выполняется автоматически
- ❌ `KEI Цена визита` НЕ добавляется
- ✅ **Добавляются точные Direct колонки** (если есть данные для запросов)

## 📋 Колонки в Excel/CSV

### Без Direct

```
Запрос
Частота (мир)
Частота (точная)
KEI Эффективность
KEI Стандартный
KEI Конкуренция
...
                         ← KEI Цена визита НЕ добавляется (нет данных)
KEI Потенциал дохода
KEI Синергия
KEI Релевантность
...
```

### С Direct

```
Запрос
Частота (мир)
Частота (точная)
KEI Эффективность
KEI Стандартный
KEI Конкуренция
...
                         ← KEI Цена визита НЕ добавляется
KEI Потенциал дохода
...
Direct: Показы           ← Начинаются точные Direct колонки
Direct: Клики
Direct: CPC средний (₽)
Direct: Premium CTR (%)
Direct: Конкуренция
Direct: Рекомендуемая ставка (₽)
...
KEI Direct: Эффективность          ← Вместо оценочной "Цены визита"
KEI Direct: Качество (0-100)
KEI Direct: Прибыльность (₽)       ← Точный расчёт ROI
KEI Direct: Потенциал трафика
KEI Direct: Бюджет на 100 кликов (₽)  ← Точный расчёт бюджета
```

## 🎯 Почему `KEI Цена визита` НЕ добавляется?

**Причина:** Для корректного расчёта цены визита нужны **реальные данные CPC** из Yandex Direct.

**Проблема старой метрики:**
```python
# Старый расчёт kei_cost_per_visit
competition = serp_docs_count / frequency  # Оценка конкуренции
cost_per_visit = 1000 / (competition + 1)  # Условная метрика ❌
```
Это **грубая оценка**, не отражающая реальную стоимость клика.

**Решение:**
- **БЕЗ Direct:** метрика не добавляется (нет данных для точного расчёта)
- **С Direct:** добавляются точные метрики:
  1. **KEI Direct: Эффективность** - на основе реального CPC
  2. **KEI Direct: Прибыльность** - прогноз прибыли с учетом реальных кликов  
  3. **KEI Direct: Бюджет на 100 кликов** - точный расчет требуемого бюджета

**Итог:** Лучше **не показывать метрику**, чем показывать неточную!

## 🔧 Условие добавления Direct колонок

```python
# В коде экспорта
has_direct_data = 'direct_shows' in df.columns and (df['direct_shows'] > 0).any()

if has_direct_data:
    # Добавляем Direct колонки
    # НЕ добавляем kei_cost_per_visit
else:
    # НЕ добавляем Direct колонки
    # Добавляем kei_cost_per_visit
```

**Проверка:** Хотя бы у одного запроса должны быть показы > 0.

## 💡 Рекомендации

### Для точных данных
```python
YANDEX_DIRECT_ENABLED = True
YANDEX_DIRECT_CLIENT_ID = "ваш_id"
YANDEX_DIRECT_CLIENT_SECRET = "ваш_secret"
# Получите токен: python get_yandex_direct_token.py
```

### Для быстрого анализа без Direct
```python
YANDEX_DIRECT_ENABLED = False
# или оставьте Client ID/Secret пустыми
```

## 📊 Итого

| Условие | KEI Цена визита | Direct колонки | Примечание |
|---------|----------------|----------------|------------|
| Client ID/Secret пустые | ❌ Нет | ❌ Нет | Нет данных для расчёта |
| Есть Client ID/Secret, нет токена, выбрано "пропустить" | ❌ Нет | ❌ Нет | Нет данных для расчёта |
| Есть Client ID/Secret, нет токена, выбрано "получить" | ❌ Нет | ✅ Да | Точные метрики Direct |
| Есть токен, Direct выполнен, есть данные | ❌ Нет | ✅ Да | Точные метрики Direct |
| Есть токен, но нет данных для запросов | ❌ Нет | ❌ Нет | Запросы низкочастотные |

---

**Вывод:** Система автоматически адаптируется под наличие данных Direct! 🎉

