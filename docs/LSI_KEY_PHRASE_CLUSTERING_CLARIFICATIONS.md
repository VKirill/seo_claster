# 📋 Уточнения: LSI фразы, key_phrase и режим BALANCED

## 1. LSI фразы кластера в JSON ✅

### Задача
Добавить LSI фразы на уровень кластера в JSON экспорте.

### Текущее состояние
LSI фразы уже собираются и агрегируются на **этапе 5** pipeline:

```python
# pipeline/stages/lsi_aggregator.py
analyzer.lsi_aggregator = ClusterLSIAggregator(top_n_per_cluster=10)
cluster_lsi = analyzer.lsi_aggregator.aggregate_cluster_lsi(analyzer.df)
analyzer.df['cluster_lsi_phrases'] = ...
```

### Что нужно сделать
1. ✅ LSI фразы уже есть в DataFrame в колонке `cluster_lsi_phrases`
2. ❌ Нужно добавить их в JSON экспорт кластера

### Пример ожидаемой структуры JSON:

```json
{
  "name": "скуд для офиса",
  "target_url": "/skud/",
  "total_frequency": 609,
  "avg_frequency": 50,
  "funnel_stage": "Interest",
  "main_intent": "commercial",
  "cluster_id": 687,
  "keywords_count": 12,
  
  "lsi_phrases": [
    {
      "phrase": "система контроля доступа",
      "frequency": 45,
      "queries_count": 8
    },
    {
      "phrase": "электронный замок",
      "frequency": 32,
      "queries_count": 6
    },
    {
      "phrase": "карточка доступа",
      "frequency": 28,
      "queries_count": 5
    }
  ],
  
  "keywords": [...]
}
```

### Проблема: unique_lsi_phrases пустая

Если таблица `unique_lsi_phrases` пустая, значит:
1. LSI фразы не были извлечены из SERP XML
2. Нужно повторно собрать SERP данные

**Решение**: При отсутствии LSI в БД - повторный сбор SERP данных активирует извлечение LSI.

---

## 2. key_phrase: Natasha или pymorphy3? 🔍

### Вопрос:
> ты точно через natasha это определил?
> `"key_phrase": "контроллер сетевой скуд"`

### Ответ: НЕТ, используется **pymorphy3**, не Natasha!

### Как работает key_phrase:

```python
# seo_analyzer/analysis/key_phrases_extractor.py

class KeyPhrasesExtractor:
    def __init__(self):
        self.morph = pymorphy3.MorphAnalyzer()  # ← pymorphy3!
    
    def extract_key_phrases(self, query: str):
        # 1. Морфологический анализ каждого слова
        for word in words:
            parsed = self.morph.parse(word)[0]
            pos = parsed.tag.POS  # Часть речи
            lemma = parsed.normal_form  # Нормальная форма
            
            if pos == 'NOUN':  # Существительное
                nouns.append(lemma)
        
        # 2. Извлечение именных групп (прилагательное + существительное)
        noun_phrases = self._extract_noun_phrases_simple(word_analysis)
        
        # 3. Определение ключевой фразы
        # Приоритет:
        # - Самая длинная именная группа
        # - Все существительные
        # - Весь запрос
```

### Примеры работы:

| Запрос | Анализ | Ключевая фраза |
|--------|--------|----------------|
| `elsys mb light 2a 00 tп контроллер сетевой скуд` | Существительные: контроллер, скуд<br>Именные группы: "сетевой контроллер" | `контроллер сетевой скуд` |
| `купить скуд в москве` | Глагол: купить<br>Существительное: скуд | `скуд` |
| `электронный замок для офиса` | Именная группа: "электронный замок" | `электронный замок` |

### Где используется Natasha?

**Natasha используется только для географии:**

```python
# seo_analyzer/clustering/semantic_checker.py
from natasha import (
    Segmenter, MorphVocab,
    NewsEmbedding, NewsMorphTagger,
    NewsNERTagger, Doc  # ← Natasha NER
)

def extract_geo(self, query: str) -> Optional[str]:
    """Извлекает географическую сущность через Natasha NER"""
    doc = Doc(query)
    doc.segment(self.segmenter)
    doc.tag_ner(self.ner_tagger)
    
    for span in doc.spans:
        if span.type == 'LOC':  # Локация
            return span.text.lower()
```

### Итог:
- ✅ **key_phrase**: pymorphy3 (морфологический анализ)
- ✅ **geo (география)**: Natasha NER
- ✅ Это правильно и работает корректно!

---

## 3. Режим BALANCED: объяснение "50%" 🎯

### Вопрос:
> не понимаю, что за 50% вообще? Если из 20 URL 8 или более одинаковые, то уже должны попадать в общую группу

### Пояснение:

**Это ДВА РАЗНЫХ условия:**

#### Условие 1: Похожесть двух запросов (попарно)
```
Запрос 1: 20 URL в SERP
Запрос 2: 20 URL в SERP

Общие URL: 8 из 20
→ Если >= 7 общих URL → запросы ПОХОЖИ ✅
```

**Это простое попарное сравнение двух запросов.**

---

#### Условие 2: Добавление запроса в КЛАСТЕР (50%)

Когда запрос хочет войти в **существующий кластер** из нескольких запросов:

```
Кластер уже содержит:
- Запрос A
- Запрос B  
- Запрос C
- Запрос D
(всего 4 запроса)

Новый запрос Z хочет войти в этот кластер:

Проверяем:
Z ↔ A: 8 общих URL ✅ (похожи)
Z ↔ B: 5 общих URL ❌ (не похожи, <7)
Z ↔ C: 9 общих URL ✅ (похожи)
Z ↔ D: 6 общих URL ❌ (не похожи, <7)

Итого: похож на 2 из 4 запросов = 50%
```

#### Режим BALANCED требует:
> Новый запрос должен быть похож минимум на **50% запросов В КЛАСТЕРЕ**

```python
# Расчет:
required_similar = 4 * 0.5 = 2 запроса
similar_count = 2 (Z похож на A и C)

if similar_count >= required_similar:  # 2 >= 2
    return True  # ✅ ДОБАВЛЯЕМ в кластер
```

---

### Почему это важно?

#### Проблема режима SOFT:
```
Кластер: [A, B, C, D, E]

A ↔ B: 8 общих URL ✅
B ↔ C: 8 общих URL ✅
C ↔ D: 8 общих URL ✅
D ↔ E: 8 общих URL ✅

НО! A ↔ E: 0 общих URL ❌

Транзитивное замыкание:
A → B → C → D → E
```

**В SOFT режиме** запросы A и E попадают в один кластер, хотя **у них 0 общих URL!**

**В BALANCED режиме** этого не произойдет, потому что:
- Когда E хочет войти в кластер [A, B, C, D]:
  - E ↔ D: 8 общих URL ✅
  - E ↔ C: 2 общих URL ❌
  - E ↔ B: 0 общих URL ❌
  - E ↔ A: 0 общих URL ❌
  - Похож только на 1 из 4 = 25% < 50% ❌
- E НЕ войдет в кластер!

---

### Визуализация разницы:

#### Попарное условие (>=7 общих URL):
```
Запрос1 (20 URL) ↔ Запрос2 (20 URL)
    ↓ пересечение
  8 общих URL
    ↓
  ✅ ПОХОЖИ (8 >= 7)
```

#### Условие кластера (50% в BALANCED):
```
       Новый запрос
           ↓
    Кластер [A, B, C, D]
           ↓
  Проверяем попарно:
    ✅ A (8 общих)
    ❌ B (5 общих)
    ✅ C (9 общих)
    ❌ D (6 общих)
           ↓
  Похож на 2 из 4 = 50%
           ↓
  ✅ ДОБАВЛЯЕМ в кластер
```

---

### Сравнение режимов на примере:

**Кластер из 10 запросов:**

| Режим | Условие | Пример |
|-------|---------|--------|
| **STRICT** | Похож на **ВСЕ 10** запросов | Очень жесткое требование |
| **BALANCED** | Похож минимум на **5 из 10** (50%) | Умеренное требование |
| **SOFT** | Похож хотя бы на **1 из 10** | Очень мягкое требование |

---

### Код (для ясности):

```python
# seo_analyzer/clustering/serp_advanced_clusterer.py (строка 373-380)

elif self.mode == self.MODE_BALANCED:
    # Должен быть схож с min_cluster_cohesion % запросов
    required_similar = max(1, int(len(cluster_queries) * self.min_cluster_cohesion))
    # len(cluster_queries) = 4
    # min_cluster_cohesion = 0.5
    # required_similar = max(1, 4 * 0.5) = 2
    
    return similar_count >= required_similar
    # similar_count = 2
    # 2 >= 2 → True ✅

else:  # MODE_SOFT
    # Достаточно схожести хотя бы с одним
    return similar_count > 0
    # 2 > 0 → True ✅
```

---

## Итоговые ответы:

### 1️⃣ LSI фразы в JSON
- ✅ Данные есть в DataFrame (`cluster_lsi_phrases`)
- ❌ Нужно добавить в JSON экспорт
- Если таблица пустая → повторный сбор SERP активирует извлечение LSI

### 2️⃣ key_phrase через Natasha?
- ❌ НЕТ, используется **pymorphy3** (морфологический анализ)
- ✅ Natasha используется ТОЛЬКО для географии (`geo`)
- ✅ Это правильный подход!

### 3️⃣ 50% в режиме BALANCED
- **Попарное сравнение**: >=7 общих URL из 20 → похожи
- **Кластер**: новый запрос должен быть похож на >=50% запросов УЖЕ в кластере
- **Защита**: предотвращает транзитивное смешивание несвязанных запросов

---

✅ **Все вопросы прояснены!**

