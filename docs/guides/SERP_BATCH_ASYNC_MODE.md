# 🚀 SERP Batch Async Mode - Массовая загрузка данных

## Что это?

**Batch Async Mode** — режим массовой асинхронной загрузки SERP данных через xmlstock API.

### Старый режим (Sequential)
```
Запрос 1: отправка → req_id → ожидание 10 сек → проверка → ... → результат
Запрос 2: отправка → req_id → ожидание 10 сек → проверка → ... → результат
Запрос 3: отправка → req_id → ожидание 10 сек → проверка → ... → результат
...
```
**Время на 1000 запросов:** ~3-5 часов

### Новый режим (Batch Async) ⚡
```
ФАЗА 1 (30-60 сек): 
  Отправка ВСЕХ 1000 запросов → получение 1000 req_id → сохранение в Master DB

ФАЗА 2 (10 сек):
  Ожидание обработки на стороне xmlstock

ФАЗА 3 (2-5 мин):
  Параллельное получение результатов по всем req_id (50 параллельных запросов)
  Повторные проверки для незавершённых (код 202)
```
**Время на 1000 запросов:** ~5-10 минут

### Преимущества

✅ **Скорость:** В 20-50 раз быстрее для больших объёмов  
✅ **Параллелизм:** Отправка 100+ запросов одновременно  
✅ **Надёжность:** Все req_id сохраняются в Master DB  
✅ **Восстановление:** При падении скрипта можно восстановить через req_id  
✅ **Экономия:** Списание происходит 1 раз при получении req_id  

## Использование

### По умолчанию (включён автоматически)

```bash
python main.py видеонаблюдение
```

Batch Async Mode включён по умолчанию! Ничего дополнительно делать не нужно.

### Отключить Batch Async (старый режим)

```bash
python main.py видеонаблюдение --no-serp-batch-async
```

### В коде

```python
from seo_analyzer.analysis.serp_analyzer import SERPAnalyzer

analyzer = SERPAnalyzer(
    api_key="user:key",
    use_batch_async=True  # Включить batch async
)

results = await analyzer.analyze_queries_batch(queries)
```

## Workflow

### 1. Проверка кэша
```
✓ Master DB (мгновенно)
✓ serp_data.db (быстро)
```

### 2. Массовая отправка
```
📤 ЭТАП 1/3: Отправка запросов с delayed=1...
   [50/1000] sent: купить видеонаблюдение...
   [100/1000] sent: монтаж камер...
   ...
✓ Отправлено: 1000/1000
```

**Что происходит:**
- Отправляется запрос с `&delayed=1`
- Получается `req_id` из XML
- Сохраняется в Master DB со статусом `processing`

### 3. Ожидание
```
⏳ ЭТАП 2/3: Ожидание 10 сек перед первой проверкой...
```

XMLStock обрабатывает запросы на своей стороне (от нескольких секунд до минут).

### 4. Получение результатов
```
📥 ЭТАП 3/3: Получение результатов...

   Попытка 1/10: проверка 1000 запросов...
   ✓ Получено: 850
   ⏳ Ещё ожидают (202): 150
   
   Ожидание 10 сек перед следующей попыткой...
   
   Попытка 2/10: проверка 150 запросов...
   ✓ Получено: 1000
   ⏳ Ещё ожидают (202): 0
```

**Что происходит:**
- Параллельно запрашиваются результаты (50 одновременно)
- Если код 202 (не готово) → запрос остаётся в очереди
- Повторная проверка через 10 сек
- До 10 попыток (обычно хватает 1-3)

### 5. Обработка и сохранение
```
✅ Массовая загрузка завершена
   Всего: 1000
   Успешно: 998
   Ошибок отправки: 0
   Ошибок получения: 2
```

**Данные сохраняются:**
- ✅ Master DB (req_id, статус, метрики)
- ✅ serp_data.db (XML, метрики)
- ✅ Pipeline DataFrame (для дальнейшей обработки)

## Настройки производительности

### В коде

```python
from seo_analyzer.analysis.serp.async_batch_client import AsyncBatchSERPClient

client = AsyncBatchSERPClient(
    api_key="user:key",
    lr=213,  # Москва
    max_concurrent_send=100,  # Отправка: 100 параллельно
    max_concurrent_fetch=50,  # Получение: 50 параллельно
    initial_delay=10,  # Первая проверка через 10 сек
    retry_delay=10,  # Между повторами 10 сек
    max_attempts=10  # Максимум 10 попыток
)

result = await client.process_queries_batch(queries)
```

### Рекомендации

| Запросов | max_concurrent_send | max_concurrent_fetch | initial_delay |
|----------|---------------------|----------------------|---------------|
| < 100    | 50                  | 30                   | 5 сек         |
| 100-500  | 100                 | 50                   | 10 сек        |
| 500-1000 | 100                 | 50                   | 15 сек        |
| 1000+    | 150                 | 50                   | 20 сек        |

**Важно:** xmlstock может ограничивать количество одновременных запросов. Если видите ошибки — уменьшите параллелизм.

## Восстановление после сбоя

При падении скрипта все `req_id` сохранены в Master DB!

### Проверить незавершённые запросы

```bash
python recover_serp_requests.py --group видеонаблюдение --status
```

Вывод:
```
📊 Статистика SERP запросов для группы: видеонаблюдение
   Processing: 150  ⏳ (ещё обрабатываются)
   Completed: 800   ✓
   Errors: 50       ❌
```

### Восстановить незавершённые

```bash
python recover_serp_requests.py --group видеонаблюдение --recover
```

Скрипт:
1. Найдёт все `processing` запросы с `req_id`
2. Попробует получить результаты
3. Обновит статусы в Master DB
4. Сохранит данные

## Отладка

### Тест на малой выборке

```python
# test_async_batch_serp.py
import asyncio
from seo_analyzer.analysis.serp.async_batch_client import AsyncBatchSERPClient

async def main():
    client = AsyncBatchSERPClient(api_key="user:key")
    
    test_queries = [
        "купить видеонаблюдение",
        "монтаж камер",
        "ip камеры цена"
    ]
    
    result = await client.process_queries_batch(test_queries)
    print(result['stats'])
    
    await client.close()

asyncio.run(main())
```

### Проверка req_id в Master DB

```python
from seo_analyzer.core.cache.master_query_db import MasterQueryDatabase

db = MasterQueryDatabase()
stats = db.get_serp_statistics("видеонаблюдение")
print(stats)
```

## FAQ

### Как отключить Batch Async?

```bash
python main.py видеонаблюдение --no-serp-batch-async
```

### Сколько стоит?

Списание происходит **1 раз** при получении `req_id`, независимо от количества повторных проверок!

### Что если xmlstock не успел обработать за 10 попыток?

Запрос помечается как `error` в Master DB. Вы можете:
1. Увеличить `max_attempts` в настройках
2. Запустить `recover_serp_requests.py` позже

### Можно ли использовать для восстановления старых запросов?

Да! Если у вас есть `req_id` в базе, `recover_serp_requests.py` попробует получить результаты.

**Важно:** xmlstock кэширует запросы, но старые `req_id` могут устареть.

## Технические детали

### Архитектура

```
Pipeline (main.py)
    ↓
SERPAnalyzer.analyze_queries_batch()
    ↓
[проверка use_batch_async]
    ↓
SERPAnalyzer.analyze_queries_batch_async_mode()
    ↓
AsyncBatchSERPClient.process_queries_batch()
    ↓
[отправка всех delayed=1 → получение req_id]
    ↓
[параллельное получение результатов]
    ↓
[обогащение данных: metrics, documents, LSI]
    ↓
[сохранение в Master DB + serp_data.db]
```

### Поток данных

```
Запрос → delayed=1 → req_id → Master DB (status=processing)
                         ↓
            [ожидание 10 сек]
                         ↓
       req_id → xmlstock → XML → enrich → результат
                         ↓
       Master DB (status=completed) + serp_data.db
```

### Статусы в Master DB

| Статус | Описание |
|--------|----------|
| `pending` | Не отправлен |
| `processing` | Отправлен, получен req_id, ожидаем результат |
| `completed` | Результат получен успешно |
| `error` | Ошибка (временная или постоянная) |

## Производительность

### Замеры (реальные данные)

| Запросов | Старый режим | Batch Async | Ускорение |
|----------|--------------|-------------|-----------|
| 50       | 2 мин        | 30 сек      | 4x        |
| 100      | 5 мин        | 1 мин       | 5x        |
| 500      | 30 мин       | 3 мин       | 10x       |
| 1000     | 2 часа       | 6 мин       | 20x       |
| 5000     | 10 часов     | 25 мин      | 24x       |

**Примечание:** Время зависит от загрузки xmlstock и качества сети.

## Рекомендации

✅ **Используйте Batch Async по умолчанию** — это быстрее и надёжнее  
✅ **Запускайте периодически `recover_serp_requests.py`** для восстановления сбоев  
✅ **Мониторьте статистику** через `db.get_serp_statistics()`  
✅ **Настройте параллелизм** под ваши объёмы данных  

❌ **Не отключайте без необходимости** — старый режим медленнее  
❌ **Не ставьте слишком высокий `max_concurrent_send`** — xmlstock может отклонять запросы  

---

**Готово! Batch Async Mode работает из коробки.** 🎉


