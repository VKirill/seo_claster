# Режимы SERP кластеризации

## Проблема старого алгоритма

Старый алгоритм использовал **транзитивное замыкание** (компоненты связности):

- Если A похож на B (7+ общих URL)
- И B похож на C (7+ общих URL)
- То A, B, C попадают в ОДИН кластер
- **Даже если A и C имеют всего 1-2 общих URL!**

### Пример проблемы

```
Запросы:
1. "rfid карта скуд"     → URL: [ozon, market, us-plast, wildberries, ...]
2. "rfid ключ скуд"      → URL: [ozon, market, us-plast, smart-card, ...]
3. "rfid браслет скуд"   → URL: [ozon, market, brasletov, zkteco, ...]
```

Старый алгоритм:
- Карта ↔ Ключ: 8 общих URL → **СВЯЗАНЫ**
- Ключ ↔ Браслет: 7 общих URL → **СВЯЗАНЫ**
- **Результат**: все 3 запроса в ОДНОМ кластере ❌

**Но!** Карта и Браслет имеют только 3 общих URL (маркетплейсы), это **РАЗНЫЕ продукты!**

---

## Решение: Продвинутая кластеризация

Новый `AdvancedSERPClusterer` предлагает **3 режима** работы:

### 1️⃣ STRICT (строгий) — **Рекомендуемый**

**Правило:** Запрос добавляется в кластер, только если он схож со **ВСЕМИ** запросами в кластере.

```python
mode="strict"
```

**Пример:**
- Кластер: ["rfid карта скуд"]
- Кандидат: "rfid карта 125 кгц скуд"
- Проверка: схож с КАЖДЫМ запросом в кластере (100%)
- Результат: добавляется ✅

**Применение:**
- Очень точная кластеризация
- Минимум ложных объединений
- Много маленьких кластеров

---

### 2️⃣ BALANCED (сбалансированный)

**Правило:** Запрос должен быть схож минимум с **60%** запросов в кластере.

```python
mode="balanced"
min_cluster_cohesion=0.6  # 60%
```

**Пример:**
- Кластер: ["rfid карта скуд", "rfid карта 125", "rfid карта mifare"]
- Кандидат: "rfid карта em marine"
- Проверка: схож с 2 из 3 запросов (67% > 60%)
- Результат: добавляется ✅

**Применение:**
- Оптимальный баланс между точностью и охватом
- **По умолчанию в системе**
- Предотвращает объединение "карта" + "браслет"

---

### 3️⃣ SOFT (мягкий)

**Правило:** Достаточно схожести хотя бы с **одним** запросом (транзитивное замыкание).

```python
mode="soft"
```

**Пример:**
- Кластер: ["rfid карта скуд", "rfid ключ скуд"]
- Кандидат: "rfid браслет скуд"
- Проверка: схож с "rfid ключ" (7+ URL)
- Результат: добавляется ✅ (даже если не схож с "rfid карта")

**Применение:**
- Максимально широкие кластеры
- Может объединять семантически разные запросы
- Аналог старого алгоритма

---

## Дополнительные улучшения

### ✅ Взвешивание по позициям

URL в топ-3 важнее, чем в топ-30:

```python
position_weights=True

# Веса:
Топ-1 до 3:  вес × 3.0
Топ-4 до 10: вес × 2.0
Топ-11 до 20: вес × 1.5
Топ-21 до 30: вес × 1.0
```

**Эффект:** Запросы с общими URL в топе группируются сильнее.

---

### ✅ Семантический анализ

Автоматическое разделение по **типам продуктов**:

```python
semantic_check=True

# Распознаваемые типы:
карта, браслет, ключ, бейдж, метка, брелок,
считыватель, ридер, антенна, чип
```

**Эффект:** 
- "rfid карта" и "rfid браслет" **НЕ** попадут в один кластер
- Даже если у них много общих URL (маркетплейсы)

---

## Настройка

### config.py

```python
CLUSTERING_CONFIG = {
    "serp_advanced": {
        "min_common_urls": 7,          # Порог схожести
        "top_positions": 30,           # Глубина SERP
        "max_cluster_size": 100,       # Макс. размер кластера
        "mode": "balanced",            # strict/balanced/soft
        "position_weights": True,      # Взвешивание
        "semantic_check": True,        # Семантика
        "min_cluster_cohesion": 0.6,   # Связность (60%)
    }
}
```

### Командная строка

```bash
# Режим balanced (по умолчанию)
python main.py semantika/rfid.csv

# Режим strict (строгая кластеризация)
python main.py semantika/rfid.csv --serp-mode strict

# Режим soft (мягкая кластеризация)
python main.py semantika/rfid.csv --serp-mode soft

# Изменить порог (по умолчанию 7)
python main.py semantika/rfid.csv --serp-similarity-threshold 10

# Использовать старый алгоритм
python main.py semantika/rfid.csv --use-legacy-serp
```

---

## Сравнение результатов

### Старый алгоритм (SOFT транзитивный)

```
Кластер "rfid скуд" (4 запроса):
├─ rfid карта скуд
├─ rfid ключ скуд
├─ rfid браслеты скуд
└─ rfid браслет для скуд
```

❌ Проблема: объединены **РАЗНЫЕ продукты**

---

### Новый алгоритм (BALANCED)

```
Кластер "rfid карта скуд" (1 запрос):
└─ rfid карта скуд

Кластер "rfid ключ скуд" (1 запрос):
└─ rfid ключ скуд

Кластер "rfid браслеты скуд" (2 запроса):
├─ rfid браслеты скуд
└─ rfid браслет для скуд
```

✅ Правильно: каждый тип продукта в своем кластере

---

## Когда использовать каждый режим?

| Режим | Применение | Результат |
|-------|-----------|-----------|
| **STRICT** | Очень узкая семантика (модели, артикулы) | Много маленьких точных кластеров |
| **BALANCED** | Универсальный (рекомендуемый) | Оптимальное разделение |
| **SOFT** | Широкая семантика (общие темы) | Меньше крупных кластеров |

---

## Статистика кластеризации

После кластеризации выводится статистика:

```
📊 Статистика кластеризации:
  • Всего кластеров: 156
  • Средний размер: 3.2
  • Мин/Макс: 1/45
  • Одиночных запросов: 78
```

**Хорошие показатели:**
- Средний размер: 2-5 запросов
- Одиночных: 30-50% от общего числа
- Макс. размер: < 50 запросов

---

## FAQ

### Q: Какой режим выбрать?

**A:** Начните с `balanced` (по умолчанию). Если кластеры слишком большие → `strict`. Если слишком дробные → `soft`.

### Q: Как изменить связность в balanced?

**A:** В `config.py`:
```python
"min_cluster_cohesion": 0.7  # 70% вместо 60%
```

### Q: Как отключить семантический анализ?

**A:** В `config.py`:
```python
"semantic_check": False
```

### Q: Старый алгоритм все еще работает?

**A:** Да, используйте флаг `--use-legacy-serp`.

---

## Тестирование на своих данных

Используйте тестовый скрипт `test_serp_clustering.py` для быстрой проверки:

```python
# Отредактируйте список TEST_QUERIES в файле
TEST_QUERIES = [
    "ваш запрос 1",
    "ваш запрос 2",
    # ...
]

# Запустите
python test_serp_clustering.py
```

Скрипт покажет:
- Попарную схожесть запросов
- Результаты кластеризации в разных режимах
- Статистику по кластерам

---

## Разработка

Для добавления новых типов продуктов:

```python
# seo_analyzer/clustering/serp_advanced_clusterer.py

def _extract_product_type(self, query: str) -> Optional[str]:
    product_types = [
        'карта', 'карты',
        'браслет', 'браслеты',
        # Добавьте свои:
        'наклейка', 'наклейки',
        'стикер', 'стикеры',
    ]
```

---

## Автор

Улучшение разработано для решения проблемы ложного объединения семантически разных запросов через транзитивные связи.

**Версия:** 1.4  
**Дата:** Декабрь 2025

