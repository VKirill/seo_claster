# Система мультигрупповой обработки запросов

## 📋 Обзор

Система позволяет обрабатывать несколько групп запросов с раздельным хранением данных и общей базой знаний для классификации доменов.

## 🎯 Основные возможности

### 1. Множественные группы запросов
- Каждая группа = отдельный CSV файл в `semantika/`
- Автоматическое обнаружение всех групп
- Независимая обработка каждой группы

### 2. Раздельное хранение данных
```
output/
├── groups/
│   ├── skud/                    # Группа "skud"
│   │   ├── serp_data.db        # БД SERP данных группы
│   │   ├── serp_cache/         # Кэш SERP
│   │   ├── seo_analysis.xlsx   # Результаты анализа
│   │   └── ...
│   ├── crm/                     # Группа "crm"
│   │   ├── serp_data.db
│   │   └── ...
│   └── ...
└── global_domain_stats.db       # Общая БД классификации доменов
```

### 3. Глобальная БД классификации доменов
- Накопление статистики по доменам из всех групп
- Автоматическая классификация: коммерческий / информационный
- Улучшение точности с каждой новой группой

## 🚀 Использование

### Добавление новой группы

1. Создайте CSV файл в `semantika/`:
```bash
semantika/
├── skud.csv        # Группа "СКУД"
├── crm.csv         # Группа "CRM"
└── my_group.csv    # Ваша группа
```

2. Формат файла стандартный:
```csv
Запрос;frequency_world;frequency_exact
купить скуд;1000;800
скуд цена;500;400
```

### Команды CLI

#### Показать все группы
```bash
python main.py --list-groups
```

Вывод:
```
📁 Доступные группы запросов (3):

✅ 💾 skud                 (1500 запросов)
      Файл: semantika\skud.csv
      Output: output\groups\skud

✅    crm                  (800 запросов)
      Файл: semantika\crm.csv
      Output: output\groups\crm
```

#### Обработать одну группу
```bash
python main.py --group skud
```

#### Обработать все группы
```bash
python main.py --process-all-groups
```

### Управление группами

#### Показать список групп
```bash
python manage_groups.py list
```

#### Глобальная статистика доменов
```bash
python manage_groups.py stats
```

Вывод:
```
📊 Глобальная статистика доменов

Всего доменов: 1250
Коммерческих: 680
Информационных: 570

Топ-10 коммерческих доменов:
  ozon.ru                        1234 запросов,  95.2% коммерц., 5 групп
  market.yandex.ru               1105 запросов,  92.8% коммерц., 4 групп
  ...
```

#### Информация о домене
```bash
python manage_groups.py domain ozon.ru
```

## 🔧 Архитектура

### Компоненты

#### 1. QueryGroupManager
Управление группами запросов:
```python
from seo_analyzer.core.query_groups import QueryGroupManager

manager = QueryGroupManager()
groups = manager.discover_groups()  # Автообнаружение

# Загрузка группы
group = manager.get_group("skud")
df = manager.load_queries(group)
```

#### 2. GroupDatabaseManager
Управление БД групп:
```python
from seo_analyzer.core.query_groups import GroupDatabaseManager

db_manager = GroupDatabaseManager(group.db_path)

# Обновление статистики домена
db_manager.update_domain_stats(
    domain="ozon.ru",
    query_group="skud",
    is_commercial=True
)

# Получение классификации
info = db_manager.get_domain_classification("ozon.ru")
```

#### 3. EnhancedDomainClassifier
Классификация доменов с глобальной БД:
```python
from seo_analyzer.core.domain_classifier_enhanced import EnhancedDomainClassifier

classifier = EnhancedDomainClassifier(
    group_db_path=group.db_path,
    use_global_db=True
)

# Классификация (использует глобальную БД)
classification = classifier.classify_domain("ozon.ru")
# 'commercial' | 'informational' | 'unknown'

# Подробная информация
info = classifier.get_classification_info("ozon.ru")
# {
#     'classification': 'commercial',
#     'source': 'global_db',
#     'confidence': 0.95,
#     'total_queries': 1234,
#     'groups_count': 5
# }
```

### Структура БД

#### Глобальная БД (global_domain_stats.db)

**Таблица: domain_stats**
```sql
CREATE TABLE domain_stats (
    domain TEXT,
    query_group TEXT,
    commercial_count INTEGER,
    informational_count INTEGER,
    total_queries INTEGER,
    UNIQUE(domain, query_group)
)
```

**Таблица: domain_aggregated_stats**
```sql
CREATE TABLE domain_aggregated_stats (
    domain TEXT PRIMARY KEY,
    total_commercial INTEGER,
    total_informational INTEGER,
    total_queries INTEGER,
    groups_count INTEGER,
    is_commercial BOOLEAN,
    commercial_ratio REAL,
    confidence_score REAL
)
```

## 📊 Workflow

### 1. Добавление новой группы

```bash
# 1. Создаем файл
echo "Запрос;frequency_world;frequency_exact" > semantika/new_group.csv
echo "запрос 1;1000;800" >> semantika/new_group.csv

# 2. Проверяем
python main.py --list-groups

# 3. Обрабатываем
python main.py --group new_group
```

### 2. Автоматическое обновление глобальной БД

При обработке группы:
1. Собираются данные из SERP
2. Извлекаются домены
3. Определяется коммерциальность запроса
4. Обновляется `domain_stats` для группы
5. Пересчитывается `domain_aggregated_stats`

### 3. Использование глобальной статистики

При переанализе или добавлении новой группы:
1. Классификатор использует глобальную БД
2. Точность растет с каждой группой
3. Новые домены добавляются автоматически

## 🎓 Примеры использования

### Пример 1: Обработка группы

```bash
# Обработать группу "skud"
python main.py --group skud

# Результаты в output/groups/skud/
```

### Пример 2: Пакетная обработка

```bash
# Обработать все группы
python main.py --process-all-groups

# Глобальная статистика будет накоплена
python manage_groups.py stats
```

### Пример 3: Анализ домена

```bash
# Узнать классификацию домена
python manage_groups.py domain market.yandex.ru

# Вывод:
# Классификация: Коммерческий
# Коммерциализация: 92.5%
# Всего запросов: 1105
# Групп: 4
```

## 🔍 Логика классификации

### Приоритет источников

1. **Глобальная БД** (наиболее надежно)
   - Данные из всех групп
   - Статистическая уверенность
   
2. **БД группы** (domain_stats)
   - Автоматическая классификация по offer_info
   
3. **TXT файлы** (ручная классификация)
   - `commercial_domains.txt`
   - `informational_domains.txt`

### Алгоритм классификации

```python
# Порог коммерциализации: 60%
commercial_ratio = commercial_count / total_queries

if commercial_ratio > 0.6:
    is_commercial = True
else:
    is_commercial = False

# Confidence зависит от количества наблюдений
confidence = min(1.0, total_queries / 100.0)
```

## ⚙️ Конфигурация

### Пути

```python
# Автоматически генерируемые пути для группы
QueryGroup(
    name="skud",
    input_file=Path("semantika/skud.csv"),
    output_dir=Path("output/groups/skud"),
    db_path=Path("output/groups/skud/serp_data.db"),
    cache_dir=Path("output/groups/skud/serp_cache")
)
```

### Глобальная БД

```python
# Фиксированный путь
GroupDatabaseManager.GLOBAL_DB_PATH = Path("output/global_domain_stats.db")
```

## 🚨 Важные моменты

1. **Обратная совместимость**
   - Обычный режим (без групп) работает как раньше
   - Один файл → `output/`
   - Группы → `output/groups/{group_name}/`

2. **Автоматическое обновление**
   - Глобальная БД обновляется после каждого анализа
   - Не требуется ручное вмешательство

3. **Переанализ**
   - При переанализе группы используется накопленная статистика
   - Точность классификации улучшается

## 📚 API Reference

### QueryGroupManager

```python
manager = QueryGroupManager()

# Обнаружение групп
groups = manager.discover_groups()

# Добавить группу
group = manager.add_group("name", Path("file.csv"))

# Получить группу
group = manager.get_group("name")

# Загрузить запросы
df = manager.load_queries(group)

# Список групп
info = manager.list_groups()
```

### GroupDatabaseManager

```python
db = GroupDatabaseManager(db_path)

# Обновить статистику домена
db.update_domain_stats(domain, group_name, is_commercial)

# Пакетное обновление
db.batch_update_domains(domains_data, group_name)

# Получить классификацию
info = db.get_domain_classification(domain)
```

### EnhancedDomainClassifier

```python
classifier = EnhancedDomainClassifier(
    group_db_path=db_path,
    use_global_db=True
)

# Классифицировать
result = classifier.classify_domain(domain)

# Подробная информация
info = classifier.get_classification_info(domain)
```

## 🎯 Преимущества

1. **Модульность** - Каждая группа независима
2. **Масштабируемость** - Легко добавлять новые группы
3. **Переиспользование** - Общая база знаний
4. **Точность** - Улучшается с каждой группой
5. **Прозрачность** - Видно источник классификации

