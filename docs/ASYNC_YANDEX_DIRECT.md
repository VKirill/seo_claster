# Асинхронный сбор данных Yandex Direct

## 🎯 Что изменилось

Теперь сбор данных из Yandex Direct API происходит **параллельно** с остальным анализом, что существенно ускоряет общее время выполнения.

## ⚡ Как это работает

### До изменений:
```
1. Загрузка данных
2. Предобработка
3. SERP анализ
4. Кластеризация
5. Классификация
6. Расчет метрик
7. **Yandex Direct** ← ждали завершения всех предыдущих этапов
8. Экспорт
```

### После изменений:
```
1. Загрузка данных
2. Предобработка
   🚀 YANDEX DIRECT ЗАПУЩЕН В ФОНЕ (параллельно)
3. SERP анализ ┐
4. Кластеризация │ 
5. Классификация ├─ Пока выполняются эти этапы,
6. Расчет метрик ┘   Yandex Direct собирает данные
   ⏳ Ожидание завершения Yandex Direct (если еще не готов)
7. Экспорт ← данные Direct уже готовы!
```

## 📊 Преимущества

1. **Экономия времени**: Пока идет анализ (кластеризация, классификация, метрики), Yandex Direct параллельно собирает бюджеты
2. **К моменту экспорта**: Данные Direct уже готовы и сразу попадают в файлы
3. **Умная проверка**: Если Direct отключен - фоновая задача не запускается

## 🔧 Настройка

Никаких изменений в настройках не требуется! Все работает автоматически если:

```python
# config_local.py
YANDEX_DIRECT_ENABLED = True
YANDEX_DIRECT_CLIENT_ID = "..."
YANDEX_DIRECT_CLIENT_SECRET = "..."
```

## 💡 Технические детали

- Используется `asyncio.create_task()` для запуска фоновой задачи
- Задача запускается после **Этапа 2** (после фильтрации мусорных запросов)
- Перед экспортом pipeline **ожидает** завершения задачи с помощью `await`
- Если Direct отключен или произошла ошибка - pipeline продолжает работу без него

## 📝 Файлы изменений

- `pipeline/analyzer.py` - добавлен запуск фоновой задачи
- `pipeline/stages/yandex_direct_enricher.py` - рефакторинг проверки конфигурации

