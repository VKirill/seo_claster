# 🎯 Фильтрация запросов с нулевой частотностью

## Что это?

Начиная с этой версии, анализатор **автоматически удаляет запросы с нулевой частотностью** (`frequency_world = 0`).

## Почему это нужно?

Запросы с нулевой частотностью в колонке "!Max частотность !Весь !мир" = 0:

- ❌ **Не имеют поискового трафика**
- ❌ **Не приносят посетителей на сайт**
- ❌ **Увеличивают время анализа** (кластеризация, embeddings, граф)
- ❌ **Искажают статистику** кластеров и тем
- ❌ **Занимают место** в выгрузках и отчетах

## Как это работает?

### По умолчанию (автоматически)

Просто запустите анализ:

```bash
python main.py
```

или

```batch
run.bat
```

**Результат:**
```
🔧 ЭТАП 2: Предобработка
--------------------------------------------------------------------------------
✓ Отфильтровано 15 запросов со стоп-словами
✓ Удалено 234 запросов с нулевой частотностью    ← Автоматически!
✓ Удалено 0 слишком коротких запросов
```

### Проверка перед анализом

Узнайте, сколько у вас "пустышек":

```bash
python test_quick.py
```

**Вывод:**
```
✅ Тест 3: Статистика данных
   Всего запросов: 11949
   Запросов с нулевой частотностью: 234    ← Вот сколько будет удалено!
   Общая частотность: 2,453,498
```

## Настройка фильтра

### Вариант 1: Убрать только нули (по умолчанию)

```bash
python main.py --min-frequency 1
```

### Вариант 2: Убрать низкочастотные (< 10)

```bash
python main.py --min-frequency 10
```

Удалит все запросы с частотностью меньше 10.

### Вариант 3: Только популярные (>= 100)

```bash
python main.py --min-frequency 100
```

Анализирует только запросы с частотностью 100 и выше.

### Вариант 4: Оставить все (включая нули)

```bash
python main.py --min-frequency 0
```

⚠️ **Не рекомендуется!** Увеличит время анализа без пользы.

## Примеры использования

### Стандартный анализ

```bash
# Запустить с дефолтными настройками (убрать нули)
run.bat
```

### Быстрый анализ (убрать низкочастотники)

```bash
# Минимум 20 показов + быстрые опции
run.bat --min-frequency 20 --skip-embeddings --skip-graph
```

### Фокус на топовых запросах

```bash
# Только запросы с частотностью >= 50
run.bat --min-frequency 50
```

### Полный анализ всего

```bash
# Включить все запросы (даже с нулями)
run.bat --min-frequency 0
```

## Что происходит с удаленными запросами?

Они **НЕ попадут**:
- ❌ В выгрузки CSV (`seo_analysis_full.csv`, `seo_analysis_top1000.csv`)
- ❌ В кластеры и темы
- ❌ В граф связей
- ❌ В дашборд HTML
- ❌ В статистику

Они **не учитываются** при:
- Подсчете общей частотности
- Расчете средних значений
- Построении кластеров
- Определении тем

## Статистика фильтрации

После анализа смотрите файл `output/statistics.json`:

```json
{
  "preprocessing": {
    "original_queries": 11949,
    "removed_stopwords": 389,
    "removed_zero_frequency": 234,
    "removed_duplicates": 1110,
    "final_unique_queries": 10450
  }
}
```

## Рекомендации

| Ситуация | Команда | Причина |
|----------|---------|---------|
| **Обычный анализ** | `run.bat` | Дефолтные настройки оптимальны |
| **Большая семантика (>50k)** | `run.bat --min-frequency 10` | Убрать шум и ускорить |
| **Фокус на топе** | `run.bat --min-frequency 100` | Только популярные запросы |
| **Проверка на мусор** | `python test_quick.py` | Посмотреть статистику |
| **Первый раз** | `run.bat` | Стандартные настройки |

## Часто задаваемые вопросы

### Q: Можно ли вернуть удаленные запросы?

**A:** Да, запустите с `--min-frequency 0`

### Q: Как узнать, сколько удалится?

**A:** Запустите `python test_quick.py` - увидите статистику

### Q: Что если у меня все запросы с нулями?

**A:** Проверьте корректность CSV файла. Колонка должна называться:
- `frequency_world`
- `частотность`
- `Max частотность Весь мир`

### Q: Фильтр работает для обеих колонок частотности?

**A:** Нет, только для `frequency_world`. Колонка `frequency_exact` ("!") не фильтруется.

### Q: Замедлит ли это анализ?

**A:** Наоборот, ускорит! Меньше запросов = быстрее кластеризация.

## Дополнительные материалы

- 📖 Полное руководство: [FILTERING.md](FILTERING.md)
- 📖 Документация: [README.md](README.md)
- 📖 Примеры использования: [USAGE_GUIDE.md](USAGE_GUIDE.md)

---

**Итог:** По умолчанию анализатор убирает "пустышки" автоматически. Это правильно и полезно! 🎯


