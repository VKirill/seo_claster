# 🎯 Фильтрация по соотношению частотностей

## Что это?

Новый фильтр, который автоматически удаляет "мусорные" запросы на основе формулы:

```
Соотношение = Частотность / Точная частотность (!)
```

Если соотношение **> 51**, запрос удаляется как мусорный.

## Зачем это нужно?

### Проблема

В семантическом ядре часто попадают запросы с **огромным разрывом** между общей и точной частотностью:

| Запрос | Частотность | Точная (!) | Соотношение | Проблема |
|--------|-------------|------------|-------------|----------|
| "телефон" | 100,000 | 500 | **200** | Слишком общий |
| "apple" | 500,000 | 1,000 | **500** | Неоднозначный |
| "система" | 200,000 | 800 | **250** | Бесполезный |
| "купить телефон iphone 13" | 1,000 | 500 | **2** | ✅ Хороший! |

### Решение

Такие запросы **невозможно оптимизировать**:
- ❌ Слишком конкурентные
- ❌ Неконкретные (много значений)
- ❌ Низкая конверсия
- ❌ Плохой ROI

Фильтр их автоматически удаляет!

## Как это работает?

### Автоматически (по умолчанию)

```bash
python main.py
```

Удалит запросы с соотношением > 51.

**Результат:**
```
🔧 ЭТАП 2: Предобработка
--------------------------------------------------------------------------------
✓ Отфильтровано 15 запросов со стоп-словами
✓ Удалено 0 запросов с нулевой частотностью
✓ Удалено 127 запросов с соотношением частотностей > 51    ← Автоматически!
✓ Удалено 0 слишком коротких запросов
```

### С настройкой

```bash
# Строже (удалит больше мусора)
python main.py --max-frequency-ratio 20

# Мягче (удалит меньше)
python main.py --max-frequency-ratio 100

# Отключить (не рекомендуется)
python main.py --max-frequency-ratio 999999
```

## Проверка данных

### Перед запуском

```bash
python test_quick.py
```

**Вывод:**
```
✅ Тест 3: Статистика данных
   Всего запросов: 11949
   Запросов с соотношением > 51: 127           ← Будут удалены!
   Среднее соотношение: 3.5
   Максимальное соотношение: 245.8             ← Есть мусор!
```

**Интерпретация:**
- Среднее < 10 → **отличная семантика** ✅
- Среднее 10-30 → **хорошая семантика** ✅
- Среднее 30-50 → **есть мусор** ⚠️
- Среднее > 50 → **много мусора** ❌
- Максимальное > 100 → **точно есть мусор** ❌

## Примеры использования

### Стандартный анализ

```batch
run.bat
```

Применит: `--max-frequency-ratio 51` (по умолчанию)

### Чистая продакшн-семантика

```batch
run.bat --max-frequency-ratio 30
```

Удалит больше мусора, оставит только конкретные запросы.

### Строгая фильтрация

```batch
run.bat --max-frequency-ratio 20
```

Только очень конкретные запросы с точной частотностью.

### Полная очистка семантики

```batch
run.bat --min-frequency 5 --max-frequency-ratio 30
```

Удалит:
- Запросы с частотностью < 5
- Запросы с соотношением > 30

## Примеры запросов

### ✅ Хорошие запросы (соотношение ≤ 51)

| Запрос | Частотность | Точная (!) | Соотношение |
|--------|-------------|------------|-------------|
| "купить телефон iphone 13 pro max" | 1,000 | 800 | 1.25 |
| "установка скуд цена москва" | 500 | 300 | 1.67 |
| "обслуживание систем скуд" | 200 | 150 | 1.33 |
| "считыватель rfid для скуд" | 150 | 100 | 1.50 |

### ❌ Плохие запросы (соотношение > 51)

| Запрос | Частотность | Точная (!) | Соотношение | Почему плохой? |
|--------|-------------|------------|-------------|----------------|
| "система" | 200,000 | 800 | 250 | Слишком общий |
| "скуд" | 50,000 | 500 | 100 | Неконкретный |
| "телефон" | 100,000 | 1,000 | 100 | Бесполезный |
| "apple" | 500,000 | 2,000 | 250 | Много значений |

## Когда использовать разные пороги?

### Порог 20 (строгий)

**Для:**
- E-commerce с конкретными товарами
- Узкоспециализированные ниши
- Когда нужны только готовые к покупке запросы

**Пример:**
```bash
run.bat --max-frequency-ratio 20
```

### Порог 51 (по умолчанию)

**Для:**
- Обычных проектов
- Сбалансированной семантики
- Первичного анализа

**Пример:**
```bash
run.bat
```

### Порог 100 (мягкий)

**Для:**
- Информационных сайтов
- Когда нужны широкие запросы
- Анализа всей семантики

**Пример:**
```bash
run.bat --max-frequency-ratio 100
```

## Комбинации фильтров

### Для коммерческих проектов

```bash
run.bat --min-frequency 10 --max-frequency-ratio 30
```

Оставит только:
- Популярные запросы (≥ 10 показов)
- Конкретные запросы (соотношение ≤ 30)

### Для крупной семантики

```bash
run.bat \
  --min-frequency 20 \
  --max-frequency-ratio 40 \
  --skip-embeddings \
  --skip-graph
```

Быстрый анализ с хорошей очисткой.

### Для топовых запросов

```bash
run.bat --min-frequency 100 --max-frequency-ratio 20
```

Только самые популярные и конкретные запросы.

## Математика фильтра

### Формула

```
Если (frequency_exact > 0) И (frequency_world / frequency_exact > max_ratio):
    Удалить запрос
Иначе:
    Оставить запрос
```

### Особые случаи

1. **Точная частотность = 0**
   - Запрос НЕ удаляется (нет данных для расчета)
   - Но может быть удален фильтром `--min-frequency`

2. **Обе частотности = 0**
   - Удаляется фильтром `--min-frequency 1`

3. **Соотношение = 1.0**
   - Идеальный запрос! Частотность = точной частотности
   - Всегда проходит фильтр

## Часто задаваемые вопросы

### Q: Почему именно 51?

**A:** Эмпирически определенный порог. Запросы с соотношением > 50 обычно:
- Слишком общие
- Имеют много значений
- Сложны в оптимизации
- Имеют низкую конверсию

### Q: Можно ли отключить этот фильтр?

**A:** Да: `--max-frequency-ratio 999999`

Но не рекомендуется! Мусор останется в семантике.

### Q: Влияет ли это на скорость?

**A:** Да, ускоряет! Меньше запросов = быстрее кластеризация.

### Q: Что если у меня все запросы хорошие?

**A:** Отлично! Фильтр ничего не удалит. Проверьте через `test_quick.py`.

### Q: Какой порог лучше?

**A:**
- **Новичкам:** 51 (по умолчанию)
- **Опытным:** 30-40
- **Экспертам:** 20-30

## Дополнительные материалы

📖 **Документация:**
- [FILTERING.md](FILTERING.md) - полное руководство по всем фильтрам
- [FILTER_ZERO_FREQUENCY.md](FILTER_ZERO_FREQUENCY.md) - фильтр по частотности
- [README.md](README.md) - основная документация

📖 **Примеры:**
- [USAGE_GUIDE.md](USAGE_GUIDE.md) - руководство пользователя
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт

---

**Итог:** Фильтр по соотношению автоматически очищает семантику от мусора. Это правильно и полезно! 🎯


