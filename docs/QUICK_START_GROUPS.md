# Быстрый старт: Система групп запросов

## 🚀 За 5 минут

### 1. Создайте группы запросов

Добавьте CSV файлы в папку `semantika/`:

```
semantika/
├── skud.csv       # Уже существует
└── crm.csv        # Создайте новый
```

Формат файла `crm.csv`:
```csv
Запрос;frequency_world;frequency_exact
купить crm систему;1200;950
crm для бизнеса;800;650
внедрение crm;600;480
```

### 2. Проверьте список групп

```bash
python main.py --list-groups
```

Вывод:
```
📁 Доступные группы запросов (2):

✅    skud                 (1500 запросов)
      Файл: semantika\skud.csv
      Output: output\groups\skud

✅    crm                  (3 запросов)
      Файл: semantika\crm.csv
      Output: output\groups\crm
```

### 3. Обработайте группу

```bash
# Одну группу
python main.py --group crm

# Все группы
python main.py --process-all-groups
```

### 4. Проверьте результаты

```bash
# Структура output
output/
├── groups/
│   ├── skud/
│   │   ├── serp_data.db
│   │   ├── seo_analysis.xlsx
│   │   └── ...
│   └── crm/
│       ├── serp_data.db
│       ├── seo_analysis.xlsx
│       └── ...
└── global_domain_stats.db  # Общая БД
```

### 5. Посмотрите статистику

```bash
# Глобальная статистика доменов
python manage_groups.py stats

# Информация о домене
python manage_groups.py domain ozon.ru
```

## 📋 Основные команды

### Работа с группами

```bash
# Показать все группы
python main.py --list-groups

# Обработать одну группу
python main.py --group <название>

# Обработать все группы
python main.py --process-all-groups
```

### Управление

```bash
# Список групп
python manage_groups.py list

# Глобальная статистика
python manage_groups.py stats

# Информация о домене
python manage_groups.py domain <домен>
```

## 🎯 Преимущества

✅ **Раздельное хранение** - Каждая группа в своей папке  
✅ **Общая база знаний** - Классификация доменов улучшается  
✅ **Легко добавлять** - Просто создайте CSV файл  
✅ **Автоматика** - Группы обнаруживаются автоматически  

## 📚 Подробная документация

См. [docs/guides/MULTI_GROUP_SYSTEM.md](guides/MULTI_GROUP_SYSTEM.md)

